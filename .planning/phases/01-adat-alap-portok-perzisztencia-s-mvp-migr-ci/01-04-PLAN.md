---
phase: 01-adat-alap-portok-perzisztencia-s-mvp-migr-ci
plan: 04
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/domain/ports/ContentPort.ts
  - src/domain/ports/ExportPort.ts
  - src/domain/ports/LlmPort.ts
  - src/domain/ports/SyncPort.ts
  - src/adapters/llm/noop.ts
  - src/adapters/sync/noop.ts
  - src/app/container.ts
  - src/app/container.test.ts
autonomous: true
requirements: [PREP-01, PREP-02]
must_haves:
  truths:
    - "Mind az 5 port (Storage, Content, Export, Llm, Sync) TypeScript interfészként létezik a domain rétegben"
    - "Az app alapból NoopLlmAdapter + NoopSyncAdapter mögött fut; nincs élő LLM/sync kód, ami a mérföldkövön kívüli funkciót valósítana meg"
    - "A NoopSyncAdapter nem tesz semmit érdemben (pending() mindig üres tömb), a dirty-könyvelést a StorageAdapter már önmagában végzi"
  artifacts:
    - path: "src/domain/ports/LlmPort.ts"
      provides: "LlmPort interfész"
    - path: "src/domain/ports/SyncPort.ts"
      provides: "SyncPort interfész + ChangeLogEntry típus"
    - path: "src/adapters/llm/noop.ts"
      provides: "NoopLlmAdapter"
    - path: "src/adapters/sync/noop.ts"
      provides: "NoopSyncAdapter"
    - path: "src/app/container.ts"
      provides: "container objektum (storage/llm/sync + content/export típus-only)"
  key_links:
    - from: "src/app/container.ts"
      to: "src/adapters/llm/noop.ts"
      via: "llm: NoopLlmAdapter binding, config.llmEnabled=false"
      pattern: "NoopLlmAdapter"
    - from: "src/app/container.ts"
      to: "src/adapters/sync/noop.ts"
      via: "sync: NoopSyncAdapter binding"
      pattern: "NoopSyncAdapter"
---

<objective>
Ez a plan definiálja a fennmaradó 4 portot (Content, Export, Llm, Sync — a Storage már megvan 01-01-ből), létrehozza a `NoopLlmAdapter`-t és a `NoopSyncAdapter`-t (Null Object minta), és kiegészíti a 01-01-ben létrehozott `app/container.ts`-t egy teljes kompozíciós gyökérré, ami mind az 5 portot huzalozza (a Content/Export portokat egyelőre csak TÍPUS-szinten, adapter nélkül, mert a tényleges tartalom-katalógus/export-logika Phase 3-4 tárgya).

EXPLICIT NEM-CÉL (PITFALLS.md Pitfall 11 — scope-robbanás elleni védelem): ez a plan NEM épít valódi sync-transportot, konfliktus-feloldást, auth-rendszert, vagy élő LLM-hívást. Kizárólag a portok és a Null Object alapértelmezés készül el — az app "AI és sync nélkül is teljes" invariánsa erre a plan-re épül.

Purpose: PREP-01/PREP-02 — az LLM és a sync port mögött van, kikapcsolható/hiányzó implementációval is teljes értékű az app.
Output: `domain/ports/{Content,Export,Llm,Sync}Port.ts`, `adapters/llm/noop.ts`, `adapters/sync/noop.ts`, kiegészített `app/container.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/ARCHITECTURE.md
@.planning/phases/01-adat-alap-portok-perzisztencia-s-mvp-migr-ci/01-PATTERNS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ContentPort, ExportPort, LlmPort, SyncPort + NoopLlmAdapter + NoopSyncAdapter</name>
  <files>src/domain/ports/ContentPort.ts, src/domain/ports/ExportPort.ts, src/domain/ports/LlmPort.ts, src/domain/ports/SyncPort.ts, src/adapters/llm/noop.ts, src/adapters/sync/noop.ts</files>
  <behavior>
    - `NoopLlmAdapter.enrichSpec(x)` az `x` bemenetet változatlanul adja vissza (identitás-függvény — a determinisztikus pipeline sosem függ az LLM-től)
    - `NoopLlmAdapter.suggestFollowups(...)` mindig üres tömböt ad
    - `NoopSyncAdapter.pending()` mindig üres tömböt ad; `markDirty()` egy no-op Promise-t ad vissza (nem dob, nem csinál semmit — a valós dirty-bumpolást a StorageAdapter.put()/softDelete() már elvégezte 01-01/01-02-ben)
  </behavior>
  <read_first>
    .planning/research/ARCHITECTURE.md (Pattern 4 — SyncPort/outbox kód, Pattern 5 — LlmPort/NoopLlmAdapter kód, PONTOS célalak),
    .planning/phases/01-adat-alap-portok-perzisztencia-s-mvp-migr-ci/01-PATTERNS.md ("No Analog Found" szakasz — ContentPort/ExportPort/LlmPort/SyncPort/noop.ts indoklása)
  </read_first>
  <action>
    Hozd létre `src/domain/ports/LlmPort.ts`-t: `LlmPort` interfész 3 metódussal — `rateAnswer(question: unknown, answer: unknown): Promise<{ level: "unknown" | "weak" | "strong" }>`, `suggestFollowups(context: unknown): Promise<string[]>`, `enrichSpec<T>(spec: T): Promise<T>` (a bemeneti típussal megegyező kimenet — a Noop-implementáció identitás kell legyen).

    Hozd létre `src/domain/ports/SyncPort.ts`-t: exportálj egy `ChangeLogEntry` típust (`{ id: string; changedAt: string }` minimális alak — a teljes outbox-séma a sync-mérföldkő tárgya), és a `SyncPort` interfészt 2 metódussal — `markDirty(id: string): Promise<void>`, `pending(): Promise<ChangeLogEntry[]>`. EXPLICIT NE adj hozzá `push()`/`pull()` metódust (ARCHITECTURE.md Pattern 4 szerint ezt szándékosan a sync-mérföldkő adja hozzá).

    Hozd létre `src/domain/ports/ContentPort.ts`-t: minimális, előretekintő interfész — `forQuestion(questionId: string): Promise<unknown | null>` (a visszatérési típus egyelőre `unknown`, mert a coaching-tartalom konkrét alakja Phase 2-3 tárgya; NE találj ki most egy `CoachingContent` típust, ami feltételezné a leendő adatmodellt).

    Hozd létre `src/domain/ports/ExportPort.ts`-t: minimális interfész — `serialize(viewModel: unknown, format: "markdown" | "pdf" | "excel"): Promise<Blob>` (a `viewModel` alakja Phase 4 tárgya, itt csak a port-szerződés kontúrja készül).

    Hozd létre `src/adapters/llm/noop.ts`-t: exportáld a `NoopLlmAdapter: LlmPort` konstans objektumot — `rateAnswer` mindig `{ level: "unknown" }`-t ad, `suggestFollowups` mindig `[]`-t, `enrichSpec` a bemenetet változatlanul adja vissza (identitás).

    Hozd létre `src/adapters/sync/noop.ts`-t: exportáld a `NoopSyncAdapter: SyncPort` konstans objektumot — `markDirty` egy azonnal felbontott Promise-t ad (nem csinál semmit), `pending` mindig `[]`-t ad.

    Domain-tisztasági szabály: a 4 port-fájl egyike se importáljon `adapters/`-ből, `rxdb`-ből vagy `react`-ből.
  </action>
  <acceptance_criteria>
    - `grep -RcE "from ['\"](rxdb|dexie|react|\\.\\./\\.\\./adapters)" src/domain/ports/` egyenlő 0-val
    - Vitest teszt: `NoopLlmAdapter.enrichSpec({foo: 1})` mélyen egyenlő `{foo: 1}`-gyel
    - Vitest teszt: `NoopSyncAdapter.pending()` felbontva `[]`-t ad
    - `pnpm exec tsc --noEmit` nem jelez hibát a `src/domain/ports/` és `src/adapters/llm/`, `src/adapters/sync/` fájlokra
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec tsc --noEmit 2>&1 | grep -E "domain/ports|adapters/(llm|sync)" ; pnpm test -- noop --run 2>&1 | tail -20</automated>
  </verify>
  <done>Mind a 4 fennmaradó port definiálva, a két Noop-adapter identitás/üres viselkedéssel bizonyítottan működik.</done>
</task>

<task type="auto">
  <name>Task 2: Kompozíciós gyökér bővítése — container.ts mind az 5 porttal</name>
  <files>src/app/container.ts, src/app/container.test.ts</files>
  <read_first>
    src/app/container.ts (01-01-ben létrehozott `createStorageAdapter(storage)` factory),
    .planning/research/ARCHITECTURE.md (Pattern 5 — "app/container.ts — a döntés EGY helyen van" kód-idézet)
  </read_first>
  <action>
    Bővítsd `src/app/container.ts`-t: exportálj egy `config` objektumot `{ llmEnabled: false }`-fel (a jövőbeli `LiveLlmAdapter`-váltás kapcsolópontja — kommenttel jelezve, hogy jelenleg nincs élő ág, mert nincs `LiveLlmAdapter` a kódbázisban). Exportálj egy async `createContainer(storage: RxStorage)` függvényt, ami visszaad egy objektumot `{ storage: StoragePort, llm: LlmPort, sync: SyncPort }`-tal: `storage` a meglévő `createStorageAdapter(storage)` eredménye, `llm` mindig `NoopLlmAdapter` (a `config.llmEnabled` jelenleg nem ágaztat, mert nincs alternatíva — a kommentben jelezd, hogy Phase 8/v2 fogja ezt tényleges feltételes logikává bővíteni), `sync` mindig `NoopSyncAdapter`. A `content`/`export` portokat NE vedd fel a visszaadott objektumba (nincs még konkrét adapter) — helyette exportálj egy külön, típus-only kommentet/JSDoc-jegyzetet a fájl tetején, ami megnevezi, hogy a `ContentPort`/`ExportPort` bekötése Phase 3 illetve Phase 4 tárgya.
  </action>
  <acceptance_criteria>
    - `container.test.ts`: `createContainer(getRxStorageMemory())` felbontása után `result.llm === NoopLlmAdapter` (referenciális egyenlőség, mert konstans objektum), `result.sync.pending()` felbontva `[]`
    - `result.storage.list()` hívható és felbontódik (smoke-teszt, nem dob)
    - `pnpm exec tsc --noEmit` nem jelez hibát a `src/app/container.ts`-re
  </acceptance_criteria>
  <verify>
    <automated>pnpm test -- container --run 2>&1 | tail -20</automated>
  </verify>
  <done>A kompozíciós gyökér mind az 5 portot ismeri; Storage/Llm/Sync ténylegesen be van kötve, Content/Export dokumentáltan később következik.</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Type | Exports |
|---|---|---|
| `src/domain/ports/LlmPort.ts` | interface | `LlmPort` |
| `src/domain/ports/SyncPort.ts` | interface | `SyncPort`, `ChangeLogEntry` |
| `src/domain/ports/ContentPort.ts` | interface | `ContentPort` |
| `src/domain/ports/ExportPort.ts` | interface | `ExportPort` |
| `src/adapters/llm/noop.ts` | Null Object | `NoopLlmAdapter` |
| `src/adapters/sync/noop.ts` | Null Object | `NoopSyncAdapter` |
| `src/app/container.ts` | composition root (extended) | `config`, `createContainer(storage)` |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Jövőbeli LiveLlmAdapter becsatolási pont (`config.llmEnabled`) | Jelenleg nincs élő ág, de ez a helyettesítési pont, ahol egy jövőbeli API-kulcs-kezelés bekerülne |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-04-01 | Spoofing / Elevation of Privilege | `config.llmEnabled` jövőbeli élő-adapter kapcsolópont | low | mitigate | Jelenleg `config.llmEnabled` mindig `false`, nincs kód-ág, ami `LiveLlmAdapter`-t konstruálna vagy API-kulcsot kezelne — a port csak a Null Object-et köti be; a valós kulcskezelés/CSP-előfeltétel explicit a jövőbeli AI-integrációs fázis (v2) és a Phase 5 CSP-feladat tárgya |
| T-01-04-02 | Tampering | `SyncPort.pending()` jövőbeli outbox-tábla | low | accept | `NoopSyncAdapter.pending()` mindig `[]`-t ad, nincs valós outbox-adat, amit manipulálni lehetne ebben a fázisban |

</threat_model>

<verification>
- `pnpm test -- noop container --run` mind zöld
- `pnpm exec tsc --noEmit` hibamentes
</verification>

<success_criteria>
- PREP-01 (LlmPort + NoopLlmAdapter, kikapcsolható/hiányzó élő ág) és PREP-02 (SyncPort + NoopSyncAdapter, dirty-könyvelés a StorageAdapterben) teljesül
- Mind az 5 port TypeScript interfészként létezik a domain rétegben
</success_criteria>

<output>
Create `.planning/phases/01-adat-alap-portok-perzisztencia-s-mvp-migr-ci/01-04-SUMMARY.md` when done
</output>
