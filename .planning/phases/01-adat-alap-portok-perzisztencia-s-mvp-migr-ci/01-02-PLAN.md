---
phase: 01-adat-alap-portok-perzisztencia-s-mvp-migr-ci
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/domain/ports/StoragePort.ts
  - src/adapters/storage/indexeddb/StorageAdapter.ts
  - src/adapters/storage/indexeddb/StorageAdapter.test.ts
  - src/adapters/storage/indexeddb/db.ts
  - src/adapters/storage/indexeddb/db.test.ts
  - src/features/projects/ProjectListView.tsx
  - src/features/projects/ProjectListView.test.tsx
autonomous: true
requirements: [DATA-03, DATA-04]
must_haves:
  truths:
    - "A törölt projekt eltűnik a projekt-listából, de a rekord fizikailag megmarad (deletedAt kitöltve) — tombstone, nem hard delete"
    - "Az RxDB kollekció verzió-kulcsolt migrationStrategies-t használ, ami bizonyítottan képes egy régebbi verziójú dokumentumot felhozni, még akkor is, ha jelenleg a lánc üres"
  artifacts:
    - path: "src/domain/ports/StoragePort.ts"
      provides: "StoragePort.softDelete(id) metódus hozzáadva"
    - path: "src/adapters/storage/indexeddb/StorageAdapter.ts"
      provides: "softDelete implementáció + list() alapból deletedAt==null szűréssel"
  key_links:
    - from: "src/features/projects/ProjectListView.tsx"
      to: "src/adapters/storage/indexeddb/StorageAdapter.ts"
      via: "storage.softDelete(id) hívás a 'Törlés' gombból"
      pattern: "storage\\.softDelete"
---

<objective>
Ez a plan egészíti ki a 01-01-ben létrehozott StoragePort-ot és RxdbStorageAdapter-t a soft-delete (tombstone) mechanizmussal (DATA-03), és bizonyítja be, hogy az RxDB kollekció verzió-kulcsolt migrációs mechanizmusa működik — üres lánccal is (DATA-04). Ez a "harmadik+ feladat" a Walking Skeleton után: a Task 4-ben létrehozott placeholder-mentes stubot valós törlési viselkedéssel egészíti ki, validációval és hibaállapotokkal.

Purpose: A törlés nem lehet fizikai DELETE (PITFALLS.md Pitfall 2 — "zombie-rekordok" sync esetén), és a séma-verziózásnak működnie kell azelőtt, hogy bármilyen valós adat felhalmozódna (PITFALLS.md Pitfall 4).
Output: `StoragePort.softDelete()`, `RxdbStorageAdapter` tombstone-szűrt `list()`, bizonyítottan működő RxDB `migrationStrategies` mechanizmus, "Törlés" gomb a ProjectListView-ban.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/research/ARCHITECTURE.md
@.planning/research/PITFALLS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: softDelete (tombstone) — StoragePort, StorageAdapter, UI</name>
  <files>src/domain/ports/StoragePort.ts, src/adapters/storage/indexeddb/StorageAdapter.ts, src/adapters/storage/indexeddb/StorageAdapter.test.ts, src/features/projects/ProjectListView.tsx, src/features/projects/ProjectListView.test.tsx</files>
  <behavior>
    - `StorageAdapter.softDelete(id)` beállítja a `deletedAt` mezőt friss ISO-időbélyegre, bumpolja a `revision`-t és `updatedAt`-ot, `dirty=true` marad
    - `list()` alapból csak a `deletedAt` nélküli (nem törölt) rekordokat adja vissza
    - `get(id)` a törölt rekordot IS visszaadja (a tombstone nem fizikai törlés — a rekord elérhető marad direkt lekérdezéssel)
    - A ProjectListView "Törlés" gombjára kattintva a sor eltűnik a listából
  </behavior>
  <read_first>
    src/domain/ports/StoragePort.ts (01-01-ben létrehozott, jelenleg list/get/put),
    src/adapters/storage/indexeddb/StorageAdapter.ts (01-01-ben létrehozott put() implementáció — a revision/updatedAt/dirty bumpolási mintát reprodukáld softDelete()-ben is),
    .planning/research/PITFALLS.md (Pitfall 2 — "Törlés = fizikai DELETE" szakasz)
  </read_first>
  <action>
    Bővítsd `src/domain/ports/StoragePort.ts`-t egy új metódussal: `softDelete(id: string): Promise<void>`.

    Bővítsd `src/adapters/storage/indexeddb/StorageAdapter.ts`-t: implementáld a `softDelete(id)`-t — `collection.findOne(id).exec()`, ha nincs találat dobj egy hibát ("Project not found: {id}"), egyébként `ProjectEnvelopeSchema.parse()` az aktuális állapoton, majd `collection.upsert()` egy módosított objektummal: `deletedAt: new Date().toISOString()`, `revision: existing.revision + 1`, `updatedAt: new Date().toISOString()`, `dirty: true`, minden más mező VÁLTOZATLAN (`data`, `id`, `schemaVersion`, `updatedBy`). NE hívj RxDB `.remove()`-ot vagy bármilyen fizikai törlési műveletet a softDelete implementáción belül. Módosítsd a `list()`-et: az RxDB lekérdezést szűkítsd RxDB query-szelektorral `{ deletedAt: { $exists: false } }`-ra (mert a `deletedAt` mező — a 01-01-PLAN.md Task 3 döntése szerint — RxDB-sémában NEM nullable típusú, hanem hiányzó/opcionális mező jelzi a "nem törölt" állapotot; a Zod-domain oldalon ugyanez `null`-ként jelenik meg — a StorageAdapternek explicit módon kell mappelnie a kettő között: RxDB `undefined`/hiányzó mező → domain `null`, és fordítva, domain `null` → RxDB mezőt KIHAGY az objektumból írás előtt, sose írj szó szerint `null`-t az RxDB dokumentumba). A `get(id)` VÁLTOZATLAN marad — törölt rekordra is visszaadja az `Envelope`-ot (a tombstone-t is látva, `deletedAt` nem-null).

    Bővítsd `src/features/projects/ProjectListView.tsx`-et: minden sorhoz adj egy "Törlés" gombot, `onClick`-re hívja a `storage.softDelete(id)`-t, majd a `list()`-et újra, frissítve az állapotot.

    Bővítsd a teszt-fájlokat a fenti `<behavior>` négy esetének megfelelően.
  </action>
  <acceptance_criteria>
    - `StorageAdapter.test.ts`-ben egy új teszteset: put → list (1 elem) → softDelete → list (0 elem) → get(id) közvetlenül (nem null, `deletedAt` kitöltve) — mind zöld
    - Region-scoped grep: a `softDelete` függvénytörzsében (a metódus nyitó és záró kapcsos zárójele közötti sorokban) nincs `.remove()` hívás — `awk '/softDelete\\(/,/^  }/' src/adapters/storage/indexeddb/StorageAdapter.ts | grep -c "\\.remove()"` egyenlő 0-val
    - `ProjectListView.test.tsx`: "Törlés" gombra kattintás után a sor eltűnik a renderelt listából
  </acceptance_criteria>
  <verify>
    <automated>pnpm test -- StorageAdapter ProjectListView --run 2>&1 | tail -40</automated>
  </verify>
  <done>Tombstone soft-delete működik, a list() alapból elrejti a törölteket, a get() nem.</done>
</task>

<task type="auto">
  <name>Task 2: Verzió-kulcsolt migrationStrategies mechanizmus bizonyítása (üres lánccal induló)</name>
  <files>src/adapters/storage/indexeddb/db.ts, src/adapters/storage/indexeddb/db.test.ts</files>
  <read_first>
    src/adapters/storage/indexeddb/db.ts (01-01-ben létrehozott `projectEnvelopeSchema`/`createProjectDatabase`),
    .planning/research/ARCHITECTURE.md (Pattern 3 — "Explicit séma-verziózás és migrációs lánc" szakasz, RxDB `migrationStrategies` leírás),
    .planning/research/PITFALLS.md (Pitfall 4 — "Séma-verziózás hiánya" + "Looks Done But Isn't" checklist "Séma-verzió" pontja)
  </read_first>
  <action>
    Ne módosítsd a produkciós `projectEnvelopeSchema` verziószámát (marad `version: 0`, `migrationStrategies: {}` — ez maga a DATA-04 "üres lánccal induló" követelménye, mert ez egy vadonatúj webalkalmazás, nincs még valós korábbi verzió, amit migrálni kellene). A követelmény a MECHANIZMUS bizonyítása, nem egy valós jelenlegi migráció.

    Hozz létre `src/adapters/storage/indexeddb/db.test.ts`-t: definiálj a teszt-fájlon BELÜL egy önálló, minimális, KÉT-mezős szintetikus teszt-sémát (pl. `{ id, label }`), `version: 0`-val, `getRxStorageMemory()`-vel hozz létre egy adatbázist ezzel a sémával, szúrj be egy dokumentumot. Zárd le/dobd el ezt a db-példányt (RxDB memory storage minden `createRxDatabase` hívásnál friss állapotot ad, ha új `name`-et használsz, VAGY explicit `db.close()`-t hívsz — használd az utóbbit). Hozz létre EGY MÁSODIK adatbázis-példányt UGYANAZZAL a `name`-mel és UGYANAZZAL a kollekció-névvel, de a séma `version: 1`-re emelve egy ÚJ, kötelező mezővel (pl. `note: { type: 'string' }`, `required` benne), és egy valós `migrationStrategies: { 1: (oldDoc) => ({ ...oldDoc, note: "default" }) }` stratégiával. Nyisd meg ezt a második adatbázist (ugyanazon `getRxStorageMemory()` store felett, ha a plugin ezt támogatja indexeddb-szerű perzisztens memory-store-ral; ha a memory storage NEM oszt meg állapotot két külön `createRxDatabase` hívás között, dokumentáld ezt explicit kommentben a teszt tetején, és helyette bizonyítsd a migrációs mechanizmust EGYETLEN adatbázis-példányon belül, a kollekció újra-hozzáadásával `db.addCollections()` egy magasabb verziószámú sémával ugyanarra a kollekció-névre — válaszd azt a variánst, amelyik ténylegesen lefut a telepített RxDB verzióval, és a másikat NE hagyd a fájlban kommentelt holt kódként). Assertáld, hogy a migrált dokumentum `note === "default"`-ot tartalmaz.
  </action>
  <acceptance_criteria>
    - `db.test.ts` legalább egy tesztesete ténylegesen lefuttatja az RxDB `migrationStrategies` mechanizmusát (nem csak importálja/deklarálja), és asszertálja a migrált mező jelenlétét
    - `pnpm test -- db.test --run` zöld
    - A produkciós `projectEnvelopeSchema` VÁLTOZATLAN marad `version: 0` és `migrationStrategies: {}` (grep: `grep -A2 "migrationStrategies" src/adapters/storage/indexeddb/db.ts | grep -c "{}"` legalább 1)
  </acceptance_criteria>
  <verify>
    <automated>pnpm test -- db.test --run 2>&1 | tail -30</automated>
  </verify>
  <done>Bizonyított, hogy a verzió-kulcsolt migrációs mechanizmus valóban működik, miközben a produkciós séma jelenleg üres lánccal fut.</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Type | Exports/Change |
|---|---|---|
| `src/domain/ports/StoragePort.ts` | interface (extended) | + `softDelete(id): Promise<void>` |
| `src/adapters/storage/indexeddb/StorageAdapter.ts` | class (extended) | `softDelete()` impl, `list()` tombstone-filtered |
| `src/adapters/storage/indexeddb/db.test.ts` | test | proves RxDB `migrationStrategies` mechanism works |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RxDB migrationStrategies függvény bemenete | Egy potenciálisan sérült/korábbi-alakú dokumentum kerül a migrációs stratégia-függvénybe |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-02-01 | Tampering | RxDB migrationStrategies függvény sérült régi-alakú dokumentummal | medium | mitigate | A migrációs stratégia-függvény defenzíven, opcionális-lánc (`?.`) hozzáféréssel olvassa a régi mezőket, és az RxDB saját hibakezelése (dobott kivétel a `addCollections()`-nál, ha a stratégia dob) surfaceolja a hibát ahelyett, hogy csendben korrupt adatot írna |
| T-01-02-02 | Repudiation | softDelete nem naplózza, KI törölt | low | accept | Egyfelhasználós lokális app, D-06 szerint egyetlen `"local-user"` stub aktor van; valódi audit-trail a sync-mérföldkő (v2) tárgya |

</threat_model>

<verification>
- `pnpm test -- StorageAdapter ProjectListView db.test --run` mind zöld
</verification>

<success_criteria>
- DATA-03 (soft-delete/tombstone) és DATA-04 (verzió-kulcsolt migrációs lánc, üres lánccal induló, bizonyítottan működő mechanizmus) teljesül
</success_criteria>

<output>
Create `.planning/phases/01-adat-alap-portok-perzisztencia-s-mvp-migr-ci/01-02-SUMMARY.md` when done
</output>
