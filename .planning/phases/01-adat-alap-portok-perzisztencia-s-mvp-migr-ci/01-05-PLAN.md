---
phase: 01-adat-alap-portok-perzisztencia-s-mvp-migr-ci
plan: 05
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/adapters/storage/memory/InMemoryStorageAdapter.ts
  - src/adapters/migration/legacyImport.ts
  - src/adapters/migration/legacyImport.test.ts
  - src/adapters/migration/legacyImport.integration.test.ts
  - src/test/fixtures/legacy-export-fixture.json
autonomous: true
requirements: [MIG-01]
must_haves:
  truths:
    - "A régi Tauri-MVP szintetikus export-fixtúrája (id+data JSON-blob sorok) importálható a webes StoragePort-ra, Zod-sémákkal validálva"
    - "Az import idempotens: kétszer lefuttatva a második futás nem duplikál és nem ír felül meglévő adatot"
    - "Az import nem-destruktív: egy hibás/érvénytelen sor nem akadályozza a többi érvényes sor importját"
  artifacts:
    - path: "src/adapters/migration/legacyImport.ts"
      provides: "importLegacyExport(rows, storage) — parse+Zod-validáció+idempotens import"
    - path: "src/adapters/storage/memory/InMemoryStorageAdapter.ts"
      provides: "gyors, RxDB-mentes StoragePort teszt-dupla"
  key_links:
    - from: "src/adapters/migration/legacyImport.ts"
      to: "src/domain/model/schema.ts"
      via: "ProjectSchema.safeParse(parsedLegacyProject)"
      pattern: "ProjectSchema\\.safeParse"
    - from: "src/adapters/migration/legacyImport.ts"
      to: "src/domain/ports/StoragePort.ts"
      via: "existing.get(id) idempotencia-ellenőrzéshez, existing.put(envelope) íráshoz"
      pattern: "existing\\.(get|put)"
---

<objective>
Ez a plan szállítja a MIG-01 követelményt CONTEXT.md D-01/D-02 pontos hatókörében: KIZÁRÓLAG a formátum/import-logika (parse + Zod-validáció + idempotens, nem-destruktív import) egy szintetikus, valósághű fixtúrán — élő migrációs UX vagy a régi Tauri app export-gombja NEM ennek a planek a tárgya.

Purpose: A jövőbeli valós Tauri-MVP-adat migrációja fájdalommentes legyen, ha a régi `{id, data}` sor-alak (`src/lib/storageAdapters.ts` `ProjectRow`) és a Zod-validáció már most, tesztelve rendelkezésre áll (PITFALLS.md Pitfall 10).
Output: `legacyImport.ts`, egy realisztikus szintetikus fixtúra, unit- és integrációs tesztek (utóbbi a valós RxDB-alapú StorageAdapter ellen).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-adat-alap-portok-perzisztencia-s-mvp-migr-ci/01-CONTEXT.md
@.planning/research/PITFALLS.md
@src/lib/storageAdapters.ts
@src/data/types.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: legacyImport.ts — parse+Zod-validáció+idempotens import, szintetikus fixtúrán</name>
  <files>src/adapters/storage/memory/InMemoryStorageAdapter.ts, src/adapters/migration/legacyImport.ts, src/adapters/migration/legacyImport.test.ts, src/test/fixtures/legacy-export-fixture.json</files>
  <behavior>
    - Egy friss (üres) storage-on futtatva a fixtúra importja jelenti: hány sor importálódott, hány sor volt érvénytelen (indexszel/Zod-hibával), nulla "már létezik" sor
    - Ugyanazt a fixtúrát UGYANARRA a storage-ra másodszor lefuttatva: nulla új import, minden korábban sikeresen importált sor "már létezik"-ként jelentve — semmi nem duplikálódik, semmi nem íródik felül
    - Egy hibás sor (pl. hiányzó `name` mező a beágyazott `data`-ban) NEM akadályozza a fixtúra TÖBBI, érvényes sorának importját
    - A sikeresen importált rekord `data` mezője mezőről mezőre megegyezik a forrás `data`-val (magyar ékezetes mezőket is beleértve), semmi nem sérül/vész el
  </behavior>
  <read_first>
    src/lib/storageAdapters.ts (a PONTOS `ProjectRow = { id: string; data: string }` típus és a `parseProjectPayload`/`toProjectRecord` (de)szerializációs mintázat, amit a legacy formátum reprezentál),
    src/data/types.ts (a TELJES legacy `Project` mezőkészlet — a fixtúra ezt kell kövesse),
    src/domain/model/schema.ts (01-01-ben létrehozott `ProjectSchema`, `createEnvelopeSchema`),
    src/domain/model/envelope.ts (01-01-ben létrehozott `Envelope<T>`, `CURRENT_APP_SCHEMA_VERSION`),
    src/domain/ports/StoragePort.ts (01-01-ben létrehozott interfész — ebben a planben CSAK `get`/`put`-ot használod, `softDelete`-et NEM, mert a migráció sose tombstone-öl)
  </read_first>
  <action>
    Hozd létre `src/adapters/storage/memory/InMemoryStorageAdapter.ts`-t: `export class InMemoryStorageAdapter implements StoragePort` egy belső `Map<string, Envelope<Project>>`-pel — `list()` a nem-törölt (ha van `deletedAt` mező kezelve — ELEGENDŐ ebben a planben csak `list`/`get`/`put`-ot implementálni, mert az InMemoryStorageAdapter EBBEN a fázisban kizárólag Plan05 saját tesztjeinek szolgál gyors StoragePort-duplaként, és 01-01 óta a StoragePort csak ezt a 3 metódust deklarálja ezen a ponton — NE implementálj `softDelete`/`exportBackup`/`importBackup`-ot, ha az interfész jelenlegi állapota nem követeli meg őket, hogy ne kelljen felesleges kódot karbantartani egy olyan interfész-verzió ellen, ami már bővült más planekben). `get(id)` a Map-ből ad vissza egy másolatot vagy `null`-t. `put(record)` egyszerűen `Map.set(record.id, record)`.

    Hozd létre `src/test/fixtures/legacy-export-fixture.json`-t: egy `ProjectRow[]` tömb (pontosan `{id: string, data: string}` alakú elemek, ahol `data` egy `JSON.stringify`-olt, a `src/data/types.ts` szerint TELJESEN kitöltött `Project` objektum stringje), legalább 4 elemmel: (1) egy teljesen kitöltött, magyar ékezetes mezőket tartalmazó projekt (pl. `customerOrOrganization: "Székesfehérvári Önkormányzat"`, `businessProblem` egy hosszabb, ékezetes mondat), (2) egy minimálisan kitöltött/friss (üres string mezőkkel, alapértelmezett `decisionScores`/`checklistAnswers`/`completion`-nel) projekt, (3) egy már archivált projekt (`archivedAt` nem null, a többi mező érvényes), (4) egy SZÁNDÉKOSAN ÉRVÉNYTELEN sor, aminek a `data`-jában hiányzik a kötelező `name` mező (ez teszteli a részleges-siker/hibakezelést). Mind a 4 elem `id`-je legyen valós formátumú UUID-string (pl. `crypto.randomUUID()`-szerű minta, kézzel begépelve).

    Hozd létre `src/adapters/migration/legacyImport.ts`-t: definiálj egy `LegacyProjectRow = { id: string; data: string }` típust (megegyezik a `src/lib/storageAdapters.ts` `ProjectRow`-jával, de itt önállóan deklarálva, mert a régi fájl nem exportálja). Exportálj egy `LegacyImportResult` típust: `{ imported: number; skippedExisting: number; skippedInvalid: Array<{ id: string; issues: string[] }> }`. Exportálj egy async `importLegacyExport(rows: LegacyProjectRow[], storage: Pick<StoragePort, "get" | "put">): Promise<LegacyImportResult>` függvényt: inicializálja a számlálókat 0-ra és egy üres `skippedInvalid` tömböt; MINDEN sorra egyenként (egymástól függetlenül, egy sor hibája nem szakítja meg a ciklust): (a) próbálja `JSON.parse(row.data)`-t — ha dob, adjon hozzá egy `skippedInvalid` bejegyzést `["JSON parse error: " + hibaüzenet]`-tel és folytassa a következő sorral; (b) `ProjectSchema.safeParse(parsed)` — ha `success: false`, adjon hozzá egy `skippedInvalid` bejegyzést a Zod `error.issues`-ból épített üzenet-tömbbel, és folytassa; (c) `await storage.get(row.id)` — ha NEM `null` (a rekord már létezik), növelje `skippedExisting`-et és folytassa, NE írjon felül semmit; (d) egyébként építsen egy friss `Envelope<Project>`-et: `id: row.id` (a legacy ID-t VÁLTOZATLANUL megőrzi, mert a jelenlegi kódbázis `makeId()` már `crypto.randomUUID()`-t használ — a legacy ID már megfelel DATA-01-nek, NEM kell újat generálni), `schemaVersion: CURRENT_APP_SCHEMA_VERSION`, `data: parsed` (a Zod-validált, változatlan legacy payload), `revision: 1`, `updatedAt: new Date().toISOString()`, `updatedBy: "local-user"` (D-06), `deletedAt: null` (az envelope-szintű tombstone MINDIG null importkor — a legacy `data.archivedAt` mező a domain-payload RÉSZEKÉNT megmarad változatlanul, ez egy MÁSIK fogalom, mint az envelope sync-tombstone-ja, a kettőt SOSE mosd össze), `dirty: false` (az importált adat "önmagával szinkronban van", nincs függő változtatás); hívja meg `await storage.put(envelope)`-ot, növelje `imported`-et. A ciklus végén adja vissza a `LegacyImportResult`-ot.

    Hozd létre `src/adapters/migration/legacyImport.test.ts`-t: töltse be a `legacy-export-fixture.json`-t, hozzon létre egy friss `InMemoryStorageAdapter`-t, futtassa `importLegacyExport`-ot KÉTSZER egymás után UGYANARRA az adapter-példányra, és assertálja a fenti `<behavior>` mind a 4 pontját.
  </action>
  <acceptance_criteria>
    - Első futás: `result.imported === 3` (a fixtúra 3 érvényes sora), `result.skippedInvalid.length === 1` (a szándékosan hibás sor)
    - Második futás UGYANARRA a storage-ra: `result.imported === 0`, `result.skippedExisting === 3`
    - Egy célzott teszt: a `customerOrOrganization` (vagy más ékezetes mező) értéke az importált rekordban BYTE-RE megegyezik a fixtúra forrás-értékével
    - `grep -c "storage.softDelete" src/adapters/migration/legacyImport.ts` egyenlő 0-val (a migráció sosem tombstone-öl)
  </acceptance_criteria>
  <verify>
    <automated>pnpm test -- legacyImport --run 2>&1 | tail -40</automated>
  </verify>
  <done>A szintetikus legacy-fixtúra bizonyítottan idempotensen és nem-destruktívan importálható, hibás sorok mellett is.</done>
</task>

<task type="auto">
  <name>Task 2: Integrációs teszt a valós RxDB-alapú StorageAdapter ellen</name>
  <files>src/adapters/migration/legacyImport.integration.test.ts</files>
  <read_first>
    src/adapters/storage/indexeddb/StorageAdapter.ts (01-01-ben létrehozott `RxdbStorageAdapter`),
    src/app/container.ts (01-01-ben létrehozott `createStorageAdapter(storage)` factory),
    src/adapters/migration/legacyImport.ts (Task 1-ben létrehozott import-logika)
  </read_first>
  <action>
    Hozd létre `src/adapters/migration/legacyImport.integration.test.ts`-t: importáld a VALÓS `createStorageAdapter`-t a `src/app/container.ts`-ből (NEM az `InMemoryStorageAdapter`-t), hozz létre egy friss adaptert `createStorageAdapter(getRxStorageMemory())`-vel (import `getRxStorageMemory` a `rxdb/plugins/storage-memory`-ból — ugyanaz a minta, mint a 01-01-PLAN.md `StorageAdapter.test.ts`-ében). Töltsd be ugyanazt a `legacy-export-fixture.json`-t, futtasd `importLegacyExport`-ot ELŐSZÖR, majd MÁSODSZOR is ugyanerre a valós adapterre. Assertáld: (a) az első futás után `storageAdapter.list()` (a valós, tombstone-szűrt, Zod-validáló lekérdezési útvonalon át) visszaadja a 3 sikeresen importált projektet név szerint; (b) a második futás `imported: 0`-t jelent (idempotencia a valós perzisztencia-réteggel szemben is, nem csak az in-memory duplával szemben).
  </action>
  <acceptance_criteria>
    - `pnpm test -- legacyImport.integration --run` zöld
    - A teszt-fájl importálja a `createStorageAdapter`-t a `src/app/container.ts`-ből — `grep -c "from .*container" src/adapters/migration/legacyImport.integration.test.ts` legalább 1
    - A teszt NEM importálja az `InMemoryStorageAdapter`-t (`grep -c "InMemoryStorageAdapter" src/adapters/migration/legacyImport.integration.test.ts` egyenlő 0-val)
  </acceptance_criteria>
  <verify>
    <automated>pnpm test -- legacyImport.integration --run 2>&1 | tail -30</automated>
  </verify>
  <done>A migrációs logika a TELJES valós perzisztencia-stacken (Envelope + Zod + RxDB) keresztül is bizonyítottan idempotensen működik, nem csak az izolált tesztduplán.</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Type | Exports |
|---|---|---|
| `src/adapters/storage/memory/InMemoryStorageAdapter.ts` | test double | `InMemoryStorageAdapter implements StoragePort` |
| `src/adapters/migration/legacyImport.ts` | pure module | `importLegacyExport(rows, storage)`, `LegacyProjectRow`, `LegacyImportResult` |
| `src/test/fixtures/legacy-export-fixture.json` | fixture | szintetikus, valósághű legacy export (4 sor, 1 szándékosan érvénytelen) |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Legacy export-fixtúra (jövőben: valós Tauri-export) → importLegacyExport() | Egy "saját" korábbi rendszer JSON-blob-formátuma kerül parse+validáció+írás útvonalba — kevésbé kockázatos, mint egy tetszőleges felhasználói fájl (01-03), de a validáció ugyanúgy kötelező |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-05-01 | Tampering | Sérült/érvénytelen legacy `data` JSON-blob sor összeomlasztja az importot vagy részleges/hibás adatot ír | high | mitigate | `JSON.parse` és `ProjectSchema.safeParse` (nem `parse`) minden sorra egyenként, try/catch-csel; egy sor hibája `skippedInvalid`-ba kerül, SOSE dob uncaught kivételt, SOSE akadályozza a többi sor importját |
| T-01-05-02 | Repudiation | Az importált rekordok `updatedBy: "local-user"`-t kapnak, elveszítve az eredeti szerzőség-információt | low | accept | A legacy Tauri-app maga sem ismert user/auth-fogalmat (egyfelhasználós desktop app volt) — nincs ténylegesen elveszíthető információ; a valós szerzőség-nyomon-követés a sync-mérföldkő (v2) tárgya |

</threat_model>

<verification>
- `pnpm test -- legacyImport legacyImport.integration --run` mind zöld
</verification>

<success_criteria>
- MIG-01 teljesül: a legacy Tauri-MVP szintetikus export-fixtúrája nem-destruktívan, idempotensen, Zod-validálva importálható, mind egy gyors teszt-duplán, mind a valós RxDB-perzisztencián keresztül
</success_criteria>

<output>
Create `.planning/phases/01-adat-alap-portok-perzisztencia-s-mvp-migr-ci/01-05-SUMMARY.md` when done
</output>
