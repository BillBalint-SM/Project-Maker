---
phase: 01-adat-alap-portok-perzisztencia-s-mvp-migr-ci
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/domain/model/envelope.ts
  - src/domain/model/types.ts
  - src/domain/model/schema.ts
  - src/domain/model/factory.ts
  - src/domain/ports/StoragePort.ts
  - src/adapters/storage/indexeddb/db.ts
  - src/adapters/storage/indexeddb/StorageAdapter.ts
  - src/adapters/storage/indexeddb/StorageAdapter.test.ts
  - src/app/container.ts
  - src/main.tsx
  - src/features/projects/ProjectListView.tsx
  - src/features/projects/ProjectListView.test.tsx
autonomous: false
requirements: [DATA-01, DATA-02, DATA-05]
must_haves:
  truths:
    - "Minden mentett Project egy Envelope<Project> burokban létezik, stabil UUID id-vel, schemaVersion/revision/updatedAt/updatedBy/dirty mezőkkel"
    - "A StorageAdapter.put() Zod-validálja a teljes envelope-ot; érvénytelen adat dob, nem íródik csendben"
    - "A projekt-lista nézet valós IndexedDB-ből olvas, és 'Új teszt-projekt' gombbal valós írást tud kiváltani"
    - "pnpm dev ténylegesen kiszolgálja a / route-ot a ProjectListView-vel"
  artifacts:
    - path: "src/domain/model/envelope.ts"
      provides: "Envelope<T> interfész + CURRENT_APP_SCHEMA_VERSION konstans"
    - path: "src/domain/model/schema.ts"
      provides: "ProjectSchema, createEnvelopeSchema Zod-sémák"
    - path: "src/domain/ports/StoragePort.ts"
      provides: "StoragePort interfész (list/get/put)"
    - path: "src/adapters/storage/indexeddb/StorageAdapter.ts"
      provides: "RxDB-alapú StoragePort implementáció"
    - path: "src/features/projects/ProjectListView.tsx"
      provides: "Walking Skeleton proof-of-life UI"
  key_links:
    - from: "src/features/projects/ProjectListView.tsx"
      to: "src/app/container.ts"
      via: "import { container } from '../../app/container'; container.storage.list()/put()"
      pattern: "container\\.storage\\.(list|put)"
    - from: "src/app/container.ts"
      to: "src/adapters/storage/indexeddb/StorageAdapter.ts"
      via: "createStorageAdapter() factory"
      pattern: "createStorageAdapter"
    - from: "src/adapters/storage/indexeddb/StorageAdapter.ts"
      to: "src/domain/model/schema.ts"
      via: "ProjectSchema/createEnvelopeSchema .parse() on every read/write"
      pattern: "Schema\\.(parse|safeParse)"
---

## Phase Goal

**As a** Project-Maker felhasználó, **I want to** hogy a projektjeim megbízhatóan, verziózottan mentődjenek egy böngésző-alapú alkalmazásban, és egy valós projekt-listában visszaolvashatók legyenek, **so that** bizalommal építhessek a következő fázisokban (felmérés, coaching, export) erre az adatalapra anélkül, hogy a munkám elveszhetne.

<objective>
Ez a plan szállítja a Phase 1 Walking Skeleton-jét: a domain adatmodellt (Envelope<T>, Project, Zod-sémák), az első portot (StoragePort) és annak valós, IndexedDB-alapú (RxDB) implementációját, egy kompozíciós gyökeret (app/container.ts), valamint egy routing mögötti, valós adatot olvasó/író UI-t (ProjectListView). A plan végén bizonyítottan működik a teljes lánc: build → domain-modell/Zod-validáció → RxDB-perzisztencia → routing → UI → `pnpm dev`.

CONTEXT.md D-03 szerint a proof-of-life felület kifejezetten egy projekt-lista nézet, ami valós (teszt-)projekteket olvas ki az IndexedDB-ből — ez egyszerre bizonyítja az olvasást, az írást és a routingot; NEM a valódi felmérő/interjú UI (az Phase 2, D-04).

A StoragePort ebben a planben csak `list`/`get`/`put`-ot tartalmaz — a `softDelete` (01-02) és `exportBackup`/`importBackup` (01-03) metódusokat a KÖVETKEZŐ, ezt a fájlt módosító planek adják hozzá az interfészhez és az adapterhez (interfész-first, inkrementális bővítés — lásd 01-02-PLAN.md és 01-03-PLAN.md).

Purpose: Ez az egyetlen, visszamenőleg legdrágábban javítható döntéscsomag (stabil ID, sync-envelope, Zod-validáció) — mindenre erre épül a további 4 fázis.
Output: `src/domain/model/*`, `src/domain/ports/StoragePort.ts`, `src/adapters/storage/indexeddb/*`, `src/app/container.ts`, `src/features/projects/ProjectListView.tsx`, és egy futó `pnpm dev` demonstráció.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-adat-alap-portok-perzisztencia-s-mvp-migr-ci/01-CONTEXT.md
@.planning/phases/01-adat-alap-portok-perzisztencia-s-mvp-migr-ci/01-PATTERNS.md
@.planning/research/ARCHITECTURE.md
@.planning/research/STACK.md
@src/data/types.ts
@src/lib/storageAdapters.ts
</context>

<interfaces>
<!-- A jelenlegi (LEGACY, Tauri-korabeli) ProjectStorageAdapter port — koncepcionális analóg, NEM szó szerint átveendő, de a metódus-alak mintázatot innen örökli az új StoragePort. -->

Forrás: src/lib/storageTypes.ts (teljes tartalom, 17 sor):
```typescript
import type { Project } from "../data/types";

export type ProjectStorageInfo = {
  mode: "SQLite" | "localStorage";
  databasePath?: string;
};

export interface ProjectStorageAdapter {
  readonly info: ProjectStorageInfo;
  listProjects(archived: boolean): Promise<Project[]>;
  getProject(id: string): Promise<Project | null>;
  saveProject(project: Project): Promise<void>;
  deleteProject(id: string): Promise<void>;
}

export type ProjectStorageAdapterFactory = () => Promise<ProjectStorageAdapter>;
```

Forrás: src/lib/project.ts — `makeId()` mintája (crypto.randomUUID fallback-kel), amit az ÚJ `factory.ts`-ben ugyanígy kell újraírni (nem importálható közvetlenül, mert domain-tiszta, IO-mentes fájlt hozunk létre, ami nem függhet a régi `src/lib/`-től):
```typescript
function makeId() {
  if (typeof crypto !== "undefined" && "randomUUID" in crypto) {
    return crypto.randomUUID();
  }
  return `id-${Date.now()}-${Math.random().toString(16).slice(2)}`;
}
```

RxDB pontos API (Context7 `/pubkey/rxdb` alapján ellenőrizve, 2026-07-09):
```typescript
import { createRxDatabase } from 'rxdb';
import { getRxStorageDexie } from 'rxdb/plugins/storage-dexie';
import { getRxStorageMemory } from 'rxdb/plugins/storage-memory'; // csak tesztekben

const db = await createRxDatabase({ name: 'project-maker', storage: getRxStorageDexie() });
await db.addCollections({
  projects: {
    schema: projectEnvelopeSchema, // lásd Task 3 action
    migrationStrategies: {} // DATA-04: üres lánccal induló, verzió-kulcsolt migráció
  }
});
```

React Router 7 pontos API (Context7 `/remix-run/react-router` alapján ellenőrizve, 2026-07-09):
```typescript
import { createBrowserRouter } from "react-router";
import { RouterProvider } from "react-router/dom";

const router = createBrowserRouter([{ path: "/", Component: ProjectListView }]);
createRoot(document.getElementById("root")!).render(<RouterProvider router={router} />);
```
</interfaces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Csomag-legitimitás ellenőrzés telepítés előtt</name>
  <files>package.json (ebben a taskban MÉG NEM módosul — csak ellenőrzés történik a Task 3 telepítési lépése előtt)</files>
  <what-built>Nincs kód még — ez egy védelmi kapu, mielőtt bármelyik új függőség (rxdb, rxjs, zod, react-router) telepítésre kerülne. A projekt kutatási dokumentumaiban (STACK.md, SUMMARY.md, ARCHITECTURE.md, PITFALLS.md) nincs "## Package Legitimacy Audit" szakasz, ezért a fallback szabály szerint minden új csomagot [ASSUMED] szinten kell kezelni, és emberi jóváhagyást kell kérni telepítés előtt.</what-built>
  <action>Állítsd meg a végrehajtást a Task 3 `pnpm add` lépése előtt, és kérj emberi jóváhagyást mind a 4 új függőség (rxdb, rxjs, zod, react-router) npmjs.com-os legitimitására. Ne folytasd Task 3-mal, amíg a jóváhagyás meg nem érkezik.</action>
  <how-to-verify>
    Ellenőrizd az alábbi 4 csomagot az npmjs.com-on (csomagnév, kiadó/szervezet, heti letöltésszám, nincs friss gyanús tulajdonos-váltás):
    1. https://www.npmjs.com/package/rxdb (várt: `pubkey`/`rxdb-dev` szervezet, több millió heti letöltés)
    2. https://www.npmjs.com/package/rxjs (várt: ReactiveX szervezet, tíz milliós heti letöltés)
    3. https://www.npmjs.com/package/zod (várt: `colinhacks`, tíz milliós heti letöltés)
    4. https://www.npmjs.com/package/react-router (várt: `remix-run` szervezet, tíz milliós heti letöltés)
    Ha mind a négy legitimnek tűnik (nincs typosquatting-gyanú, a kiadó egyezik a research dokumentumokban hivatkozottal), engedélyezd a telepítést.
  </how-to-verify>
  <resume-signal>Írd be: "jóváhagyva" (vagy sorold fel, melyik csomaggal van probléma)</resume-signal>
  <verify>
    <automated>MISSING — human-verify checkpoint, a jóváhagyás maga a kapu, nincs automatizálható előfeltétel</automated>
  </verify>
  <done>A felhasználó jóváhagyta mind a 4 csomag legitimitását (vagy megnevezte a problémás csomagot, és a végrehajtás megállt a tisztázásig).</done>
</task>

<task type="auto">
  <name>Task 2: Domain-kontraktusok — Envelope, Project-típus, Zod-sémák, StoragePort, factory</name>
  <files>src/domain/model/envelope.ts, src/domain/model/types.ts, src/domain/model/schema.ts, src/domain/model/factory.ts, src/domain/ports/StoragePort.ts</files>
  <read_first>
    src/data/types.ts (a TELJES jelenlegi Project/ProjectListItem/ChecklistAnswer/FollowUpQuestion/DecisionScores/ProjectCompletion/ReadinessGap típus-készlet — ezt kell 1:1 mezőszinten tükrözni),
    .planning/phases/01-adat-alap-portok-perzisztencia-s-mvp-migr-ci/01-PATTERNS.md ("Envelope<T> (model, transform)" és "StoragePort.ts" szakaszok — a célalak pontos kódja),
    .planning/research/ARCHITECTURE.md (Pattern 1, Pattern 2 — Envelope és StoragePort célkód)
  </read_first>
  <action>
    Hozd létre `src/domain/model/envelope.ts`-t: exportáld az `Envelope<T>` interfészt PONTOSAN ezzel a 8 mezővel: `id: string`, `schemaVersion: number`, `data: T`, `revision: number`, `updatedAt: string`, `updatedBy: string`, `deletedAt: string | null`, `dirty: boolean`. Exportálj egy `CURRENT_APP_SCHEMA_VERSION = 1` numerikus konstanst is ugyanitt (ez a domain-szintű séma-verzió, FÜGGETLEN az RxDB kollekció saját belső `version` mezőjétől, amit a 01-03-as feladatban 0-ról indítasz — a kettőt sose keverd össze, és minden helyen, ahol a kettő együtt szerepel, kommentben jelezd melyik melyik).

    Hozd létre `src/domain/model/types.ts`-t: definiáld a `Project` interfészt és minden hozzá tartozó típust (`ProjectStatus`, `Priority`, `CompletionState`, `ChecklistStatus`, `FollowUpStatus`, `Decision`, `ReadinessGapSeverity`, `DecisionScoreLabel`, `GapTargetTab`, `ChecklistAnswer`, `FollowUpQuestion`, `DecisionScores`, `ReadinessGap`, `ProjectCompletion`, `ProjectListItem`) MEZŐRŐL MEZŐRE megegyezően a `src/data/types.ts`-ben találhatóval — ez a re-platform ezen pontján egy tudatos strukturális másolat (nem újratervezés), hogy a Zod-séma és a migráció (01-05) pontosan illeszkedjen a legacy adatalakra. Ne hagyj ki, ne nevezz át, ne adj hozzá mezőt.

    Hozd létre `src/domain/model/schema.ts`-t Zod 4 sémákkal: definiálj egy `ChecklistAnswerSchema`, `FollowUpQuestionSchema`, `DecisionScoresSchema`, `ReadinessGapSchema`, `ProjectCompletionSchema` segéd-sémát a megfelelő típusokhoz, majd egy `ProjectSchema`-t, ami ezeket összefűzi a `Project` interfész teljes mezőkészletéhez. Az összes Hungarian string-unió mezőt (`status`, `priority`, `finalDecision`, a `ChecklistAnswer.status`, a `FollowUpQuestion.status`, a `ProjectCompletion.state`/`decisionScoreLabel`, a `ReadinessGap.severity`/`targetTab`) `z.enum([...])`-ként írd le, PONTOSAN a `src/data/types.ts`-ben szereplő literál értékekkel (pl. `Decision` esetén az üres string `""` IS érvényes enum-tag). FONTOS, nem nyilvánvaló részlet: a `checklistAnswers: Record<number, ChecklistAnswer>` TypeScript-mezőt `z.record(z.string(), ChecklistAnswerSchema)`-ként kódold, NEM `z.record(z.number(), ...)`-ként — a JS objektum-kulcsok futásidőben mindig string-ek, még numerikus literál property-nevek esetén is, így a Zod kulcs-sémának `z.string()`-nek kell lennie annak ellenére, hogy a TS típus `number`-t deklarál. Exportálj egy `createEnvelopeSchema(dataSchema)` factory-függvényt, ami egy tetszőleges Zod-sémát fogad be `data` mezőként, és visszaad egy Zod object-sémát az `Envelope<T>` mind a 8 mezőjére (id: nem-üres string, schemaVersion: nem-negatív egész, data: a bemenő séma, revision: nem-negatív egész, updatedAt: string, updatedBy: nem-üres string, deletedAt: nullable string, dirty: boolean). Exportáld `ProjectEnvelopeSchema = createEnvelopeSchema(ProjectSchema)`-ként a kész, konkrét sémát is.

    Hozd létre `src/domain/model/factory.ts`-t: exportálj egy `createEmptyProject(overrides?: Partial<Project>): Project` függvényt, ami egy Zod-sémának megfelelő, teljesen kitöltött, minimál-alapértékű `Project` objektumot ad vissza (minden string mező `""`, `affectedTeams: []`, `decisionScores` minden pontszáma `3` — tükrözve a `src/lib/project.ts` `defaultDecisionScores` konvencióját —, `checklistAnswers: {}` üres record, `followUps: []`, `completion` minden numerikus mezője `0`, `state`/`readinessState`/stb. egy érvényes enum-alapértékkel, `finalDecision: ""`, `archivedAt: null`, `createdAt`/`updatedAt`: friss ISO-időbélyeg, `id`: friss `crypto.randomUUID()` — FALLBACK-kel, ha a `crypto.randomUUID` nem elérhető, pontosan a fenti `<interfaces>` blokkban idézett `makeId()` mintát követve, itt önállóan újraírva, NEM a régi `src/lib/project.ts`-ből importálva). EXPLICIT NEM-CÉL: ez a factory NEM számol readiness/decision-pontszámot (az a teljes checklist/scoring motor Phase 2 tárgya) — csak sémaérvényes, nullázott alapértékeket ad.

    Hozd létre `src/domain/ports/StoragePort.ts`-t: exportáld a `StoragePort` interfészt PONTOSAN 3 metódussal ebben a planben: `list(): Promise<ProjectListItem[]>`, `get(id: string): Promise<Envelope<Project> | null>`, `put(record: Envelope<Project>): Promise<void>`. A `softDelete`/`exportBackup`/`importBackup` metódusokat a 01-02/01-03 planek adják hozzá — ITT NE deklaráld őket (interfész-first, inkrementális bővítés, nem stub-metódus).

    Domain-tisztasági szabály mind az 5 fájlra: egyik fájl se importáljon semmit `rxdb`-ből, `dexie`-ből, `react`-ből, vagy bármilyen `adapters/`/`features/` alatti fájlból.
  </action>
  <acceptance_criteria>
    - `src/domain/model/envelope.ts` a `Envelope` interfészt pontosan a fenti 8 mezővel exportálja, és exportálja a `CURRENT_APP_SCHEMA_VERSION` konstanst
    - `src/domain/model/types.ts` mind a 15 típust exportálja a `src/data/types.ts`-sel megegyező mezőkészlettel (kézi diff-fel ellenőrizhető)
    - `ProjectSchema.safeParse(fixture).success === true` egy a `src/data/types.ts` szerint teljesen kitöltött objektumra
    - `ProjectSchema.safeParse({ ...fixture, name: undefined }).success === false` (a `name` mező hiánya elutasításra kerül)
    - `grep -RcE "from ['\"](rxdb|dexie|react)" src/domain/` üres eredményt ad (0 találat) minden most létrehozott fájlra — domain-tisztaság
    - `grep -c "checklistAnswers" src/domain/model/schema.ts` legalább 1, és a sorban `z.record(z.string()` szerepel, NEM `z.record(z.number()`
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec tsc --noEmit 2>&1 | grep -E "domain/(model|ports)" ; echo "exit:$?"</automated>
  </verify>
  <done>Az 5 domain-fájl létezik, típushelyesen fordul, és a Zod-séma helyesen fogad el/utasít el egy legacy-alakú fixture-t.</done>
</task>

<task type="auto">
  <name>Task 3: RxDB StorageAdapter, db-inicializálás, kompozíciós gyökér</name>
  <files>package.json, src/adapters/storage/indexeddb/db.ts, src/adapters/storage/indexeddb/StorageAdapter.ts, src/adapters/storage/indexeddb/StorageAdapter.test.ts, src/app/container.ts</files>
  <read_first>
    .planning/research/STACK.md (RxDB/Zod telepítési parancsok, pontos verziók),
    .planning/research/ARCHITECTURE.md (Pattern 1, Pattern 3),
    src/lib/storageAdapters.ts (a `createProjectStorageAdapter()` factory-mintázat, amit `container.ts` DI-vé bővít),
    a fenti `<interfaces>` blokk RxDB kód-idézetei (Context7-ből ellenőrzött, pontos API)
  </read_first>
  <action>
    Add hozzá a `package.json` `dependencies`-hez: `rxdb@17`, `rxjs@7`, `zod@4`, `react-router@7` (futtasd `pnpm add rxdb@17 rxjs@7 zod@4 react-router@7`-t — a Task 1 checkpoint jóváhagyása UTÁN).

    Hozd létre `src/adapters/storage/indexeddb/db.ts`-t: definiálj egy `projectEnvelopeSchema` RxDB JSON-sémát `title: 'project envelope schema'`, `version: 0` (RxDB-kollekció saját belső verziója, FÜGGETLEN a domain `CURRENT_APP_SCHEMA_VERSION`-től), `primaryKey: 'id'`, `type: 'object'` mezőkkel: `id` (string, maxLength 100), `schemaVersion` (number), `data` (object — a teljes Project payload egyetlen beágyazott objektumként, mezőnkénti JSON-séma-deklaráció NÉLKÜL, mert a Zod-validáció a `StorageAdapter`-ben történik, nem az RxDB-sémában), `revision` (number), `updatedAt` (string, maxLength 30 — indexeléshez szükséges), `updatedBy` (string), `dirty` (boolean). A `deletedAt` mezőt EBBEN a planben MÉG NE vedd fel az RxDB-sémába (a 01-02-PLAN.md adja hozzá, RxDB saját ajánlása szerint NEM nullable típusként, hanem opcionális/kihagyható mezőként — lásd a 01-02-PLAN.md action részét). `required: ['id', 'schemaVersion', 'data', 'revision', 'updatedAt', 'updatedBy', 'dirty']`, `indexes: ['updatedAt']`. Exportálj egy `createProjectDatabase(storage: RxStorage): Promise<RxDatabase>` függvényt, ami `createRxDatabase({ name: 'project-maker', storage })`-ot hív, majd `db.addCollections({ projects: { schema: projectEnvelopeSchema, migrationStrategies: {} } })`-t (DATA-04: üres migrációs lánccal induló, verzió-kulcsolt kollekció — lásd bővebben 01-02-PLAN.md Task 2), és visszaadja a `db`-t. A `storage` paramétert KÍVÜLRŐL kapja (nem hardkódolja a Dexie-t), hogy a hívó dönthesse el böngészőben `getRxStorageDexie()`-t, teszben `getRxStorageMemory()`-t ad-e át — ez teszi tesztelhetővé a Task 4 alatti Vitest-fájlokat valódi IndexedDB nélkül (a `jsdom` teszt-környezet nem implementál IndexedDB-t).

    Hozd létre `src/adapters/storage/indexeddb/StorageAdapter.ts`-t: `export class RxdbStorageAdapter implements StoragePort` (a Task 2-ben létrehozott `StoragePort` interfészt implementálva), konstruktorban egy már megnyitott RxDB `db` példányt fogadva. `list(): Promise<ProjectListItem[]>`: lekérdezi a `projects` kollekció összes dokumentumát, mindegyiket `ProjectEnvelopeSchema.parse()`-olja (RxDB által visszaadott plain objektumból), a `data` mezőből épít egy `ProjectListItem`-et (id, name, contact — üres string helykitöltő, mert a `contact`-összefűzés Phase 2 tárgya —, status, priority, deadline, completionState/-Percent a `data.completion`-ből, decisionScore/-Recommendation a `data.completion`-ből, archivedAt, updatedAt), és visszaadja tömbként. `get(id): Promise<Envelope<Project> | null>`: `collection.findOne(id).exec()`, ha nincs találat `null`, egyébként `ProjectEnvelopeSchema.parse()` az RxDB dokumentum plain-objektum reprezentációján (`.toJSON()`), majd visszaadja mint `Envelope<Project>`. `put(record): Promise<void>`: ELŐSZÖR `ProjectEnvelopeSchema.parse(record)`-ot hív (ha dob, a hívó felé propagálódik — DATA-05, nincs csendes írás), majd lekéri a meglévő dokumentumot ugyanazzal az id-vel a revision-számoláshoz: ha létezik, `revision = existing.revision + 1`; ha nem, `revision = 1`. Beállítja `updatedAt = new Date().toISOString()`, `updatedBy = "local-user"` (D-06 stub — FÜGGETLENÜL attól, mit kapott a hívó ebben a mezőben, MINDIG felülírja "local-user"-re, mert egyetlen lokális aktor van), `dirty = true`, majd `collection.upsert({...})`-ot hív a végleges, Zod-validált objektummal.

    Hozd létre `src/app/container.ts`-t: exportálj egy `createStorageAdapter(storage: RxStorage): Promise<StoragePort>` async factory-függvényt, ami `createProjectDatabase(storage)`-ot hív, majd egy `RxdbStorageAdapter`-t ad vissza rá építve — ez tükrözi a `src/lib/storageAdapters.ts` `createProjectStorageAdapter()` mintáját, de típusbiztos DI-vel, Tauri-detektálás nélkül (egyetlen web-cél van). EBBEN a planben MÉG NE exportálj egy kész `container` szingletont — a Task 4 UI-nak elég a factory-t hívnia egyszer az alkalmazás indulásakor (`getRxStorageDexie()`-vel); a `llm`/`sync`/`content`/`export` portokkal bővített, teljes `container` objektumot a 01-04-PLAN.md hozza létre ugyanebben a fájlban.

    Hozd létre `src/adapters/storage/indexeddb/StorageAdapter.test.ts`-t Vitesttel: `beforeEach`-ben hozz létre egy friss adapter-példányt `createStorageAdapter(getRxStorageMemory())`-vel (import `getRxStorageMemory` a `rxdb/plugins/storage-memory`-ból) — ÍGY a teszt valós RxDB-kódot futtat IndexedDB/jsdom-függőség nélkül. Tesztesetek: (1) `put()` majd `get(ugyanaz az id)` ugyanazt a `data` payloadot adja vissza; (2) `list()` üres tömböt ad üres adatbázison; (3) `put()` egy hiányos objektummal (pl. `name` nélkül) Zod-hibát dob, és utána a `list()` üres marad (nincs csendes részleges írás).
  </action>
  <acceptance_criteria>
    - `package.json` `dependencies` tartalmazza `rxdb`, `rxjs`, `zod`, `react-router` bejegyzéseket telepített (lockfile-ban rögzített) verzióval
    - `StorageAdapter.test.ts` mindhárom tesztesete lefut és zöld: `pnpm test -- StorageAdapter` sikeresen lezárul
    - `put()`-ban a `updatedBy` mező MINDIG `"local-user"`, még akkor is, ha a hívó mást adott át (teszttel bizonyítva)
    - `grep -c "getRxStorageDexie" src/adapters/storage/indexeddb/StorageAdapter.test.ts` egyenlő 0-val (a teszt a memory storage-ot használja, nem a Dexie-t)
  </acceptance_criteria>
  <verify>
    <automated>pnpm test -- StorageAdapter --run 2>&1 | tail -30</automated>
  </verify>
  <done>Valós RxDB-alapú StorageAdapter létezik, Zod-validált put/get/list-tel, és a teszt bizonyítja a round-trip helyességét memory-storage-on.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Routing + ProjectListView (Walking Skeleton proof-of-life UI)</name>
  <files>src/main.tsx, src/features/projects/ProjectListView.tsx, src/features/projects/ProjectListView.test.tsx</files>
  <behavior>
    - Üres lista esetén "Nincs megjeleníthető projekt." szöveg jelenik meg (a meglévő `ProjectTable.tsx`-ből ismert, `TESTING.md`-ben dokumentált UX-string mintát követve)
    - Két, storage.list()-ből visszaadott projekt esetén mindkét projekt neve megjelenik a DOM-ban
    - "Új teszt-projekt" gombra kattintva meghívódik a `storage.put()` egy `createEmptyProject()`-ből épített, Zod-érvényes Envelope-pal, majd a lista frissül (a gomb utáni re-render új sort mutat)
  </behavior>
  <read_first>
    src/main.tsx (jelenlegi mount-kód, amit router-re cserélsz),
    src/features/projects/ProjectTable.tsx (csak a lista-renderelési mintázat és az üres-állapot szöveg referenciaként — NE portold a szűrést/bulk-selectiont/export-triggert, D-04 szerint ez csak proof-of-life),
    src/App.test.tsx (a `vi.hoisted` + `vi.mock` mintázat, amit a ProjectListView.test.tsx-ben ugyanígy kell alkalmazni a `container` modulra)
  </read_first>
  <action>
    Módosítsd `src/main.tsx`-et: cseréld le a jelenlegi `<App />` közvetlen mountolását egy React Router 7 data-router beállításra (`createBrowserRouter` a `react-router`-ből, `RouterProvider` a `react-router/dom`-ból), EGYETLEN route-tal: `{ path: "/", Component: ProjectListView }`. Az alkalmazás-indításkor (a router-konfiguráción kívül, egyszeri modul-szintű hívásként) hívd meg a `createStorageAdapter(getRxStorageDexie())`-t (import `getRxStorageDexie` a `rxdb/plugins/storage-dexie`-ból) egy Promise-ban, és add át a felbontott `StoragePort`-ot a `ProjectListView`-nek egy egyszerű module-szintű "lazy singleton" mintával (pl. egy `let storagePromise: Promise<StoragePort> | null = null; function getStorage() { ... }` — a régi `App.tsx`-et NE módosítsd, NE mountold tovább; a fájl megmarad a repóban referenciának, de a belépési pont mostantól az új router).

    Hozd létre `src/features/projects/ProjectListView.tsx`-et: named export `function ProjectListView()`. `useEffect`-ben hívja meg a storage `list()`-jét, `useState<ProjectListItem[]>([])`-be teszi az eredményt. Ha a lista üres, jelenítse meg a "Nincs megjeleníthető projekt." szöveget (pontosan ezzel a magyar stringgel, a meglévő UX-konvenció szerint). Egyébként rendereljen egy egyszerű listát/táblázatot: minden sorban `name`, `status`, `updatedAt` (formázatlan ISO string elfogadható — ez proof-of-life UI, D-04 szerint a végleges vizuál nem ennek a planek a tárgya). Adj hozzá egy "Új teszt-projekt" gombot: `onClick`-re hívja meg a `createEmptyProject({ name: "Teszt projekt " + new Date().toISOString() })`-öt (a Task 2-ben létrehozott factory-ból), csomagolja egy friss `Envelope<Project>`-be (id: friss `crypto.randomUUID()`, schemaVersion: `CURRENT_APP_SCHEMA_VERSION`, data: a factory eredménye, revision: 0 — a `put()` úgyis felülírja 1-re —, updatedAt: friss ISO, updatedBy: "local-user", deletedAt: null, dirty: true), hívja meg a `storage.put()`-ot, majd hívja újra a `list()`-et és frissítse az állapotot.

    Hozd létre `src/features/projects/ProjectListView.test.tsx`-et: Vitest + Testing Library. Mockold a storage-hozzáférést a `vi.hoisted` + `vi.mock` mintával (EGYEZŐEN az `App.test.tsx`-ben látott mintával — NE valós RxDB-t vagy `container.ts`-t importálj közvetlenül; mockold azt a modult, amiből a `ProjectListView` a storage-ot beszerzi). Tesztesetek a fenti `<behavior>` szerint: (1) üres lista → "Nincs megjeleníthető projekt." látszik; (2) két mock-projekt → mindkét név megjelenik; (3) gombklikk → `put` mock meghívva, majd `list` mock másodszor is meghívva.
  </action>
  <acceptance_criteria>
    - `pnpm dev` elindul és a `/` route a `ProjectListView`-t szolgálja ki hiba nélkül (manuális/automatizált smoke-check: `curl -s http://127.0.0.1:5173/ | grep -c "root"` legalább 1, miután a dev-szerver fut)
    - `ProjectListView.test.tsx` mindhárom esete zöld
    - `src/main.tsx` importálja a `createBrowserRouter`-t és `RouterProvider`-t, és NEM importálja/rendereli többé az `App`-ot
    - `grep -c "ProjectTable" src/features/projects/ProjectListView.tsx` egyenlő 0-val (nincs importálva a régi, teljes feature-készletű komponens — csak mintaként lett elolvasva)
  </acceptance_criteria>
  <verify>
    <automated>pnpm test -- ProjectListView --run 2>&1 | tail -30</automated>
  </verify>
  <done>A `/` route valós, IndexedDB-ből olvasó és oda visszaíró projekt-listát jelenít meg, a teszt-készlet zöld.</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Type | Exports |
|---|---|---|
| `src/domain/model/envelope.ts` | type module | `Envelope<T>`, `CURRENT_APP_SCHEMA_VERSION` |
| `src/domain/model/types.ts` | type module | `Project`, `ProjectListItem`, `ChecklistAnswer`, `FollowUpQuestion`, `DecisionScores`, `ReadinessGap`, `ProjectCompletion`, and related string-union types (mirrors `src/data/types.ts`) |
| `src/domain/model/schema.ts` | Zod schemas | `ProjectSchema`, `createEnvelopeSchema`, `ProjectEnvelopeSchema` |
| `src/domain/model/factory.ts` | pure factory | `createEmptyProject(overrides?)` |
| `src/domain/ports/StoragePort.ts` | interface | `StoragePort` (list/get/put) |
| `src/adapters/storage/indexeddb/db.ts` | RxDB config | `projectEnvelopeSchema`, `createProjectDatabase(storage)` |
| `src/adapters/storage/indexeddb/StorageAdapter.ts` | class | `RxdbStorageAdapter implements StoragePort` |
| `src/app/container.ts` | composition root | `createStorageAdapter(storage)` |
| `src/features/projects/ProjectListView.tsx` | component | `ProjectListView` |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → build pipeline | Új harmadik-fél csomagok (rxdb, rxjs, zod, react-router) kerülnek a supply chain-be |
| Böngésző IndexedDB → alkalmazás olvasási útvonal | A helyi tárolt adatot bármely, az origin-en futó script (jövőbeli XSS) elérhetné — a CSP-hardening Phase 5 tárgya |
| UI "Új teszt-projekt" gomb → StoragePort.put() | Jelenleg csak fejlesztői/teszt-célú, nem valós felhasználói bemenet |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01-SC | Tampering | npm install (rxdb, rxjs, zod, react-router) | high | mitigate | Task 1 blocking human-verify checkpoint: npmjs.com-on ellenőrzött csomagnév/kiadó/letöltésszám a telepítés ELŐTT |
| T-01-01-01 | Tampering / Information Disclosure | IndexedDB olvasási útvonal (StorageAdapter.get/list), nincs CSP még | medium | accept | Phase 1-ben nincs harmadik-fél tartalom-renderelés vagy hálózati bemenet; a szigorú CSP bevezetése explicit Phase 5 tárgya (PWA-héj, biztonság, i18n fázis) — a kockázat elfogadható az ASVS L1 szinten erre az átmeneti időszakra |
| T-01-01-02 | Denial of Service | RxDB írási útvonal (StorageAdapter.put), korlátlan növekedés | low | accept | Egyfelhasználós, lokális app, nincs külső/automatizált írási forrás; a böngésző storage-kvóta hibája dobott hibaként jelenik meg, nem crash-loopként |

</threat_model>

<verification>
- `pnpm exec tsc --noEmit` hiba nélkül lefut a domain/adapters/app/features módosított fájlokra
- `pnpm test -- StorageAdapter ProjectListView --run` mindkét teszt-fájlra zöld
- `pnpm dev` elindítható és a `/` route valós adatot mutat
</verification>

<success_criteria>
- Envelope<Project> + Zod + RxDB StorageAdapter + ProjectListView együtt bizonyítják a teljes stack működését (Walking Skeleton)
- DATA-01 (stabil ID), DATA-02 (sync-envelope), DATA-05 (Zod-validáció put-on) teljesül ezen a ponton
</success_criteria>

<output>
Create `.planning/phases/01-adat-alap-portok-perzisztencia-s-mvp-migr-ci/01-01-SUMMARY.md` when done
</output>
