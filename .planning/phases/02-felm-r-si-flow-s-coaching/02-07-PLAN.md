---
phase: 02-felm-r-si-flow-s-coaching
plan: 07
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - src/features/survey/CockpitPanel.tsx
  - src/features/survey/CockpitPanel.test.tsx
  - src/features/survey/DecisionPanel.tsx
  - src/features/survey/DecisionPanel.test.tsx
  - src/features/survey/OverviewPanel.tsx
  - src/features/survey/OverviewPanel.test.tsx
autonomous: true
requirements: [SURVEY-06, SURVEY-07]
must_haves:
  truths:
    - "A Cockpit-panel mutatja a decision score-t, a readiness %-ot, a gap-listát, és minden gap-sor 'Javítás' gombja meghívja az onFixGap(gap) callbacket a legacy fixGap-szerződéssel (D-08)"
    - "A Decision-panel szerkeszthetővé teszi a 6 decisionScores mezőt (1-5 skála) és a végső Go/No-Go döntés mezőit — enélkül a decisionScore soha nem változna a UI-n keresztül"
    - "Az Overview-panel szerkeszthetővé teszi az összes readiness-képletbe befolyó alapadat-mezőt (ügyfél, kapcsolat, felelősök, üzleti probléma/eredmény/MVP-cél), playbookId szerkesztése NÉLKÜL (D-03 véglegesség)"
  artifacts:
    - path: "src/features/survey/CockpitPanel.tsx"
      provides: "áttekintés + hiányosság-lista panel (D-07 külön nézet)"
    - path: "src/features/survey/DecisionPanel.tsx"
      provides: "decisionScores szerkesztő + végső Go/No-Go döntés panel (D-07 külön nézet)"
    - path: "src/features/survey/OverviewPanel.tsx"
      provides: "projekt alapadat-szerkesztő panel"
  key_links:
    - from: "src/features/survey/CockpitPanel.tsx"
      to: "onFixGap prop"
      via: "gap-sor Button onClick={() => onFixGap(gap)}"
      pattern: "onFixGap"
---

<objective>
Ez a plan porthol Mantine-ra a legacy 3 read/edit tabot, amik D-07 szerint KÜLÖN nézetként élnek tovább az egységesített interjú+checklist mellett: a Cockpit (áttekintés + hiányosság-lista, `DecisionCockpit` legacy mintája), a Decision (a `decisionScores` 6 mezőjének szerkesztése + a végső Go/No-Go döntés rögzítése — enélkül SURVEY-07 decision score-ja soha nem mozdulna el a UI-n keresztül a factory-alapértékről), és egy "Alapadatok" (Overview) panel a readiness-képletbe befolyó projekt-alapadatokhoz (ügyfél, kapcsolattartók, felelősök, üzleti probléma/eredmény/MVP-cél — a `02-RESEARCH.md` Open Question #1 ajánlása szerint, mivel D-05/D-07 kifejezetten csak az Interjú+Checklist egyesítéséről és a Cockpit+Decision elkülönítéséről rendelkezik, az Overview-mezőket NEM tiltja/nem szünteti meg).

Mindhárom panel ÖNÁLLÓAN tesztelhető, props-alapú komponens (Interface-First) — a Wave 3 `SurveyView` szereli össze őket egy közös `useForm<Project>()` kontextusban, negyedik/harmadik fülként a Felmérés (ChecklistCard-lista) mellett.

Purpose: SURVEY-06 (completion %) és SURVEY-07 (readiness/decision score, playbook-súlyokból, szerkesztés után újraszámolva) csak akkor funkcionálisan teljes, ha van UI a mögöttes bemenetek (decisionScores, alapadatok) szerkesztésére és a kimenet (score, gap-lista) megjelenítésére.
Output: `CockpitPanel.tsx`, `DecisionPanel.tsx`, `OverviewPanel.tsx` + tesztjeik.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-felm-r-si-flow-s-coaching/02-CONTEXT.md
@.planning/phases/02-felm-r-si-flow-s-coaching/02-RESEARCH.md
@.planning/phases/02-felm-r-si-flow-s-coaching/02-PATTERNS.md
@.planning/phases/02-felm-r-si-flow-s-coaching/02-UI-SPEC.md
@src/features/project-detail/tabs/CockpitTab.tsx
@src/features/project-detail/tabs/OverviewTab.tsx
@src/domain/model/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: CockpitPanel — áttekintés, score-faktorok, hiányosság-lista, fixGap</name>
  <files>src/features/survey/CockpitPanel.tsx, src/features/survey/CockpitPanel.test.tsx</files>
  <read_first>
    src/features/project-detail/tabs/CockpitTab.tsx (a TELJES fájl — `DecisionCockpit`, `ScoreExplanation`, `EstimateBlockers`, `MiniStat`, `ScoreFactor` — a PONTOS gap-severity/score-magyarázat logika, amit meg kell tartani, csak Mantine `Card`/`Badge`/`Progress`/`Button`/`RingProgress`-re cserélve a kézi `<div className="...">`/`<button>` helyett),
    .planning/phases/02-felm-r-si-flow-s-coaching/02-PATTERNS.md ("Cockpit reuse — gap list + fixGap button" szakasz — az `onFixGap` prop-szerződés és a gap-severity számítás megőrzendő pontosan),
    .planning/phases/02-felm-r-si-flow-s-coaching/02-UI-SPEC.md ("Haladás-jelzők felirata" — "Készenléti pontszám: {readinessPercent}%" és "Döntési pontszám: {decisionScore}"; Color szekció — accent teal KIZÁRÓLAG progress-sávra és aktív állapotra, danger szín kizárólag destruktív akcióra/kritikus gap-badge-re)
  </read_first>
  <action>
    Hozd létre `src/features/survey/CockpitPanel.tsx`-t: named export `function CockpitPanel({ project, onFixGap }: { project: Project; onFixGap: (gap: ReadinessGap) => void })`. Portold a legacy `DecisionCockpit` teljes tartalmát Mantine-ra: (1) egy fő panel `RingProgress` vagy `Text` a `completion.decisionScore`/100-zal + `completion.decisionScoreLabel`, `completion.decisionRecommendation` cím, `completion.nextRecommendedAction` szöveg, és HA van `firstGap` (`completion.readinessGaps[0]`), egy primary `Button` "Következő teendő" felirattal, `onClick={() => onFixGap(firstGap)}`; (2) egy `MiniStat`-szerű sor (Readiness %, readinessState, kritikus/fontos gap-szám) Mantine `Badge`/`Text` kombinációval; (3) egy "Score faktorok" szekció, MINDEN `ScoreFactor` sávot Mantine `Progress`-re cserélve (a legacy `factor-track`/`span style width` helyett); (4) a `ScoreExplanation` tábla-szerű felsorolás (súly% + érték minden 7 tényezőre); (5) az `EstimateBlockers` szekció (miért nem becsülhető, blokkoló gap-ek `severity !== "Pontosítás"` szűrve); (6) a "Hiányzó információk" gap-lista, minden sor `Badge` (severity: Kritikus=danger szín, Fontos=amber, Pontosítás=teal/kék) + kategória + üzenet + nextStep + "Javítás" `Button onClick={() => onFixGap(gap)}`. A `slug(gap.severity)` CSS-osztály-alapú színezést cseréld Mantine `Badge color` propra a UI-SPEC Color-szekció szerinti hozzárendeléssel.

    Hozd létre `src/features/survey/CockpitPanel.test.tsx`-t: egy teszt `Project` fixture-rel (némi `readinessGaps`-szel és nem-alapértelmezett `decisionScores`-szel), asszertáld: (1) a decision score/readiness % szám megjelenik a DOM-ban; (2) a gap-lista minden eleme megjelenik kategóriával+üzenettel; (3) egy gap-sor "Javítás" gombjára kattintva `onFixGap` PONTOSAN azzal a gap-objektummal hívódik.
  </action>
  <acceptance_criteria>
    - `grep -c "onFixGap" src/features/survey/CockpitPanel.tsx` legalább 2 (a "Következő teendő" gomb ÉS a gap-lista "Javítás" gombjai is hívják)
    - `pnpm vitest run src/features/survey/CockpitPanel.test.tsx` zöld
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run src/features/survey/CockpitPanel.test.tsx</automated>
  </verify>
  <done>A Cockpit-panel Mantine-porttal, változatlan onFixGap-szerződéssel és gap-severity logikával renderel; teszttel bizonyítva.</done>
</task>

<task type="auto">
  <name>Task 2: DecisionPanel — decisionScores szerkesztő + végső Go/No-Go döntés</name>
  <files>src/features/survey/DecisionPanel.tsx, src/features/survey/DecisionPanel.test.tsx</files>
  <read_first>
    src/features/project-detail/tabs/DecisionTab.tsx (a TELJES fájl — a `decisionScores` 6 mezőjének szerkesztő UI-ja + a `finalDecision`/`decisionDate`/`decisionMaker`/`decisionNote` mezők),
    src/domain/model/types.ts (`DecisionScores`, `Decision` típusok — MÁR LÉTEZNEK, nem bővítendők)
  </read_first>
  <action>
    Hozd létre `src/features/survey/DecisionPanel.tsx`-t: named export `function DecisionPanel({ control }: { control: Control<Project> })`. RHF `Controller`-ekkel Mantine `NumberInput` vagy `Slider` (min 1, max 5) mind a 6 `decisionScores.*` mezőre (`businessValue`, `strategicAlignment`, `urgency`, `confidence`, `complexity`, `risk` — a `Controller name` `decisionScores.businessValue` stb. mintát követi, `Path<Project>` cast-tal ha szükséges, ugyanaz a technika, mint a 02-06-PLAN.md ChecklistCard-jában), MINDEGYIK mellett egy rövid label, ami jelzi, hogy a Komplexitás/Kockázat mezőknél alacsonyabb érték jobb (`inverseScaleToPercent` — a legacy `ScoreFactor inverted` jelzés szöveges megfelelője). Alatta a végső döntés mezői: `finalDecision` (Mantine `Select`, opciók: "" / "Go" / "Feltételes Go" / "No-Go"), `decisionDate` (dátum input), `decisionMaker` (TextInput), `decisionNote` (Textarea).

    Hozd létre `src/features/survey/DecisionPanel.test.tsx`-t: rendereld egy `useForm<Project>()` kontextusban egy teszt-projekttel, módosíts egy decisionScores mezőt (pl. Sürgősség csúszka/számmező átállítása 5-re) és asszertáld, hogy az RHF `getValues().decisionScores.urgency === 5` a módosítás után; asszertáld, hogy a `finalDecision` Select opciói között szerepel "Go"/"Feltételes Go"/"No-Go".
  </action>
  <acceptance_criteria>
    - `grep -c "decisionScores" src/features/survey/DecisionPanel.tsx` legalább 6 (mind a 6 mező szerepel)
    - `pnpm vitest run src/features/survey/DecisionPanel.test.tsx` zöld
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run src/features/survey/DecisionPanel.test.tsx</automated>
  </verify>
  <done>A Decision-panel szerkeszthetővé teszi a decisionScores mind a 6 mezőjét és a végső döntés mezőit; teszttel bizonyítva.</done>
</task>

<task type="auto">
  <name>Task 3: OverviewPanel — projekt alapadatok (readiness-képletbe befolyó mezők)</name>
  <files>src/features/survey/OverviewPanel.tsx, src/features/survey/OverviewPanel.test.tsx</files>
  <read_first>
    src/features/project-detail/tabs/OverviewTab.tsx (a TELJES fájl — az összes alapadat-mező 3 szekcióban: "Projekt alapadatok", "Kapcsolat és felelősök", "Üzleti összefoglaló"),
    .planning/phases/02-felm-r-si-flow-s-coaching/02-RESEARCH.md ("Open Questions" #1 — az Overview/Alapadatok fül megtartásának indoklása: a mezők közvetlenül a calculateReadinessPercent/collectReadinessGaps képletbe mennek)
  </read_first>
  <action>
    Hozd létre `src/features/survey/OverviewPanel.tsx`-t: named export `function OverviewPanel({ control }: { control: Control<Project> })`. Portold a legacy `OverviewTab` mind a 3 szekcióját (Projekt alapadatok: name/customerOrOrganization/status/priority/deadline/kickoffDate/plannedDecisionDate/affectedTeams; Kapcsolat és felelősök: projectManager/businessAnalyst/productOwner/techLead/contactPhone/contactEmail/contactOther; Üzleti összefoglaló: businessProblem/expectedBusinessOutcome/firstMvpGoal) Mantine `TextInput`/`Select`/`Textarea` + RHF `Controller` mezőkre. A `status`/`priority` mezők Mantine `Select`-ek a meglévő `ProjectStatus`/`Priority` enum-értékekkel (importáld a `data`-t a `domain/model/types.ts` string-union típusaiból KÉZZEL felsorolva, mivel a legacy `projectStatuses`/`priorities` konstans-tömbök a RÉGI `src/data/checklist.ts`-ben élnek — NE importáld onnan, hogy a domain-tisztaság ne sérüljön; deklarálj egy helyi `const projectStatusOptions = ["Előkészítés", "Becslés alatt", "Fejlesztésre kész", "Blokkolt"] as const` és `const priorityOptions = ["Kiemelt", "Fontos", "Alap", "Alacsony"] as const` tömböt ugyanezekkel az értékekkel). A `affectedTeams: string[]` mezőt a legacy mintával (vessző-elválasztott TextInput, `.split(",").map(trim).filter(Boolean)`) kezeld. A `playbookId` mező NEM jelenik meg ezen a panelen (D-03 — a playbook a létrehozás után véglegesen rögzült, nincs szerkesztő UI rá).

    Hozd létre `src/features/survey/OverviewPanel.test.tsx`-t: rendereld egy `useForm<Project>()` kontextusban, módosíts egy TextInput mezőt (pl. `customerOrOrganization`) és asszertáld az RHF `getValues().customerOrOrganization` frissült értékét; asszertáld, hogy a DOM NEM tartalmaz `playbookId` feliratú vagy name-attribútumú mezőt.
  </action>
  <acceptance_criteria>
    - `grep -c "playbookId" src/features/survey/OverviewPanel.tsx` egyenlő 0 (nincs playbookId-szerkesztő mező, D-03)
    - `pnpm vitest run src/features/survey/OverviewPanel.test.tsx` zöld
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run src/features/survey/OverviewPanel.test.tsx</automated>
  </verify>
  <done>Az Overview-panel szerkeszthetővé teszi a readiness-képletbe befolyó alapadat-mezőket, playbookId szerkesztés nélkül; teszttel bizonyítva.</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Type | Exports |
|---|---|---|
| `src/features/survey/CockpitPanel.tsx` | component | `CockpitPanel` |
| `src/features/survey/DecisionPanel.tsx` | component | `DecisionPanel` |
| `src/features/survey/OverviewPanel.tsx` | component | `OverviewPanel` |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| DecisionPanel/OverviewPanel szabad-szöveg mezők -> autosave -> StoragePort.put | Felhasználói bemenet (döntési jegyzet, üzleti probléma stb.) kerül a perzisztált adatba |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-07-01 | Tampering | OverviewPanel — playbookId véletlen szerkeszthetősége | medium | mitigate | Task 3 explicit teszt bizonyítja, hogy a panel NEM rendereli a playbookId mezőt szerkeszthetőként (D-03 véglegesség megtartása UI-szinten is) |
| T-02-07-02 | Information Disclosure (jövőbeli, NEM ebben a fázisban) | decisionNote/businessProblem stb. plain-text render | low | accept | React auto-escape, nincs dangerouslySetInnerHTML; Markdown-render Phase 3-4/5 tárgya |

</threat_model>

<verification>
- `pnpm vitest run src/features/survey/CockpitPanel.test.tsx src/features/survey/DecisionPanel.test.tsx src/features/survey/OverviewPanel.test.tsx` mind zöld
- `pnpm exec tsc --noEmit` hiba nélkül lefut az érintett fájlokra
</verification>

<success_criteria>
- Mind a 3 kiegészítő panel (Cockpit/Decision/Overview) létezik, önmagában tesztelve, készen a Wave 3 SurveyView-integrációra
</success_criteria>

<output>
Create `.planning/phases/02-felm-r-si-flow-s-coaching/02-07-SUMMARY.md` when done
</output>
