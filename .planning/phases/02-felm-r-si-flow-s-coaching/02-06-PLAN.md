---
phase: 02-felm-r-si-flow-s-coaching
plan: 06
type: execute
wave: 2
depends_on: ["02-01", "02-03", "02-04"]
files_modified:
  - src/app/store/checklistUiStore.ts
  - src/app/store/checklistUiStore.test.ts
  - src/features/survey/useAutosave.ts
  - src/features/survey/useAutosave.test.ts
  - src/features/survey/CoachingPanel.tsx
  - src/features/survey/CoachingPanel.test.tsx
  - src/features/survey/ChecklistCard.tsx
  - src/features/survey/ChecklistCard.test.tsx
autonomous: true
requirements: [SURVEY-02, SURVEY-03, SURVEY-05, COACH-01]
must_haves:
  truths:
    - "Egy playbook-tétel kártyája kibontva automatikusan mutatja a 4-rovatos coaching-panelt, külön 'segítség' gomb/kattintás nélkül (D-06)"
    - "A checklist-mezők (státusz/felelős/határidő/válasz/nyitott kérdés/következő lépés) RHF watch(callback)+debounce mintával, NEM useWatch+useEffect-tel indítanak mentést"
    - "A kibontott tételek Set-je Zustandban él, mindig ÚJ Set-példánnyal frissül (sosem in-place mutáció)"
    - "A data-checklist-item={itemId} attribútum és a checklist-{itemId}-status mező-name konvenció megmarad, hogy a D-08 fixGap-navigáció (Wave 3) működjön"
  artifacts:
    - path: "src/app/store/checklistUiStore.ts"
      provides: "Zustand store: expandedItemIds Set<string>, toggle/setAll/expandOnly"
    - path: "src/features/survey/useAutosave.ts"
      provides: "useAutosave(watch, onSave) — RHF watch(callback) + use-debounce"
    - path: "src/features/survey/ChecklistCard.tsx"
      provides: "egy playbook-tétel Accordion-kártyája, beágyazott CoachingPanel-lel és follow-up mezőkkel"
  key_links:
    - from: "src/features/survey/ChecklistCard.tsx"
      to: "src/features/survey/CoachingPanel.tsx"
      via: "kibontott Accordion.Panel automatikusan rendereli a CoachingPanel-t"
      pattern: "CoachingPanel"
    - from: "src/features/survey/useAutosave.ts"
      to: "react-hook-form"
      via: "watch(callback) subscription, NEM useWatch"
      pattern: "watch\\("
---

<objective>
Ez a plan szállítja a guided checklist kártyalista (SURVEY-02/05) és a kérdésenkénti coaching-panel (COACH-01) UI-magját, valamint a debounce-olt autosave (SURVEY-03) hook-ját. A Zustand-alapú `checklistUiStore` tartja a kibontott tételek állapotát (D-15/D-16), a `ChecklistCard` egy playbook-tételt reprezentál Mantine `Accordion.Item`-ként, RHF `Controller`-ekkel a mezőkhöz, és automatikusan (D-06) beágyazza a `CoachingPanel`-t kibontáskor, valamint a follow-up mezőket (D-13, ugyanabba a kártyába ágyazva, NEM külön nézetben).

Ez egy KOMPONENS-SZINTŰ plan — a `SurveyView` (Wave 3, 02-08) fogja összeszerelni ezeket a darabokat egy valódi `useForm<Project>()` + betöltött projekt kontextusába. Itt a komponensek `props`-alapú, önmagában tesztelhető szerződéssel épülnek (Interface-First): a `ChecklistCard` egy `control: Control<Project>` (RHF) és egy `coachingContent: CoachingContent | null` propot kap, NEM saját maga hívja a `ContentPort`-ot.

Purpose: ez a fázis leglátványosabb, leghosszabb UI-felülete — a felhasználó itt tölti a legtöbb időt a felméréssel.
Output: `checklistUiStore.ts`, `useAutosave.ts`, `CoachingPanel.tsx`, `ChecklistCard.tsx` + tesztjeik.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-felm-r-si-flow-s-coaching/02-CONTEXT.md
@.planning/phases/02-felm-r-si-flow-s-coaching/02-RESEARCH.md
@.planning/phases/02-felm-r-si-flow-s-coaching/02-PATTERNS.md
@.planning/phases/02-felm-r-si-flow-s-coaching/02-UI-SPEC.md
@src/features/project-detail/tabs/ChecklistTab.tsx
@src/content/playbook/types.ts
@src/content/coaching/types.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: checklistUiStore (Zustand) + useAutosave (RHF watch + debounce)</name>
  <files>src/app/store/checklistUiStore.ts, src/app/store/checklistUiStore.test.ts, src/features/survey/useAutosave.ts, src/features/survey/useAutosave.test.ts</files>
  <behavior>
    - `toggle(itemId)` egy be nem bontott tételt kibont (bekerül a Set-be), egy kibontottat becsuk (kikerül) — MINDIG új Set-példányt hoz létre
    - `setAll(itemIds, true)` az összes megadott id-t Set-be teszi; `setAll(itemIds, false)` üres Set-re állít
    - `expandOnly(itemId)` PONTOSAN egy elemű Set-re állít (a többi tétel becsukódik)
    - `useAutosave(watch, onSave)`: a mezőérték-változás után `onSave` PONTOSAN egyszer hívódik, a debounce-ablakon (600ms) belüli további változásokra NEM hívódik újra, csak a legutolsóra (fake timer teszt)
  </behavior>
  <read_first>
    .planning/phases/02-felm-r-si-flow-s-coaching/02-RESEARCH.md ("Pattern 1: Playbook-vezérelt checklist kártyalista" — a PONTOS Zustand store-kód; "Pattern 3: Debounce-olt autosave" — a PONTOS useAutosave-kód, react-hook-form `watch(callback)` API, `unsubscribe()` cleanup a useEffect return-jében),
    src/features/project-detail/ProjectDetailView.tsx (sorok 66-102 — a legacy `useState<Set<number>>` + `toggleChecklistItem`/`setAllChecklistItems` MINTÁJA, aminek a szemantikáját a Zustand store megőrzi, csak a lib változik)
  </read_first>
  <action>
    Hozd létre `src/app/store/checklistUiStore.ts`-t PONTOSAN a `02-RESEARCH.md` "Pattern 1" kódja szerint: `useChecklistUiStore` Zustand hook, `expandedItemIds: Set<string>` állapottal (STRING kulcsok, mert a `PlaybookItem.id` numerikus, de a `data-checklist-item`/Accordion `value` prop string-alapú — a hívó oldal `String(item.id)`-t ad át), `toggle(itemId: string)`, `setAll(itemIds: string[], open: boolean)`, `expandOnly(itemId: string)`. MINDEN mutáció ÚJ `Set`-példányt hozzon létre (`new Set(state.expandedItemIds)`), SOHA in-place `.add()`/`.delete()` a state-en (Don't Hand-Roll táblázat figyelmeztetése — ez React-hiba forrás).

    Hozd létre `src/app/store/checklistUiStore.test.ts`-t: a fenti `<behavior>` 3 store-esetére (toggle be+ki, setAll true/false, expandOnly).

    Hozd létre `src/features/survey/useAutosave.ts`-t PONTOSAN a `02-RESEARCH.md` "Pattern 3" kódja szerint: `useAutosave<T>(watch: UseFormWatch<T>, onSave: (values: T) => Promise<void>)`, belül `useDebouncedCallback` (`use-debounce`, 600ms) és egy `useEffect`, ami `watch((values) => debouncedSave(values as T))`-t subscribe-ol, és a return-értékből (`{ unsubscribe }`) cleanup-ot ad vissza.

    Hozd létre `src/features/survey/useAutosave.test.ts`-t: `vi.useFakeTimers()`-rel, egy minimál teszt-komponensben (`useForm()` + `useAutosave(watch, onSaveMock)`), amely mezőváltozást szimulál (`fireEvent`/`userEvent` egy renderelt input mezőn, VAGY közvetlen `setValue`+`trigger` hívással), majd `vi.advanceTimersByTime(600)`, és asszertálja, hogy `onSaveMock` pontosan egyszer hívódott a legutolsó értékkel (nem többször a köztes változásokra).
  </action>
  <acceptance_criteria>
    - `grep -c "new Set(" src/app/store/checklistUiStore.ts` legalább 3 (toggle/setAll/expandOnly mindegyike új példányt hoz létre)
    - `grep -c "useWatch" src/features/survey/useAutosave.ts` egyenlő 0 (a `watch(callback)` mintát követi, NEM `useWatch`-ot)
    - `pnpm vitest run src/app/store/checklistUiStore.test.ts src/features/survey/useAutosave.test.ts` zöld
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run src/app/store/checklistUiStore.test.ts src/features/survey/useAutosave.test.ts</automated>
  </verify>
  <done>A Zustand store és a debounce-autosave hook léteznek, tesztekkel bizonyítva a Set-frissítési fegyelmet és a debounce-időzítést.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: CoachingPanel — 4-rovatos, csak-megjelenítő komponens</name>
  <files>src/features/survey/CoachingPanel.tsx, src/features/survey/CoachingPanel.test.tsx</files>
  <behavior>
    - `content` propként átadott `CoachingContent`-tel mind a 4 rovat-címke ("Miért fontos", "Mit ad technikailag", "Hogyan kérdezz", "Tárgyalási etikett") és a hozzájuk tartozó szöveg megjelenik
    - `content={null}` esetén a panel nem dob hibát, egy rövid "Nincs elérhető coaching-tartalom ehhez a tételhez." üzenetet mutat (védekező eset, bár COACH-01/12 szerint ez éles adatra sosem fordulhat elő)
  </behavior>
  <read_first>
    .planning/phases/02-felm-r-si-flow-s-coaching/02-UI-SPEC.md ("Copywriting Contract" — a PONTOS 4 rovat-címke; "Kiegészítés: Komponens-leltár" — "4× Text blokk címkével egy Paper/Card alszekcióban, az Accordion Panel-en belül"),
    src/content/coaching/types.ts (a Task 02-04-ben létrehozott `CoachingContent` interfész)
  </read_first>
  <action>
    Hozd létre `src/features/survey/CoachingPanel.tsx`-t: named export `function CoachingPanel({ content }: { content: CoachingContent | null })`. Ha `content === null`, rendereld a védekező üzenetet. Egyébként egy Mantine `Paper`/`Card` (`p="md"`, enyhén eltérő háttér, hogy vizuálisan elkülönüljön a mező-szekciótól) belsejében 4 blokk, mindegyik egy `Text fw={600} size="sm"` címkével ("Miért fontos" / "Mit ad technikailag" / "Hogyan kérdezz" / "Tárgyalási etikett") és alatta egy `Text size="sm"` a megfelelő `content.miert`/`content.mit`/`content.hogyan`/`content.etikett` értékkel. A komponens TISZTÁN prezentációs — nincs `ContentPort` hívás, nincs `useEffect`, csak props-render.

    Hozd létre `src/features/survey/CoachingPanel.test.tsx`-t a fenti `<behavior>` szerint: (1) egy teszt `CoachingContent` objektummal render -> mind a 4 címke ÉS mind a 4 szöveg megjelenik; (2) `content={null}` -> a védekező üzenet jelenik meg, nincs dobott hiba/crash.
  </action>
  <acceptance_criteria>
    - `grep -c "Miért fontos" src/features/survey/CoachingPanel.tsx` legalább 1
    - `grep -c "ContentPort" src/features/survey/CoachingPanel.tsx` egyenlő 0 (tisztán prezentációs, nem hívja a portot közvetlenül)
    - `pnpm vitest run src/features/survey/CoachingPanel.test.tsx` zöld
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run src/features/survey/CoachingPanel.test.tsx</automated>
  </verify>
  <done>A 4-rovatos coaching-panel props-alapon renderel, null-content esetén nem crashel; teszttel bizonyítva.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: ChecklistCard — Accordion-tétel, RHF mezők, beágyazott CoachingPanel + follow-up</name>
  <files>src/features/survey/ChecklistCard.tsx, src/features/survey/ChecklistCard.test.tsx</files>
  <behavior>
    - A kártya összecsukott állapotban a tétel `category`/`controlPoint`/`exampleQuestion` szövegét mutatja, a mezők és a CoachingPanel NEM látszanak
    - Kibontott állapotban (Accordion.Panel nyitva) automatikusan megjelenik a CoachingPanel (a Task 2 komponens, a propként kapott coachingContent-tel) — nincs külön "segítség" gomb
    - A "Státusz" mező `name="checklist-{itemId}-status"` attribútummal rendelkezik, a kártya gyökér-eleme `data-checklist-item={itemId}` attribútumot hordoz (D-08 fixGap-kompatibilitás)
    - Follow-up hozzáadása gomb megjelenik kibontott állapotban, kattintásra az `onAddFollowUp(itemId)` callback hívódik
  </behavior>
  <read_first>
    src/features/project-detail/tabs/ChecklistTab.tsx (a TELJES fájl — a per-tétel kártya-szerkezet, a `data-checklist-item={item.id}` és `checklist-${item.id}-status`/`-owner`/`-due-date`/`-answer`/`-open-question`/`-next-step` mező-name konvenció, amit MEGŐRIZNI kell, csak Mantine Accordion+RHF Controllerre cserélve a kézi `<article>`+`SelectField`/`TextField` helyett),
    .planning/phases/02-felm-r-si-flow-s-coaching/02-PATTERNS.md ("src/features/survey/ChecklistCard.tsx" szakasz — a pontos átalakítási minta, mit KELL megőrizni a legacy kártyából),
    .planning/phases/02-felm-r-si-flow-s-coaching/02-UI-SPEC.md ("Kiegészítés: Komponens-leltár" — Accordion `multiple`, controlled `value` a Zustand store-ból; Checklist-mező `Controller`+`Select`/`Textarea`/`TextInput`)
  </read_first>
  <action>
    Hozd létre `src/features/survey/ChecklistCard.tsx`-t: named export `function ChecklistCard({ item, control, coachingContent, onAddFollowUp }: { item: PlaybookItem; control: Control<Project>; coachingContent: CoachingContent | null; onAddFollowUp: (itemId: number) => void })`. A komponens egy Mantine `Accordion.Item value={String(item.id)}` (a szülő `Accordion`-t a Wave 3 `ChecklistCard`-lista hozza létre, EZ a komponens csak egy `Accordion.Item`-et exportál, nem a teljes `Accordion`-t — hívjon a Wave-3 plan egy `<Accordion multiple value={[...]} onChange={...}>{items.map(item => <ChecklistCard key={item.id} ... />)}</Accordion>` mintát). Adj hozzá `data-checklist-item={item.id}`-t az `Accordion.Item` gyökér-DOM-eleméhez (Mantine `Accordion.Item` továbbadja a natív HTML attribútumokat).

    `Accordion.Control`: a `item.category` felirat.

    `Accordion.Panel`: (1) `item.controlPoint`/`item.exampleQuestion` szöveg; (2) RHF `Controller` mezők Mantine primitívekkel, MINDEGYIK `name={\`checklistAnswers.${item.id}.status\`}` (és `.owner`/`.dueDate`/`.answer`/`.openQuestion`/`.nextStep`) mintával — a `Controller` `name` propja RHF `Path<Project>` típusú, egy dinamikus numerikus `Record`-kulcsra a TS-fordító típushibát adhat; ha igen, castold `as Path<Project>`-re (`import type { Path } from "react-hook-form"`) az adott `name` string literálon, KOMMENTBEN jelezve, miért szükséges a cast (a `checklistAnswers: Record<number, ChecklistAnswer>` index-aláírás RHF path-inferenciája nem mindig oldja fel automatikusan a sablon-literál kulcsokat): `Select` (Státusz, `name="checklist-${item.id}-status"` HTML `name` attribútummal is — lásd lentebb), `TextInput` (Felelős, Határidő — `type="date"`), `Textarea` (Válasz/megállapítás, Nyitott kérdés, Következő lépés). MINDEN mezőre állítsd be a natív HTML `name` attribútumot IS (Mantine input-komponensek átadják) PONTOSAN a legacy `checklist-${item.id}-status`/`-owner`/`-due-date`/`-answer`/`-open-question`/`-next-step` konvencióval — ez a `document.querySelector('[name="..."]')`-alapú `fixGap`/`focusField` (Wave 3) számára KRITIKUS, nem opcionális stílus-döntés.

    (3) A `Accordion.Panel` ALJÁN, automatikusan (D-06, nincs külön gomb): `<CoachingPanel content={coachingContent} />`.

    (4) Egy "Follow-up" gomb (`onClick={() => onAddFollowUp(item.id)}`), csak akkor jelenjen meg, ha a form NEM readonly (ebben a plánban egyelőre nincs readonly/edit-mód megkülönböztetés — mindig szerkeszthető; a Wave 3 SurveyView dönti el, ha szükséges egy mód-kapcsoló, de ez NEM ennek a komponensnek a felelőssége).

    Hozd létre `src/features/survey/ChecklistCard.test.tsx`-t: rendereld egy `useForm()`+`FormProvider` (vagy közvetlen `control` prop) kontextusban, EGY teszt `PlaybookItem`-mel és egy teszt `CoachingContent`-tel, egy szülő `<Accordion multiple defaultValue={[]}>`-ba ágyazva. A fenti `<behavior>` szerinti esetek: (1) összecsukva a CoachingPanel szövege NEM látszik a DOM-ban; (2) az Accordion.Control-ra kattintva (kibontás) a CoachingPanel szövege megjelenik; (3) a Státusz mező `name` attribútuma `checklist-{id}-status`; (4) Follow-up gombra kattintva `onAddFollowUp` a helyes id-vel hívódik.
  </action>
  <acceptance_criteria>
    - `grep -c 'data-checklist-item' src/features/survey/ChecklistCard.tsx` legalább 1
    - `grep -c 'checklist-\${item.id}-status' src/features/survey/ChecklistCard.tsx` legalább 1 (vagy ekvivalens template-literál — a lényeg, hogy a natív `name` attribútum PONTOSAN ezt a konvenciót kövesse)
    - `pnpm vitest run src/features/survey/ChecklistCard.test.tsx` zöld
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run src/features/survey/ChecklistCard.test.tsx</automated>
  </verify>
  <done>Egy playbook-tétel Accordion-kártyaként renderel, kibontáskor automatikusan mutatja a coaching-panelt, a legacy mező-name/data-attribútum konvenciót megőrzi; teszttel bizonyítva.</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Type | Exports |
|---|---|---|
| `src/app/store/checklistUiStore.ts` | Zustand store | `useChecklistUiStore` (expandedItemIds, toggle, setAll, expandOnly) |
| `src/features/survey/useAutosave.ts` | hook | `useAutosave(watch, onSave)` |
| `src/features/survey/CoachingPanel.tsx` | component | `CoachingPanel` |
| `src/features/survey/ChecklistCard.tsx` | component | `ChecklistCard` |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Checklist szabad-szöveg mezők (válasz/nyitott kérdés/következő lépés) -> autosave -> StoragePort.put | Felhasználói szabad szöveg kerül a perzisztált adatba, majd egy jövőbeli (Phase 3-4) Markdown-előnézetbe |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-06-01 | Information Disclosure (jövőbeli XSS, NEM ebben a fázisban) | checklist szabad-szöveg mezők -> jövőbeli Markdown-előnézet (Phase 3-4) | low | accept | Ebben a fázisban a mezők plain-text Mantine `Textarea`/`TextInput`-ok, React auto-escape-eli a rendert; nincs `dangerouslySetInnerHTML`. A `rehype-sanitize` bevezetése explicit Phase 5 (PWA/biztonság) tárgya a projekt-szintű STACK.md szerint |
| T-02-06-02 | Tampering (React re-render hiba) | `checklistUiStore` Set-mutáció | medium | mitigate | Task 1 explicit teszt bizonyítja, hogy minden store-akció ÚJ Set-példányt hoz létre, sosem in-place mutációt |

</threat_model>

<verification>
- `pnpm vitest run src/app/store src/features/survey` mind zöld
- `pnpm exec tsc --noEmit` hiba nélkül lefut az érintett fájlokra
</verification>

<success_criteria>
- A guided checklist kártya + coaching-panel + autosave-hook + UI-state-store létezik, önmagában tesztelve, készen a Wave 3 integrációra
</success_criteria>

<output>
Create `.planning/phases/02-felm-r-si-flow-s-coaching/02-06-SUMMARY.md` when done
</output>
