---
phase: 02-felm-r-si-flow-s-coaching
plan: 05
type: execute
wave: 2
depends_on: ["02-01", "02-02", "02-03"]
files_modified:
  - src/features/projects/CreateProjectModal.tsx
  - src/features/projects/CreateProjectModal.test.tsx
  - src/features/projects/ProjectListView.tsx
  - src/features/projects/ProjectListView.test.tsx
autonomous: true
requirements: [SURVEY-01, SURVEY-04]
must_haves:
  truths:
    - "A felhasználó egy Modal-ban nevet ad és playbookot választ (jelenleg 1 opció), és Létrehozás után a projekt valóban perzisztálódik playbookId-vel"
    - "Archiválás (data.archivedAt beállítás) és Törlés (storage.softDelete) KÉT KÜLÖNBÖZŐ, felhasználó számára is megkülönböztetett akció — archiválás után a projekt az Archívum szűrőben látszik, törlés után SEHOL"
    - "A projektlista Aktív/Archívum szűrőnézet kliensoldalon szűr a meglévő storage.list() eredményén, StoragePort-módosítás nélkül"
  artifacts:
    - path: "src/features/projects/CreateProjectModal.tsx"
      provides: "playbook-választós projekt-létrehozó Modal (Mantine + RHF + Zod)"
  key_links:
    - from: "src/features/projects/ProjectListView.tsx"
      to: "src/content/playbook/index.ts"
      via: "playbooks katalógus a CreateProjectModal Select opcióihoz"
      pattern: "playbooks\\."
    - from: "src/features/projects/ProjectListView.tsx"
      to: "src/domain/ports/StoragePort.ts"
      via: "storage.put() archiváláshoz (data.archivedAt), storage.softDelete() törléshez — KÜLÖN útvonalak"
      pattern: "archivedAt"
---

<objective>
Ez a plan váltja fel a Phase 1 Walking Skeleton "Új teszt-projekt" gombját egy valódi SURVEY-01/SURVEY-04 create-flow-val: egy Mantine `Modal` + React Hook Form + Zod-resolver alapú `CreateProjectModal` (név + playbook-választás, D-03 szerint a playbook végleges), és a `ProjectListView` kiterjesztése valódi archiválás/törlés-megkülönböztetéssel (02-RESEARCH.md Pitfall 2 — a két fogalom NEM keverendő) és Aktív/Archívum kliensoldali szűréssel.

Ez a plan a `02-02-PLAN.md` (playbookId mező + `createEmptyProject(playbookId, ...)` szignatúra) és a `02-03-PLAN.md` (`playbooks` katalógus) eredményére épül — a `ProjectListView.tsx`-ben Task 1 (02-02) hagyott egy IDEIGLENES `createEmptyProject("general", ...)` literált; ez a plan azt egy VALÓDI, felhasználó által választott playbook-id-re cseréli.

Purpose: SURVEY-01 (create/list/archive/delete) és SURVEY-04 (playbook-választás) első felhasználó-látható vertikális szelete.
Output: `CreateProjectModal.tsx`, kiterjesztett `ProjectListView.tsx`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-felm-r-si-flow-s-coaching/02-CONTEXT.md
@.planning/phases/02-felm-r-si-flow-s-coaching/02-RESEARCH.md
@.planning/phases/02-felm-r-si-flow-s-coaching/02-PATTERNS.md
@.planning/phases/02-felm-r-si-flow-s-coaching/02-UI-SPEC.md
@src/features/projects/ProjectListView.tsx
@src/content/playbook/index.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CreateProjectModal — Mantine Modal + RHF + Zod, playbook-választás</name>
  <files>src/features/projects/CreateProjectModal.tsx, src/features/projects/CreateProjectModal.test.tsx</files>
  <behavior>
    - A Modal megnyitásakor egy TextInput (Projekt neve) és egy Select (Playbook, 1 opció: "Általános (30 tételes checklist)") jelenik meg
    - Létrehozás gombra kattintva (érvényes név esetén) az `onCreate(name, playbookId)` callback pontosan egyszer meghívódik a beírt névvel és a kiválasztott playbook `id`-jével
    - Üres név esetén a Létrehozás gomb nem hívja meg `onCreate`-et, hanem validációs hibát mutat
  </behavior>
  <read_first>
    .planning/phases/02-felm-r-si-flow-s-coaching/02-UI-SPEC.md ("Copywriting Contract" — Primary CTA "Új projekt létrehozása"/"Létrehozás", playbook mező-címke "Playbook", opció-felirat "Általános (30 tételes checklist)", segédszöveg "A playbook a létrehozás után nem módosítható."; "Kiegészítés: Komponens-leltár" — Modal+TextInput+Select+Button sor),
    src/content/playbook/index.ts (a `playbooks` katalógus — a Select opcióit ebből generáld, NE hardkódold a "general" stringet a komponensben, hogy egy jövőbeli 2. playbook automatikusan megjelenjen),
    .planning/phases/02-felm-r-si-flow-s-coaching/02-RESEARCH.md ("Standard Stack" — `@hookform/resolvers` Zod-resolver a `react-hook-form`-hoz)
  </read_first>
  <action>
    Hozd létre `src/features/projects/CreateProjectModal.tsx`-t: named export `function CreateProjectModal({ opened, onClose, onCreate }: { opened: boolean; onClose: () => void; onCreate: (name: string, playbookId: string) => void })`. Mantine `Modal` (`opened`, `onClose`, `title: "Új projekt létrehozása"`). Belül egy `useForm` (React Hook Form) egy Zod-sémával (`z.object({ name: z.string().min(1, "A projekt neve kötelező."), playbookId: z.string().min(1) })`, `@hookform/resolvers/zod` `zodResolver`-en át kötve). A `name` mezőt `Controller` + Mantine `TextInput` (label: "Projekt neve") rendereli; a `playbookId` mezőt `Controller` + Mantine `Select` (label: "Playbook", `data`: a `playbooks` katalógus `Object.values(...).map(p => ({ value: p.id, label: p.id === "general" ? "Általános (30 tételes checklist)" : p.name }))`, alapértelmezett érték az első playbook `id`-je, mivel jelenleg csak egy létezik). A Select ALATT jelenítsd meg a segédszöveget: "A playbook a létrehozás után nem módosítható." (Mantine `Select` `description` propján vagy egy külön `Text` elemen keresztül). A Modal alján egy "Mégse" (secondary) és egy "Létrehozás" (primary) gomb — a `handleSubmit`-en át hívja meg `onCreate(values.name, values.playbookId)`-t, majd `onClose()`-t.

    Hozd létre `src/features/projects/CreateProjectModal.test.tsx`-t Vitest + Testing Library-vel a fenti `<behavior>` szerint: (1) render `opened=true`, TextInput+Select látszik; (2) name kitöltése + Létrehozás klikk -> `onCreate` mock pontosan egyszer hívva `("...", "general")`-lel; (3) üres name-mel Létrehozás klikk -> `onCreate` NEM hívódik, validációs hibaszöveg megjelenik.
  </action>
  <acceptance_criteria>
    - `grep -c "zodResolver" src/features/projects/CreateProjectModal.tsx` legalább 1
    - `grep -c "A playbook a létrehozás után nem módosítható" src/features/projects/CreateProjectModal.tsx` legalább 1
    - `pnpm vitest run src/features/projects/CreateProjectModal.test.tsx` zöld
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run src/features/projects/CreateProjectModal.test.tsx</automated>
  </verify>
  <done>A CreateProjectModal érvényes név+playbook esetén meghívja onCreate-et, érvénytelen névnél nem; teszttel bizonyítva.</done>
</task>

<task type="auto">
  <name>Task 2: ProjectListView — valódi create-flow, archiválás/törlés megkülönböztetés, Aktív/Archívum szűrő</name>
  <files>src/features/projects/ProjectListView.tsx, src/features/projects/ProjectListView.test.tsx</files>
  <read_first>
    src/features/projects/ProjectListView.tsx (a TELJES jelenlegi tartalom, a 02-02-PLAN.md már playbookId-vel bővített `handleAddTestProject`-tel — ezt a gombot TELJESEN lecseréli ez a task egy Modal-nyitó gombra),
    .planning/phases/02-felm-r-si-flow-s-coaching/02-RESEARCH.md ("Pitfall 2: Két különálló soft delete mechanizmus" — archiválás = `data.archivedAt` + `storage.put()`, törlés = `storage.softDelete()`, SOSEM keverve),
    .planning/phases/02-felm-r-si-flow-s-coaching/02-UI-SPEC.md ("Copywriting Contract" — Empty state, Error state, Destructive confirmation szövegek PONTOSAN; "Nem-destruktív akció megjegyzés" — Archiválás toast "Projekt archiválva.", nincs megerősítő Modal; Törlés IGEN kap megerősítő Modal-t "Mégse"/"Törlés" gombokkal),
    src/domain/model/types.ts (`ProjectListItem.archivedAt: string | null` — MÁR LÉTEZIK, nem kell bővíteni)
  </read_first>
  <action>
    Módosítsd `src/features/projects/ProjectListView.tsx`-et:

    1) Cseréld le a "Új teszt-projekt" gombot és a `handleAddTestProject` függvényt: adj hozzá egy `createModalOpened` state-et (`useState(false)`), egy "Új projekt létrehozása" gombot (ami `setCreateModalOpened(true)`-t hív), és rendereld a `CreateProjectModal`-t (`opened={createModalOpened}`, `onClose`, `onCreate={handleCreateProject}`). `handleCreateProject(name, playbookId)`: hívja meg `createEmptyProject(playbookId, { id, name })`-t (a Task 1-ben/02-02-ben már bővített factory-szignatúrával), csomagolja Envelope-ba (ugyanaz a minta, mint a régi `handleAddTestProject`-ben), `storage.put()`, `refresh()`.

    2) Adj hozzá egy `handleArchive(id)` függvényt: töltse be a projektet `storage.get(id)`-vel, állítsa `data.archivedAt = new Date().toISOString()`-re, és `storage.put()`-tal írja vissza (NE `storage.softDelete()`-öt hívjon — Pitfall 2 szerint ez egy MÁSIK akció). Sikeres archiválás után mutass egy rövid notice-t: "Projekt archiválva." (NINCS megerősítő Modal — az UI-SPEC szerint ez nem-destruktív).

    3) A meglévő `handleDelete(id)` (ami MÁR `storage.softDelete()`-et hív) elé told be egy Mantine `Modal`-alapú megerősítést: "Biztosan törlöd a(z) „{projektnév}" projektet? A törölt projekt egyetlen listában sem jelenik meg többé, ez nem vonható vissza a felületen." gombokkal "Mégse"/"Törlés" (piros) — csak "Törlés" megerősítés UTÁN fusson le a tényleges `storage.softDelete()` hívás.

    4) Adj hozzá egy Aktív/Archívum kapcsolót (pl. Mantine `SegmentedControl` vagy két gomb) és egy `viewFilter: "active" | "archived"` state-et. A renderelt listát `projects.filter(p => viewFilter === "archived" ? p.archivedAt !== null : p.archivedAt === null)`-ként számítsd — a `storage.list()` VÁLTOZATLANUL a teljes (nem-tombstone) listát adja vissza, a szűrés KLIENSOLDALON történik (02-RESEARCH.md Open Question #2 ajánlása — nincs StoragePort-módosítás).

    5) Cseréld az üres-állapot szöveget a UI-SPEC "Empty state heading"/"Empty state body" szerintire, HA a szűrt lista üres ÉS a `viewFilter === "active"` (az archívum-nézet üres állapotára elég egy egyszerűbb "Nincs archivált projekt." szöveg, mert az UI-SPEC nem definiál rá külön copy-t).

    Frissítsd `src/features/projects/ProjectListView.test.tsx`-et: a meglévő "Új teszt-projekt" gombra épülő tesztesetet cseréld le egy olyanra, ami megnyitja a Modal-t, kitölt egy nevet, Létrehozásra kattint, és asszertálja, hogy `storage.put` egy `playbookId: "general"`-t hordozó `data`-val hívódott. Adj hozzá 2 új tesztesetet: (a) Archiválás gombra kattintva `storage.put` hívódik `data.archivedAt` nem-null értékkel, NEM `storage.softDelete`; (b) Törlés gombra kattintva a megerősítő Modal megjelenik, és csak a Modal "Törlés" gombjára kattintva hívódik `storage.softDelete`.
  </action>
  <acceptance_criteria>
    - `grep -c "handleArchive" src/features/projects/ProjectListView.tsx` legalább 1, ÉS a függvény teste `storage.put` hívást tartalmaz, NEM `storage.softDelete`-et
    - `grep -c "CreateProjectModal" src/features/projects/ProjectListView.tsx` legalább 1
    - `grep -c "Új teszt-projekt" src/features/projects/ProjectListView.tsx` egyenlő 0 (a régi proof-of-life gomb eltávolítva)
    - `pnpm vitest run src/features/projects/ProjectListView.test.tsx` zöld
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run src/features/projects/ProjectListView.test.tsx src/features/projects/CreateProjectModal.test.tsx</automated>
  </verify>
  <done>A projektlista valódi create/archive/delete/filter flow-val működik, archiválás és törlés egyértelműen elkülönítve; tesztek zöldek.</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Type | Exports |
|---|---|---|
| `src/features/projects/CreateProjectModal.tsx` | component | `CreateProjectModal` |
| `src/features/projects/ProjectListView.tsx` (kiterjesztve) | component | `ProjectListView` (create/archive/delete/filter) |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Felhasználói create-form input (projekt neve) -> StoragePort.put() | Felhasználó által beírt szabad szöveg kerül a perzisztált Project.name mezőbe |
| Törlés-megerősítő Modal -> StoragePort.softDelete() | Véglegesnek szánt (bár tombstone-alapú) destruktív akció — véletlen kattintás elleni védelem a megerősítő lépés |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-05-01 | Tampering | CreateProjectModal name mező -> Project.name | low | accept | A Zod `ProjectSchema.name: z.string()` már validál típusszinten; a mező sima szövegként renderelődik (React auto-escape), nincs HTML-injektálási felület ebben a fázisban |
| T-02-05-02 | Repudiation / véletlen adatvesztés | Törlés (softDelete) gomb véletlen kattintás | medium | mitigate | Task 2 explicit megerősítő Modal ("Mégse"/"Törlés") a tényleges softDelete hívás elé — Archiválás (nem-destruktív) NEM kap ilyen gátat, mert visszaállítható |

</threat_model>

<verification>
- `pnpm vitest run src/features/projects` mind zöld
- `pnpm exec tsc --noEmit` hiba nélkül lefut az érintett fájlokra
</verification>

<success_criteria>
- SURVEY-01 (create/list/archive/delete) és SURVEY-04 (playbook-választás) első felhasználó számára látható, teljes vertikális szelete kész
</success_criteria>

<output>
Create `.planning/phases/02-felm-r-si-flow-s-coaching/02-05-SUMMARY.md` when done
</output>
