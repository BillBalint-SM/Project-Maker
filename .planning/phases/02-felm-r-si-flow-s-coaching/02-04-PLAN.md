---
phase: 02-felm-r-si-flow-s-coaching
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - src/content/coaching/types.ts
  - src/content/coaching/general.ts
  - src/content/coaching/general.test.ts
  - src/adapters/content/staticContent.ts
  - src/adapters/content/staticContent.test.ts
  - src/app/container.ts
autonomous: true
requirements: [COACH-01, COACH-02, COACH-03]
must_haves:
  truths:
    - "Mind a 30 checklist-tételhez (id 1-30) elérhető egy 4-rovatos (miert/mit/hogyan/etikett) magyar nyelvű CoachingContent bejegyzés"
    - "Minden rovat legfeljebb 2 mondat (D-10) — nincs tréning-anyag jellegű, hosszú bekezdés"
    - "A coaching-tartalom külön adatfájlban él, verziózva, NEM a playbook-tétel része (D-11)"
    - "ContentPort.forQuestion(questionId) a meglévő Phase-1 szerződést (Promise<unknown | null>) valós adattal tölti fel, hiányzó id-re null-t ad"
  artifacts:
    - path: "src/content/coaching/types.ts"
      provides: "CoachingContent interfész (questionId, version, miert, mit, hogyan, etikett)"
    - path: "src/content/coaching/general.ts"
      provides: "coachingCatalog: Record<string, CoachingContent> (30 bejegyzés)"
    - path: "src/adapters/content/staticContent.ts"
      provides: "staticContentAdapter: ContentPort"
  key_links:
    - from: "src/app/container.ts"
      to: "src/adapters/content/staticContent.ts"
      via: "createContainer() visszatérési objektuma tartalmazza a content: staticContentAdapter mezőt"
      pattern: "content: staticContentAdapter"
    - from: "src/adapters/content/staticContent.ts"
      to: "src/content/coaching/general.ts"
      via: "coachingCatalog[questionId] ?? null"
      pattern: "coachingCatalog\\["
---

<objective>
Ez a plan szállítja COACH-01/02/03-at: a coaching-tartalom adatkatalógusát (30 tétel x 4 rovat, magyar, verziózott, D-09/D-10/D-11/D-12 szerint) és a `ContentPort` Phase-1-ben már deklarált (`unknown`-szerződésű) portjának valós implementációját (`staticContentAdapter`), bekötve a kompozíciós gyökérbe (`app/container.ts`).

A `ContentPort.ts` fájlt NEM módosítja ez a plan (a `Promise<unknown | null>` szerződés érintetlen marad, ahogy azt a Phase 1 doc-comment előírja) — a típusos hozzáférést a feature-réteg (Wave 2, `ChecklistCard`/`CoachingPanel`) egy típusos wrapper-függvényen keresztül oldja meg, ami a Port `unknown` válaszát `CoachingContent`-re castolja.

Purpose: COACH-01 (a kérdésenkénti coaching-panel) csak akkor tud reális tartalmat mutatni, ha ez a katalógus létezik és minden playbook-tételhez van bejegyzés.
Output: `src/content/coaching/*`, `src/adapters/content/staticContent.ts`, bővített `src/app/container.ts`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-felm-r-si-flow-s-coaching/02-CONTEXT.md
@.planning/phases/02-felm-r-si-flow-s-coaching/02-RESEARCH.md
@.planning/phases/02-felm-r-si-flow-s-coaching/02-PATTERNS.md
@src/data/checklist.ts
@src/domain/ports/ContentPort.ts
@src/app/container.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: content/coaching — CoachingContent típus + mind a 30 tételre 4-rovatos magyar tartalom</name>
  <files>src/content/coaching/types.ts, src/content/coaching/general.ts, src/content/coaching/general.test.ts</files>
  <read_first>
    src/data/checklist.ts (a TELJES `checklistTemplate` — mind a 30 tétel `id`/`category`/`controlPoint`/`exampleQuestion` mezője a tartalmi horgony minden bejegyzéshez; a `hint` mező NEM használandó szó szerint — D-11/RESEARCH.md "hogyan" útmutató kifejezetten tiltja az `exampleQuestion` szó szerinti duplikálását),
    .planning/phases/02-felm-r-si-flow-s-coaching/02-RESEARCH.md ("Coaching-szöveg szerzési útmutató (D-09/D-10 operacionalizálása)" szakasz — a PONTOS stílus-szabályok minden rovatra),
    .planning/phases/02-felm-r-si-flow-s-coaching/02-CONTEXT.md (D-09/D-10/D-11/D-12 — szerzőség, terjedelem, elkülönítés, teljes lefedettség)
  </read_first>
  <action>
    Hozd létre `src/content/coaching/types.ts`-t: exportáld a `CoachingContent` interfészt: `questionId: string` (megegyezik a `PlaybookItem.id`-vel, playbook-scope-on belül — string-ként tárolva, mert a `ContentPort.forQuestion(questionId: string)` szerződés is string-et vár), `version: number` (COACH-02 — kezdő érték `1` minden bejegyzésre), `miert: string`, `mit: string`, `hogyan: string`, `etikett: string`. Adj hozzá egy `CoachingContentSchema` Zod-sémát is (minden string-mező `z.string().min(1)`, `version: z.number().int().positive()`) — ez a build/teszt-idejű integritás-védelem hiányos/hibás bejegyzés ellen (Security Domain V5, `02-RESEARCH.md` "Known Threat Patterns").

    Hozd létre `src/content/coaching/general.ts`-t: exportáld a `coachingCatalog: Record<string, CoachingContent>` konstanst, KULCSOLVA a `checklistTemplate`/"general" playbook `id`-jainak string-alakjával ("1".."30"), MIND a 30 tételre egy-egy bejegyzéssel. Minden bejegyzés `miert`/`mit`/`hogyan`/`etikett` mezője KÖVESSE PONTOSAN az alábbi stílus-szabályokat (D-09/D-10, `02-RESEARCH.md` szerzési útmutatója):
    - `miert` (miért fontos): PONTOSAN 1 mondat, üzleti kockázat/érték nyelven (NEM technikai).
    - `mit` (mit ad technikailag): PONTOSAN 1 mondat, mire használja fel a rendszer/csapat ezt az adatot (pl. becslési pontosság, tesztlefedettség-tervezés).
    - `hogyan` (hogyan kérdezz): 1-2 konkrét, üzleti nyelvű kérdésmintázat — NE a `checklistTemplate.exampleQuestion` szó szerinti duplikálása, hanem kiegészítő/alternatív megfogalmazás, gyakorlatibb, tárgyalás-technikai szinten.
    - `etikett` (tárgyalási etikett): 1 mondat, mire figyeljen a kérdező interperszonálisan.
    - Minden rovat LEGFELJEBB 2 mondat (D-10) — tömör, pásztázható, munka-közbeni olvasásra, NEM oktató jellegű bekezdés.
    - A tartalom hangneme tömör és gyakorlatias, NEM akadémikus (CONTEXT.md "Specific Ideas").

    Két KIDOLGOZOTT PÉLDA-BEJEGYZÉS (ugyanezt a minőséget és formátumot kövesd mind a 30 tételre, a `checklistTemplate` MEGFELELŐ `category`/`controlPoint`/`exampleQuestion` tartalmára alapozva minden egyes id-nél — ne generikus szöveget írj, minden bejegyzés az adott tétel konkrét tartalmára reagáljon):

    id "1" (Üzleti cél): `miert`: "Enélkül a projekt könnyen funkciólistává silányul, és a scope menet közben ellenőrizhetetlenül csúszik szét, mert nincs mihez mérni a döntéseket." `mit`: "Ez adja a scope-védelem és a prioritás-döntések viszonyítási alapját a teljes fejlesztés alatt." `hogyan`: "Kérdezz rá kétszer: mi történik, ha ezt NEM fejlesztjük le? És: honnan tudjuk majd, hogy megérte?" `etikett`: "Ne fogadd el válaszként magát a funkció-ötletet — ha egy konkrét gomb vagy képernyő a válasz, kérdezz vissza a mögötte álló üzleti okra."

    id "2" (Sikerkritérium): `miert`: "Mérhető siker nélkül a projekt lezárása és az utólagos érték-igazolás is vitatott marad — senki nem tudja megmondani, sikerült-e." `mit`: "A tesztelési és elfogadási kritériumok, valamint a későbbi utó-mérés ebből a mérőszámból indul ki." `hogyan`: "Kérdezz konkrét számot vagy arányt: mennyivel csökken vagy nő valami, és mihez képest? Kérj hozzá egy dátumot is, mikorra várható ez." `etikett`: "Ha csak érzésre (\"jobb lesz\") válaszolnak, ne engedd el a kérdést — segíts konkretizálni egy közös példával, ne minősítsd a válasz hiányát."

    Írd meg a fennmaradó 28 bejegyzést (id "3".."30") UGYANEZZEL a minőséggel és formátummal, a `checklistTemplate` megfelelő `category`/`controlPoint`/`exampleQuestion` tartalmára alapozva minden egyes tételnél (pl. id "3" Stakeholderek, id "4" Felhasználók, ... id "30" Dokumentáció — lásd a teljes listát `src/data/checklist.ts`-ben).

    Hozd létre `src/content/coaching/general.test.ts`-t: (1) `Object.keys(coachingCatalog).length === 30`; (2) `Object.keys(coachingCatalog)` pontosan `["1", "2", ..., "30"]` (nincs hiányzó/duplikált kulcs); (3) minden bejegyzésre `CoachingContentSchema.parse(entry)` nem dob hibát; (4) minden bejegyzés mind a 4 string-mezője (`miert`/`mit`/`hogyan`/`etikett`) legfeljebb 2 mondatot tartalmaz (egyszerű heurisztika: a mondatvégi írásjelek — `.`/`!`/`?` — száma legfeljebb 2 minden mezőre, engedve az `pl.`/rövidítés-szerű pontokat egy toleranciasávval, vagy egy egyszerűbb `split(/[.!?]/).filter(Boolean).length <= 2` ellenőrzéssel).
  </action>
  <acceptance_criteria>
    - `grep -c "interface CoachingContent" src/content/coaching/types.ts` egyenlő 1
    - `pnpm vitest run src/content/coaching/general.test.ts` zöld, mind a 4 esettel (30 bejegyzés, teljes id-lefedettség, Zod-validitás, mondatszám-korlát)
    - Kézi mintavétel: 3 véletlenszerű bejegyzés (pl. id "5", "15", "25") `hogyan` mezője NEM egyezik szó szerint a `checklistTemplate` megfelelő `exampleQuestion`-jével
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run src/content/coaching/general.test.ts</automated>
  </verify>
  <done>Mind a 30 tételre létezik egy Zod-valid, magyar, 4-rovatos, legfeljebb 2-mondatos coaching-bejegyzés, teszttel bizonyítva.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: ContentPort implementáció (staticContentAdapter) + container.ts wiring</name>
  <files>src/adapters/content/staticContent.ts, src/adapters/content/staticContent.test.ts, src/app/container.ts</files>
  <behavior>
    - `staticContentAdapter.forQuestion("1")` a `coachingCatalog["1"]`-gyel egyező objektumot ad vissza (Promise-ba csomagolva)
    - `staticContentAdapter.forQuestion("nem-letezo-id")` `null`-t ad vissza, nem dob hibát
    - `createContainer(...)` visszatérési objektuma tartalmaz egy `content: staticContentAdapter` mezőt, a meglévő `storage`/`llm`/`sync` mezők MELLETT (nem helyettük)
  </behavior>
  <read_first>
    src/domain/ports/ContentPort.ts (a VÁLTOZATLANUL hagyandó szerződés: `forQuestion(questionId: string): Promise<unknown | null>`),
    src/adapters/llm/noop.ts (a Noop-adapter-implementálja-portot minta: plain objektum-literál, `const`-ként exportálva, nincs class egy stateless, csak-olvasó lookup-hoz),
    src/app/container.ts (a TELJES jelenlegi fájl — a "NOTE — ContentPort/ExportPort are intentionally NOT wired..." komment, amit ez a plan old fel a ContentPort felére; a `createContainer` visszatérési típusa bővítendő),
    .planning/phases/02-felm-r-si-flow-s-coaching/02-PATTERNS.md ("src/content/coaching/types.ts, general.ts + src/adapters/content/staticContent.ts" szakasz — pontos adapter-kód és container-bővítés)
  </read_first>
  <action>
    Hozd létre `src/adapters/content/staticContent.ts`-t: exportáld a `staticContentAdapter: ContentPort` konstanst, a `src/adapters/llm/noop.ts` mintáját követve (plain objektum-literál, nem class): `{ async forQuestion(questionId: string) { return coachingCatalog[questionId] ?? null; } }`, ahol `coachingCatalog`-ot a Task 1-ben létrehozott `content/coaching/general.ts`-ből importálod.

    Módosítsd `src/app/container.ts`-t: (1) importáld `staticContentAdapter`-t; (2) töröld vagy szűkítsd a "NOTE — ContentPort/ExportPort are intentionally NOT wired..." komment ContentPort-ra vonatkozó részét (az ExportPort rész MARADJON — az Phase 4 tárgya, változatlanul); (3) bővítsd a `createContainer` visszatérési TÍPUSÁT egy `content: ContentPort` mezővel, és a visszatérési OBJEKTUMOT egy `content: staticContentAdapter` bejegyzéssel, a meglévő `storage`/`llm`/`sync` mezők mellett.

    Hozd létre `src/adapters/content/staticContent.test.ts`-t: a fenti `<behavior>` szerinti 2 eset (ismert id -> egyező tartalom, ismeretlen id -> null), plusz egy smoke-teszt, ami `Object.keys(coachingCatalog)` MINDEN kulcsára meghívja `staticContentAdapter.forQuestion(id)`-t és asszertálja, hogy egyik sem `null` (COACH-01/02 teljes lefedettség-ellenőrzés az adapter-rétegen keresztül is, nem csak a Task 1 katalógus-teszten).
  </action>
  <acceptance_criteria>
    - `grep -c "content: staticContentAdapter" src/app/container.ts` egyenlő 1
    - `grep -c "class" src/adapters/content/staticContent.ts` egyenlő 0 (plain objektum-literál, a Noop-adapter mintáját követve, nem class)
    - `pnpm vitest run src/adapters/content/staticContent.test.ts` zöld
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run src/adapters/content/staticContent.test.ts src/app/container.test.ts && pnpm exec tsc --noEmit 2>&1 | grep -E "adapters/content|app/container" ; echo "exit:$?"</automated>
  </verify>
  <done>A ContentPort valós, teszttel bizonyított implementációval bír, bekötve a kompozíciós gyökérbe; a meglévő container.test.ts továbbra is zöld.</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Type | Exports |
|---|---|---|
| `src/content/coaching/types.ts` | type module + Zod | `CoachingContent`, `CoachingContentSchema` |
| `src/content/coaching/general.ts` | static data | `coachingCatalog: Record<string, CoachingContent>` (30 bejegyzés) |
| `src/adapters/content/staticContent.ts` | adapter | `staticContentAdapter: ContentPort` |
| `src/app/container.ts` (bővítve) | composition root | `createContainer(...)` visszatérési objektuma `+= content: ContentPort` |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Statikus `content/coaching/general.ts` -> ChecklistCard/CoachingPanel render (Wave 2) | Hiányos/hibás coaching-bejegyzés futásidőben render-hibát vagy hiányzó tartalmat okozhat egy kibontott checklist-kártyán |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-04-01 | Tampering (adat-integritás) | `content/coaching/general.ts` (30x4 statikus szöveg) | medium | mitigate | Task 1 Zod-séma + smoke-teszt (30 kulcs, teljes lefedettség, mondatszám-korlát) build/teszt-időben elkapja a hiányos/hibás bejegyzést, mielőtt a Wave-2 UI megjelenítené |
| T-02-04-02 | Information Disclosure (jövőbeli, NEM ebben a fázisban) | statikus coaching-szöveg render — plain React text node, nincs `dangerouslySetInnerHTML` | low | accept | A coaching-tartalom sima string, React auto-escape-eli; nincs Markdown-render ebben a fázisban (az Phase 3-4 tárgya, `02-RESEARCH.md` Security Domain explicit jelzi) |

</threat_model>

<verification>
- `pnpm vitest run src/content/coaching src/adapters/content` mind zöld
- `pnpm vitest run src/app/container.test.ts` továbbra is zöld (nem regresszált a bővítés miatt)
- `pnpm exec tsc --noEmit` hiba nélkül lefut az érintett fájlokra
</verification>

<success_criteria>
- COACH-01/02/03 teljes adat-alapja kész: 30 tételre 4-rovatos, magyar, verziózott, Zod-valid coaching-tartalom, ContentPort mögé kötve
</success_criteria>

<output>
Create `.planning/phases/02-felm-r-si-flow-s-coaching/02-04-SUMMARY.md` when done
</output>
