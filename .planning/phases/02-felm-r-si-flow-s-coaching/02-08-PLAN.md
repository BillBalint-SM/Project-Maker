---
phase: 02-felm-r-si-flow-s-coaching
plan: 08
type: execute
wave: 3
depends_on: ["02-05", "02-06", "02-07"]
files_modified:
  - src/features/survey/SurveyView.tsx
  - src/features/survey/SurveyView.test.tsx
  - src/main.tsx
  - src/features/projects/ProjectListView.tsx
  - src/features/projects/ProjectListView.test.tsx
autonomous: true
requirements: [SURVEY-02, SURVEY-03, SURVEY-05, SURVEY-06, SURVEY-07]
must_haves:
  truths:
    - "A felhasználó a projektlistából megnyithat egy projektet a /projects/:id route-on, és ott egy 4-fülű (Alapadatok/Felmérés/Cockpit/Döntés) egységes felületen szerkesztheti a teljes felmérést"
    - "Egy checklist-mező szerkesztése után (debounce-ablakon túl) storage.put() hívódik ÚJRASZÁMOLT completion/readiness/decisionScore értékekkel"
    - "A Cockpit gap-listájának 'Javítás' gombja átvált a Felmérés fülre, kibontja és a nézetbe görgeti/fókuszálja a megfelelő checklist-mezőt (D-08 fixGap)"
    - "Belépéskor (vagy folytatáskor) csak az első kitöltetlen checklist-tétel van kibontva, a többi összecsukva (D-15); a 'mind nyit/zár' vezérlő is működik (D-16)"
  artifacts:
    - path: "src/features/survey/SurveyView.tsx"
      provides: "a teljes egységesített felmérési nézet: adatbetöltés, useForm<Project>, 4 fül, autosave, fixGap"
  key_links:
    - from: "src/main.tsx"
      to: "src/features/survey/SurveyView.tsx"
      via: "createBrowserRouter route: { path: \"/projects/:id\", Component: SurveyView }"
      pattern: "projects/:id"
    - from: "src/features/survey/SurveyView.tsx"
      to: "src/features/survey/useAutosave.ts"
      via: "useAutosave(watch, handleSave) — handleSave újraszámol és storage.put()-ol"
      pattern: "useAutosave"
    - from: "src/features/survey/SurveyView.tsx"
      to: "src/app/store/checklistUiStore.ts"
      via: "fixGap: setActiveTab + expandOnly(gap.checklistItemId) + focusField querySelector"
      pattern: "expandOnly"
---

<objective>
Ez a plan szereli össze a Wave 2-ben (02-05/06/07) önállóan épített darabokat egy valódi, felhasználó által elérhető felmérési nézetbe: a `SurveyView` betölti a projektet (`storage.get(id)`), egy `useForm<Project>()`-et inicializál a betöltött adattal, 4 fület jelenít meg Mantine `Tabs`-szal (Alapadatok/Felmérés/Cockpit/Döntés — D-07 szerint Cockpit+Döntés külön marad, D-05 szerint Felmérés az egyesített interjú+checklist), a `useAutosave` hook-on át debounce-olt mentést indít (ami a mentés ELŐTT playbook-paraméteresen újraszámolja a completion/readiness/decisionScore-t), és megvalósítja a D-08 `fixGap` cross-fül navigációt (Cockpit gap-lista -> Felmérés fül + kibontás + görgetés/fókusz).

Emellez a route-ot (`/projects/:id`) beköti a `main.tsx`-be, és a `ProjectListView`-t egy "Megnyitás" linkkel bővíti soronként.

Ez a plan zárja le a Phase 2 vertikális szeletét: ETTŐL A PONTTÓL a felhasználó ténylegesen végig tudja csinálni a teljes felmérést playbook-választástól a Cockpit döntési javaslatig.

Purpose: SURVEY-02/03/05/06/07 végpontig-végpontig bizonyítása egyetlen, felhasználó által kattintható felületen.
Output: `src/features/survey/SurveyView.tsx`, bővített `src/main.tsx` (route + ContentPort singleton), bővített `src/features/projects/ProjectListView.tsx` ("Megnyitás" link).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-felm-r-si-flow-s-coaching/02-CONTEXT.md
@.planning/phases/02-felm-r-si-flow-s-coaching/02-RESEARCH.md
@.planning/phases/02-felm-r-si-flow-s-coaching/02-PATTERNS.md
@.planning/phases/02-felm-r-si-flow-s-coaching/02-UI-SPEC.md
@src/features/project-detail/ProjectDetailView.tsx
@src/main.tsx
@src/content/playbook/index.ts
@src/adapters/content/staticContent.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: SurveyView — adatbetöltés, useForm, 4 fül, autosave-újraszámolás, fixGap</name>
  <files>src/features/survey/SurveyView.tsx</files>
  <read_first>
    src/features/project-detail/ProjectDetailView.tsx (a TELJES fájl — a tab-router `activeTab` state, a `fixGap`/`focusField` PONTOS logikája [sorok 121-154], az `updateField`/`updateChecklist`/`addFollowUp` onChange-updater minta — ez a save-path konvenció, amit az autosave-nek kell reprodukálnia, csak RHF-fel, nem kézi useState-tel),
    src/domain/scoring/completion.ts, readiness.ts, decisionScore.ts, gaps.ts, followups.ts (a Task 02-03-ban létrehozott playbook-paraméteres scoring-modulok — az autosave handler ezeket hívja újraszámoláshoz),
    src/content/playbook/index.ts (`playbooks` katalógus — `playbooks[project.playbookId]` lookup a scoring-hívásokhoz),
    src/adapters/content/staticContent.ts (`staticContentAdapter` — a coaching-tartalom betöltéséhez minden playbook-tételre),
    .planning/phases/02-felm-r-si-flow-s-coaching/02-UI-SPEC.md ("Haladás-jelzők felirata" — "Kitöltöttség: {completion}%" a checklist-fejléc felett; "Kiegészítés: Komponens-leltár" — Tabs komponens D-07-hez, Accordion multiple controlled Zustand expandedItemIds-ből)
  </read_first>
  <action>
    Hozd létre `src/features/survey/SurveyView.tsx`-t: named export `function SurveyView()`. Olvasd ki a `:id` route-paramétert (`useParams<{ id: string }>()` a `react-router`-ből). `useEffect`-ben töltsd be a projektet: `storage.get(id)` (a `getStorage()`-ból, mint a `ProjectListView`-ban), és tartsd `useState<Envelope<Project> | null>(null)`-ban az EREDETI envelope-ot (a `revision`/`deletedAt` megőrzéséhez a mentéskor), valamint `useState<Project | null>(null)`-ban a JELENLEGI (esetlegesen újraszámolt) `data`-t, amit a Cockpit-panelnek adsz át propként (ez KÜLÖNBÖZIK az RHF form-értékektől: a `project` state a MEGJELENÍTETT/újraszámolt derived-state, az RHF a SZERKESZTHETŐ mezőkötéseket kezeli). Ha `storage.get(id)` `null`-t ad (nincs ilyen projekt), jelenítsd meg a UI-SPEC "Betöltési hiba" szövegét ("A projekt betöltése nem sikerült. Frissítsd az oldalt, vagy térj vissza a projektlistához.") egy "Vissza a projektlistához" linkkel.

    Inicializálj egy `useForm<Project>()`-et (react-hook-form). Amikor a betöltés befejeződik, hívj egy `reset(envelope.data)`-t (RHF `reset` — a form defaultValues-ait NEM lehet aszinkron betöltött adattal kezdetben megadni, a `reset()` hívás tölti fel utólag). Töltsd be a coaching-tartalmat MINDEN playbook-tételre egy `Promise.all(playbook.items.map(item => staticContentAdapter.forQuestion(String(item.id))))`-lel, `useEffect`-ben, egyszer a playbook betöltésekor (a playbook a `playbooks[envelope.data.playbookId] ?? playbooks.general`-ből jön), és tárold `Record<string, CoachingContent>` állapotban (`useState`).

    `handleSave(values: Project)`: keresd meg a playbookot (`playbooks[values.playbookId] ?? playbooks.general`), számold újra `calculateCompletion(values, playbook)`-t, építs egy `recalculated: Project = { ...values, completion }`-t, majd egy Envelope-ot az EREDETI (állapotban tartott) envelope `id`/`revision`/`deletedAt`-jából + `recalculated` data-ból (`schemaVersion: CURRENT_APP_SCHEMA_VERSION`, `updatedBy`/`updatedAt` a `storage.put()` úgyis felülírja), hívd `storage.put(envelope)`-t, majd `setProject(recalculated)`-tel frissítsd a Cockpit-panelnek átadott state-et (a form-mezőket NEM kell `reset()`-elni ehhez, mert az RHF már a friss `values`-t tartja — csak a SZÁMOLT `completion` nem RHF-mező, azt a `project` state hordozza). Kösd be `useAutosave(watch, handleSave)`-et (a 02-06-PLAN.md hook-ja).

    Rendereld a 4 fület Mantine `Tabs`-szal, belső érték-készlet `"overview" | "checklist" | "cockpit" | "decision"` (a `"overview"`/`"checklist"`/`"decision"` értékek EGYEZZENEK a MEGLÉVŐ `GapTargetTab` union literáljaival — ez teszi triviálissá a `fixGap` leképezést, lásd lentebb): "Alapadatok" (`OverviewPanel control={control}`), "Felmérés" (a `completion.percent`-et mutató "Kitöltöttség: {percent}%" `Progress`/felirat a fül tetején + "Összes nyitása"/"Összes zárása" gombpár, ami `useChecklistUiStore`-t hívja `setAll(playbook.items.map(i => String(i.id)), true/false)`-szal + egy Mantine `Accordion multiple value={Array.from(expandedItemIds)} onChange={...}` a `playbook.items.map(item => <ChecklistCard key={item.id} item={item} control={control} coachingContent={coachingMap[String(item.id)] ?? null} onAddFollowUp={handleAddFollowUp} />)`-vel), "Cockpit" (`CockpitPanel project={project} onFixGap={fixGap}`), "Döntés" (`DecisionPanel control={control}`).

    `handleAddFollowUp(itemId: number)`: hívja `createFollowUpFromChecklist(getValues(), playbook, itemId)`-t (02-03-ból), és `setValue("followUps", [...getValues("followUps"), newFollowUp])`-pal told be az RHF form-állapotba (ez triggereli a `watch`-ot, tehát az autosave-et is).

    `fixGap(gap: ReadinessGap)`: `setActiveTab(gap.targetTab === "followups" ? "checklist" : gap.targetTab)` (a `GapTargetTab` 4 literálja közül a `"followups"`-t a `"checklist"` fülre képezed, mert D-13 szerint a follow-up mezők a checklist-kártyán BELÜL élnek, nincs önálló "followups" fül). Ha `gap.checklistItemId` létezik: hívj `useChecklistUiStore.getState().toggle`/`expandOnly`-t úgy, hogy a célzott tétel MINDENKÉPP bekerüljön a kibontott Set-be (ne csukj be más már nyitott tételeket feltétlenül — a legacy `fixGap` is CSAK hozzáadja az adott elemet a Set-hez, nem `expandOnly`-t hív), majd `window.setTimeout(() => { const el = document.querySelector('[name="checklist-' + gap.checklistItemId + '-status"]'); el?.scrollIntoView({behavior:"smooth", block:"center"}); el?.focus(); }, 80)` — PONTOSAN a legacy `focusField` időzítési mintáját követve (80ms setTimeout, hogy a fül-váltás utáni re-render lefusson a scroll/focus előtt). Ha `gap.targetField` van (Alapadatok-fülre mutató gap): ugyanez a mintázat, de `document.querySelector('[name="' + gap.targetField + '"]')`-vel.

    D-15 (belépéskor csak az első kitöltetlen tétel nyitva): amikor a projekt betöltődik ÉS a playbook ismert, számold ki az első tételt, aminek `checklistAnswers[item.id]?.status !== "Kész"` (vagy nincs válasz), és hívd `useChecklistUiStore.getState().expandOnly(String(firstIncompleteItem.id))`-t EGYSZER, a betöltés `useEffect`-jében (ha minden tétel kész, ne bontson ki semmit, vagy bontsa ki az elsőt — ez nem UAT-kritikus részlet, a legacy `new Set([1])` mintájának "első hiányzó tételre" általánosítása).
  </action>
  <acceptance_criteria>
    - `grep -c 'gap.targetTab === "followups" ? "checklist"' src/features/survey/SurveyView.tsx` legalább 1 (a followups->checklist fül-leképezés explicit)
    - `grep -c "calculateCompletion" src/features/survey/SurveyView.tsx` legalább 1 (az autosave mentés előtt újraszámol)
    - `grep -c "expandOnly" src/features/survey/SurveyView.tsx` legalább 1 (D-15 kezdeti kibontás)
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec tsc --noEmit 2>&1 | grep -E "features/survey/SurveyView" ; echo "exit:$?"</automated>
  </verify>
  <done>A SurveyView betölti a projektet, 4 fülön szerkeszthetővé teszi, autosave-ra újraszámol és ment, és a fixGap a legacy szerződést reprodukálja.</done>
</task>

<task type="auto">
  <name>Task 2: Routing — /projects/:id route + ContentPort singleton + "Megnyitás" link a listából</name>
  <files>src/main.tsx, src/features/projects/ProjectListView.tsx</files>
  <read_first>
    src/main.tsx (a TELJES jelenlegi tartalom — a `getStorage()` lazy-singleton minta, amit egy `getContentPort()` társ-singletonnal egészítesz ki, ÉS a router-konfiguráció, amit egy második route-tal bővítesz),
    src/adapters/content/staticContent.ts (`staticContentAdapter` — szinkron konstruálható, NEM igényel async DB-nyitást, ezért NEM a `createContainer()`-en át kötendő be — az egy MÁSODIK `createStorageAdapter`/RxDB-megnyitást indítana, ami RxDB DB8 "name already used" hibát dobna a MÁR NYITOTT `getStorage()` singleton mellett)
  </read_first>
  <action>
    Módosítsd `src/main.tsx`-et: importáld `staticContentAdapter`-t és exportálj egy `getContentPort(): ContentPort` függvényt, ami egyszerűen visszaadja `staticContentAdapter`-t (nincs Promise/lazy-init szükséges, mivel a statikus adapter szinkron konstruálható — NE hívd ehhez a `createContainer()`-t, mert az egy MÁSODIK `createStorageAdapter(getRxStorageDexie())`-t indítana a MÁR nyitott `getStorage()` singleton database-je mellett, ami RxDB "DB8: database name already used" hibát dobna). Bővítsd a `createBrowserRouter([...])` route-listáját egy MÁSODIK bejegyzéssel: `{ path: "/projects/:id", Component: SurveyView }` (importáld `SurveyView`-t a `./features/survey/SurveyView`-ból).

    Módosítsd `src/features/projects/ProjectListView.tsx`-et: minden projekt-sorhoz adj egy "Megnyitás" linket/gombot (React Router `Link to={`/projects/${project.id}`}`), ami a SurveyView route-ra navigál.
  </action>
  <acceptance_criteria>
    - `grep -c 'path: "/projects/:id"' src/main.tsx` egyenlő 1
    - `grep -c "getContentPort" src/main.tsx` legalább 1
    - `grep -c "Megnyitás" src/features/projects/ProjectListView.tsx` legalább 1
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec tsc --noEmit 2>&1 | grep -E "main.tsx|ProjectListView" ; echo "exit:$?"</automated>
  </verify>
  <done>A `/projects/:id` route a SurveyView-t szolgálja ki; a projektlista minden sorából navigálható a felmérési nézetre.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: SurveyView integrációs teszt — betöltés, szerkesztés+autosave, fixGap</name>
  <files>src/features/survey/SurveyView.test.tsx, src/features/projects/ProjectListView.test.tsx</files>
  <behavior>
    - A SurveyView betöltéskor megjeleníti a projekt nevét és a 4 fület
    - Egy checklist-mező (Státusz "Kész"-re állítása) módosítása után, a debounce-ablak (fake timer előretekerés) UTÁN, `storage.put()` egy MEGVÁLTOZOTT `completion.percent`-tel hívódik (magasabb, mint a kiinduló érték)
    - A Cockpit fülön egy gap-sor "Javítás" gombjára kattintva a nézet átvált a Felmérés fülre, és a megfelelő checklist-mező (name attribútum alapján) fókuszt kap
  </behavior>
  <read_first>
    src/features/projects/ProjectListView.test.tsx (a `vi.hoisted` + `vi.mock("../../main", ...)` minta — a SurveyView tesztje UGYANEZT a mintát alkalmazza, `getStorage` ÉS `getContentPort` mindkettő mockolva)
  </read_first>
  <action>
    Hozd létre `src/features/survey/SurveyView.test.tsx`-t: mockold a `../../main` modult (`getStorage` egy mock StoragePort-tal, aminek `get()` egy teszt-Envelope-ot ad vissza egy nem-teljes checklisttel; `getContentPort` egy mock ContentPort-tal, aminek `forQuestion` mindig egy minimál `CoachingContent`-et ad vissza), mockold a `react-router` `useParams`-ot (`{ id: "test-id" }`), és `vi.useFakeTimers()`-t a debounce-teszthez. A fenti `<behavior>` szerinti 3 eset.

    Egészítsd ki `src/features/projects/ProjectListView.test.tsx`-et EGY új tesztesettel: egy projekt-sor "Megnyitás" linkje a helyes `/projects/{id}` `href`-re mutat (`MemoryRouter`-be ágyazva rendereld a tesztet, ha eddig nem volt router-kontextus a tesztfájlban — ellenőrizd a meglévő teszt-setupot, és ha szükséges, csomagold a render-t egy `MemoryRouter`-be, mert a `Link` komponens router-kontextust igényel).
  </action>
  <acceptance_criteria>
    - `pnpm vitest run src/features/survey/SurveyView.test.tsx src/features/projects/ProjectListView.test.tsx` mind zöld
  </acceptance_criteria>
  <verify>
    <automated>pnpm run checkpoint</automated>
  </verify>
  <done>A teljes felmérési flow (betöltés, szerkesztés+újraszámolt autosave, fixGap cross-fül navigáció) integrációs teszttel bizonyított; a teljes projekt-checkpoint (typecheck+test+build) zöld.</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Type | Exports |
|---|---|---|
| `src/features/survey/SurveyView.tsx` | component | `SurveyView` |
| `src/main.tsx` (bővítve) | entry point | `getContentPort()`, `/projects/:id` route |
| `src/features/projects/ProjectListView.tsx` (bővítve) | component | "Megnyitás" link soronként |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| URL route-paraméter (`:id`) -> storage.get(id) | Egy nem-létező vagy manipulált id-re a UI ne omoljon össze, hanem kezelt hibaállapotot mutasson |
| Autosave újraszámolás -> StoragePort.put() | A klasszikus "derived state sosem tárolt nyersen" elv megsértése (pl. ha valaki elfelejtené újraszámolni mentés előtt) csendben elavult completion/score adatot perzisztálna |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-08-01 | Denial of Service (UI-szintű) | `/projects/:id` nem-létező id-re | medium | mitigate | Task 1 explicit "Betöltési hiba" állapot `storage.get(id) === null`-ra, nincs uncaught crash |
| T-02-08-02 | Tampering (adat-integritás) | autosave mentés playbook-paraméteres újraszámolás nélkül | high | mitigate | Task 3 explicit teszt bizonyítja, hogy `storage.put()` a MÓDOSÍTOTT `completion`-nel hívódik, nem a betöltéskori (elavult) értékkel |

</threat_model>

<verification>
- `pnpm run checkpoint` (typecheck + teljes teszt-suite + build) zöld a teljes Phase 2 integrációja után
- `pnpm vitest run src/features/survey/SurveyView.test.tsx src/features/projects/ProjectListView.test.tsx` mind zöld
</verification>

<success_criteria>
- A felhasználó a projektlistától a teljes guided felmérésen át a Cockpit döntési javaslatig végig tudja vinni a folyamatot, kérdésenkénti coaching-panellel, debounce-olt autosave-vel, playbook-súlyokból számolt readiness/decision score-ral
- SURVEY-01..07 és COACH-01..03 mind teljesülnek, AI és sync nélkül is teljes élményként
</success_criteria>

<output>
Create `.planning/phases/02-felm-r-si-flow-s-coaching/02-08-SUMMARY.md` when done
</output>
