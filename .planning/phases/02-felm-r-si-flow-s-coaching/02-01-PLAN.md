---
phase: 02-felm-r-si-flow-s-coaching
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-lock.yaml
  - postcss.config.cjs
  - src/main.tsx
  - src/test/setup.ts
autonomous: false
requirements: [SURVEY-02, SURVEY-05]
must_haves:
  truths:
    - "pnpm run checkpoint (typecheck + test + build) zöld a React 18->19 és a Mantine 9/RHF 7/Zustand 5 bevezetése után"
    - "MantineProvider egy explicit, 8-pontos spacing/typography kontraktusnak megfelelő createTheme()-mel csomagolja a meglévő React Router 7 gyökeret, nem a Mantine alap rem-skálájával"
    - "Mantine-komponenseket használó tesztek (Accordion, Modal, Tabs stb.) nem buknak el window.matchMedia hiánya miatt jsdom alatt"
  artifacts:
    - path: "postcss.config.cjs"
      provides: "postcss-preset-mantine + postcss-simple-vars konfiguráció a Mantine CSS-változók feloldásához Vite alatt"
    - path: "src/main.tsx"
      provides: "MantineProvider + createTheme(...) a RouterProvider körül"
    - path: "src/test/setup.ts"
      provides: "window.matchMedia mock Mantine-komponensek jsdom-teszteléséhez"
  key_links:
    - from: "src/main.tsx"
      to: "@mantine/core"
      via: "import { MantineProvider, createTheme } from '@mantine/core'; <MantineProvider theme={theme}>"
      pattern: "MantineProvider"
---

## Phase Goal

**As a** projektvezető/felmérő, **I want to** projektet létrehozni és egy egységes, guided interjú+checklist felületen végigmenni egy felmérésen kérdésenkénti coaching-panellel támogatva, **so that** gyorsan konkrét, development-ready projekt-igény szülessen, miközben junior->senior projektvezetői tudást építek.

(A ROADMAP.md "Cél:" sora leíró formában van, nem szó szerinti "As a/I want to/so that" mintában — a Phase 1 precedensét követve [01-01-PLAN.md] ez a plan lefordítja/szintetizálja a story-t a Cél-sorból és a PROJECT.md core value-jából, nem kér új `/gsd mvp-phase 2` futtatást, mert a projekt Hungarian "Cél:" konvenciója már tartalmazza mindhárom slotot.)

<objective>
Ez a plan vezeti be a React 18.3.1 -> 19.2.7 + Mantine 9.4.1 + React Hook Form 7.81 + Zustand 5.0.14 + use-debounce 10.1.1 stackváltást, amit a Phase 1 SKELETON.md explicit erre a fázisra halasztott ("a Mantine 9 kompatibilitás oka csak Phase 2-ben jelentkezik"). Ez az ELSŐ Wave-1 plan, mert minden további UI-plan (checklist kártyalista, coaching-panel, projekt-lista/create-modal, cockpit/döntés-panelek) Mantine-komponensekre és RHF-re épül.

A plan végén: az `npm`/`pnpm` csomagfa tartalmazza az új függőségeket, a `postcss.config.cjs` feloldja a Mantine CSS-változóit, a `main.tsx` egy 8-pontos spacing/typography kontraktusnak megfelelő Mantine-témával csomagolja a meglévő routert, és a teljes meglévő teszt-suite + build zöld marad az upgrade után.

Purpose: minden Phase 2 UI-plan (02-05..02-08) ezen a stacken épül; ha ez törik, minden downstream plan blokkolva van.
Output: frissített `package.json`/lockfile, `postcss.config.cjs`, `src/main.tsx` (MantineProvider+theme), `src/test/setup.ts` (matchMedia mock).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-felm-r-si-flow-s-coaching/02-CONTEXT.md
@.planning/phases/02-felm-r-si-flow-s-coaching/02-RESEARCH.md
@.planning/phases/02-felm-r-si-flow-s-coaching/02-UI-SPEC.md
@src/main.tsx
@src/test/setup.ts
@package.json
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Csomag-legitimitás ellenőrzés a SUS-jelölt csomagokra telepítés előtt</name>
  <files>package.json (ebben a taskban MÉG NEM módosul — csak ellenőrzés Task 2 pnpm add lépése előtt)</files>
  <what-built>Nincs kód még. `02-RESEARCH.md` "Package Legitimacy Audit" táblázata a `@mantine/core`, `@mantine/hooks` és `react-hook-form` csomagokat SUS-ként jelöli (a legitimacy-seam "too-new" jelzést adott a legutóbbi publish-dátumra, ami félrevezető lehet régóta létező, aktívan karbantartott csomagoknál) — a protokoll szerint ez blokkoló emberi jóváhagyást igényel telepítés előtt.</what-built>
  <action>Állítsd meg a végrehajtást Task 2 `pnpm add` lépése előtt, és kérj emberi jóváhagyást mind a 3 SUS-jelölt csomagra. Ne folytasd Task 2-vel, amíg a jóváhagyás meg nem érkezik.</action>
  <how-to-verify>
    Ellenőrizd az alábbi 3 csomagot az npmjs.com-on (csomagnév, kiadó/szervezet, heti letöltésszám, nincs friss gyanús tulajdonos-váltás):
    1. https://www.npmjs.com/package/@mantine/core (várt: `mantinedev` szervezet/repo, ~1.8M heti letöltés)
    2. https://www.npmjs.com/package/@mantine/hooks (várt: `mantinedev` szervezet/repo, ~2.1M heti letöltés)
    3. https://www.npmjs.com/package/react-hook-form (várt: `react-hook-form` szervezet/repo, ~55M heti letöltés)
    Ha mind a három legitimnek tűnik (nincs typosquatting-gyanú, a kiadó egyezik a `02-RESEARCH.md`-ben hivatkozottal), engedélyezd a telepítést. A többi csomag (`@hookform/resolvers`, `zustand`, `use-debounce`, `postcss-preset-mantine`, `postcss-simple-vars`, `react`/`react-dom` verzió-bump) a Package Legitimacy Audit szerint OK-verdiktű, nem igényel emberi jóváhagyást.
  </how-to-verify>
  <resume-signal>Írd be: "jóváhagyva" (vagy sorold fel, melyik csomaggal van probléma)</resume-signal>
  <verify>
    <automated>MISSING — human-verify checkpoint, a jóváhagyás maga a kapu, nincs automatizálható előfeltétel</automated>
  </verify>
  <done>A felhasználó jóváhagyta mind a 3 SUS-jelölt csomag legitimitását (vagy megnevezte a problémás csomagot, és a végrehajtás megállt a tisztázásig).</done>
</task>

<task type="auto">
  <name>Task 2: Csomagok telepítése + PostCSS-konfiguráció</name>
  <files>package.json, pnpm-lock.yaml, postcss.config.cjs</files>
  <read_first>
    .planning/phases/02-felm-r-si-flow-s-coaching/02-RESEARCH.md (Standard Stack táblázat — pontos verziók; "Mantine + Vite PostCSS setup" Code Example)
  </read_first>
  <action>
    Futtasd (Task 1 jóváhagyása UTÁN): `pnpm add react@19 react-dom@19 @mantine/core@9 @mantine/hooks@9 react-hook-form@7 @hookform/resolvers@5 zustand@5 use-debounce@10`. Ezután: `pnpm add -D @types/react@19 @types/react-dom@19 postcss-preset-mantine@1 postcss-simple-vars@7` — a `@types/react`/`@types/react-dom` verzió-bump NEM szerepel `02-RESEARCH.md` install-parancsában (az csak a runtime csomagokat sorolja), de technikailag kötelező: a `package.json` jelenleg `@types/react@^18.3.3`/`@types/react-dom@^18.3.0`-t pinnel, és a React 19 futásidejű csomaggal együtt ezeket is frissíteni KELL, különben a `tsc --noEmit` JSX/típushibákkal bukik.

    Hozd létre a repo gyökerében `postcss.config.cjs`-t PONTOSAN a `02-RESEARCH.md` "Mantine + Vite PostCSS setup" Code Example szerint (`postcss-preset-mantine` + `postcss-simple-vars` a Mantine breakpoint-változókkal). Ne módosítsd a `vite.config.ts`-t — a Vite automatikusan felismeri a gyökér `postcss.config.cjs`-t.
  </action>
  <verify>
    <automated>pnpm ls @mantine/core react-hook-form zustand use-debounce 2>&1 | grep -E "mantine|react-hook-form|zustand|use-debounce"</automated>
  </verify>
  <done>`package.json`/`pnpm-lock.yaml` tartalmazza az összes új függőséget a research szerinti főverzióval; `postcss.config.cjs` létezik és a Mantine hivatalos Vite-mintáját követi.</done>
</task>

<task type="auto">
  <name>Task 3: MantineProvider + téma bevezetése, matchMedia teszt-mock, teljes checkpoint</name>
  <files>src/main.tsx, src/test/setup.ts</files>
  <read_first>
    src/main.tsx (jelenlegi tartalom — a React Router 7 data-router mount, amit MantineProvider-rel kell körbevenni, NEM lecserélni),
    src/test/setup.ts (jelenlegi tartalom — ide kerül a matchMedia mock),
    .planning/phases/02-felm-r-si-flow-s-coaching/02-UI-SPEC.md ("Mantine téma-beállítás" szakasz — a PONTOS `createTheme()` hívás: fontFamily, primaryColor "brandTeal" 10-árnyalatú paletta, spacing xs-xl, fontSizes xs-xl, defaultRadius "8px")
  </read_first>
  <action>
    Módosítsd `src/main.tsx`-et: importáld `"@mantine/core/styles.css"`-t (a `"./styles.css"` import ELŐTT, hogy a projekt saját CSS-e felülírhassa a Mantine defaultokat, ha szükséges), importáld `MantineProvider`-t és `createTheme`-et a `@mantine/core`-ból. Hozd létre a `theme` konstanst PONTOSAN a `02-UI-SPEC.md` "Mantine téma-beállítás" kódrészlete szerint (fontFamily Inter-lánc, primaryColor: "brandTeal", colors.brandTeal 10-elemű tömb, spacing xs:4px/sm:8px/md:16px/lg:24px/xl:32px, fontSizes xs:14px/sm:14px/md:16px/lg:20px/xl:28px, defaultRadius: "8px"). Csomagold a meglévő `<RouterProvider router={router} />`-t `<MantineProvider theme={theme}>`-be — a router-konfiguráció (createBrowserRouter, az egyetlen "/" route) NEM változik ebben a taskban.

    Egészítsd ki `src/test/setup.ts`-t egy `window.matchMedia` mockkal a meglévő `scrollIntoView` mock ALÁ (Mantine belső hookjai — pl. `use-media-query`, `MantineProvider` szín-séma detektálás — `window.matchMedia`-t hívnak, ami jsdom-ban alapból nem implementált; enélkül minden Mantine-komponenst renderelő teszt `TypeError: window.matchMedia is not a function` hibával bukna a Wave 2 planekben). A mock egy `Object.defineProperty(window, "matchMedia", { value: (query) => ({ matches: false, media: query, onchange: null, addListener: () => {}, removeListener: () => {}, addEventListener: () => {}, removeEventListener: () => {}, dispatchEvent: () => false }) })` mintát kövessen (vagy ezzel funkcionálisan egyenértékű `vi.fn()`-alapú megoldást), mindig `matches: false`-t adva vissza (nincs sötét mód / reduced-motion-specifikus tesztigény ebben a fázisban).

    Futtasd a teljes checkpointot: `pnpm run checkpoint` (typecheck + a MEGLÉVŐ teljes teszt-suite + build) — ennek zöldnek kell maradnia a React 18->19 váltás után is, mielőtt bármelyik Wave-2 plan elindulna. Ha a React 19 upgrade bármelyik MEGLÉVŐ tesztet töri (pl. `act()` figyelmeztetések, StrictMode kettős-render különbségek), javítsd a törött tesztet MINIMÁLISAN (ne bővítsd a scope-ot funkcionális változtatással) — ez a plan kizárólag az upgrade-et szállítja, nem új funkciót.
  </action>
  <verify>
    <automated>pnpm run checkpoint</automated>
  </verify>
  <done>`pnpm run checkpoint` (typecheck+test+build) zöld; `src/main.tsx` MantineProvider+createTheme-mel csomagolja a routert; `src/test/setup.ts` matchMedia-t mockol.</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Type | Exports |
|---|---|---|
| `postcss.config.cjs` | build config | (nincs JS export — PostCSS plugin-lista) |
| `src/main.tsx` (bővítve) | entry point | `MantineProvider`/`createTheme` bevezetve a meglévő router köré |
| `src/test/setup.ts` (bővítve) | test setup | `window.matchMedia` mock |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> build pipeline | Új harmadik-fél csomagok (react@19, @mantine/core, @mantine/hooks, react-hook-form, @hookform/resolvers, zustand, use-debounce, postcss-preset-mantine, postcss-simple-vars) kerülnek a supply chain-be |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01-SC | Tampering | npm install (@mantine/core, @mantine/hooks, react-hook-form — SUS-jelölt) | high | mitigate | Task 1 blocking human-verify checkpoint: npmjs.com-on ellenőrzött csomagnév/kiadó/letöltésszám a telepítés ELŐTT |
| T-02-01-01 | Tampering | npm install (@hookform/resolvers, zustand, use-debounce, postcss-preset-mantine, postcss-simple-vars — OK-verdiktű) | low | accept | `02-RESEARCH.md` Package Legitimacy Audit már ellenőrizte ezeket (ismert szervezet, magas letöltésszám); nincs friss "too-new" jelzés rájuk |

</threat_model>

<verification>
- `pnpm run checkpoint` (typecheck + test + build) hiba nélkül lefut a React 19 + Mantine 9 bevezetése után
- `postcss.config.cjs` létezik és a `postcss-preset-mantine`/`postcss-simple-vars` pluginokat tartalmazza
- `src/main.tsx` importálja a `MantineProvider`/`createTheme`-et és becsomagolja a `RouterProvider`-t
</verification>

<success_criteria>
- A teljes Mantine 9 + React 19 + RHF 7 + Zustand 5 stack telepítve és bekötve a meglévő build-láncba, a meglévő teszt-suite törése nélkül
- Minden Wave 2 UI-plan (02-05..02-08) építhet erre a stackre
</success_criteria>

<output>
Create `.planning/phases/02-felm-r-si-flow-s-coaching/02-01-SUMMARY.md` when done
</output>
