---
phase: 02-felm-r-si-flow-s-coaching
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/domain/model/types.ts
  - src/domain/model/schema.ts
  - src/domain/model/factory.ts
  - src/domain/model/envelope.ts
  - src/adapters/storage/indexeddb/db.ts
  - src/adapters/storage/indexeddb/StorageAdapter.test.ts
  - src/adapters/storage/indexeddb/backup.test.ts
  - src/adapters/migration/legacyImport.ts
  - src/features/projects/ProjectListView.tsx
autonomous: true
requirements: [SURVEY-04]
must_haves:
  truths:
    - "Project.playbookId kötelező, nem-üres string mező; a factory nem ad rá csendes alapértéket"
    - "Egy Phase-1-ben (playbookId nélkül) mentett RxDB dokumentum get()/list() hívása playbookId: \"general\" értékkel tér vissza, Zod-hiba nélkül"
    - "A legacy MIG-01 import (Tauri-export sorok, amik sosem ismerték a playbookId fogalmát) továbbra is sikeresen importál, playbookId: \"general\" backfill-lel"
  artifacts:
    - path: "src/domain/model/envelope.ts"
      provides: "CURRENT_APP_SCHEMA_VERSION = 2 (1-ről emelve)"
    - path: "src/adapters/storage/indexeddb/db.ts"
      provides: "projectEnvelopeSchema.version = 1 + migrationStrategies[1] (playbookId backfill)"
    - path: "src/domain/model/factory.ts"
      provides: "createEmptyProject(playbookId: string, overrides?: Partial<Project>): Project"
  key_links:
    - from: "src/adapters/migration/legacyImport.ts"
      to: "src/domain/model/schema.ts"
      via: "ProjectSchema.safeParse(parsed) EGY playbookId: \"general\" backfill UTÁN, nem előtte"
      pattern: "playbookId.*general"
    - from: "src/adapters/storage/indexeddb/db.ts"
      to: "src/adapters/storage/indexeddb/StorageAdapter.test.ts"
      via: "migrationStrategies[1] tesztelve egy v0-sémájú dokumentumon"
      pattern: "migrationStrategies"
---

<objective>
Ez a plan vezeti be a `Project.playbookId` mezőt (SURVEY-04 adat-alapja, D-02/D-03) — ez a projekt ELSŐ valódi, nem-üres séma-migrációja: a domain-szintű `CURRENT_APP_SCHEMA_VERSION` 1-ről 2-re emelkedik, és az RxDB `projects` kollekció saját belső `version`-je 0-ról 1-re, `migrationStrategies[1]`-gyel, ami minden meglévő (Phase 1-ben létrehozott) dokumentumra `playbookId: "general"`-t ír be. Emellett a legacy MIG-01 import útvonal (`legacyImport.ts`) is korrekciót kap: mivel `ProjectSchema` mostantól KÖTELEZŐVÉ teszi a `playbookId`-t, a Tauri-MVP-ből importált sorok (amik sosem ismerték ezt a fogalmát) `playbookId: "general"` backfill NÉLKÜL az ÖSSZES eddig érvényesnek számító legacy sor hirtelen érvénytelenné válna — ezt a plan explicit megelőzi.

Ez a séma-migráció ugyanazt a "hozzáadott kötelező mező, régi adat nem ismeri" mintát oldja meg KÉT KÜLÖNBÖZŐ belépési ponton (RxDB `migrationStrategies` a már perzisztált Phase-1 adatra, és egy kézi Zod-elé-illesztett backfill a legacy MIG-01 import JSON-blobjaira) — mindkettő ugyanazt a "general" alapértéket alkalmazza (D-01 szerint ez az egyetlen létező playbook), de a mechanizmus eltér, mert a két adatforrás eltérő útvonalon lép be a rendszerbe.

Purpose: playbookId nélkül SURVEY-04 (playbook-választás) nem perzisztálható, és a domain/scoring (02-03) sem tudná playbook-paraméterezetten kiszámolni a pontszámokat egy adott projektre.
Output: bővített domain-model (types/schema/factory/envelope), RxDB migráció (db.ts), javított legacyImport.ts, frissített meglévő tesztek.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-felm-r-si-flow-s-coaching/02-CONTEXT.md
@.planning/phases/02-felm-r-si-flow-s-coaching/02-RESEARCH.md
@.planning/phases/02-felm-r-si-flow-s-coaching/02-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: playbookId mező hozzáadása a domain-modellhez (types/schema/factory/envelope) + meglévő hívók frissítése</name>
  <files>src/domain/model/types.ts, src/domain/model/schema.ts, src/domain/model/factory.ts, src/domain/model/envelope.ts, src/adapters/storage/indexeddb/StorageAdapter.test.ts, src/adapters/storage/indexeddb/backup.test.ts, src/features/projects/ProjectListView.tsx</files>
  <read_first>
    src/domain/model/types.ts (a teljes jelenlegi `Project` interfész — ide kerül az új mező `archivedAt` mellé),
    src/domain/model/schema.ts (a teljes jelenlegi `ProjectSchema` — ide kerül a Zod-mező),
    src/domain/model/factory.ts (a teljes jelenlegi `createEmptyProject(overrides?)` — a szignatúra megváltozik),
    src/domain/model/envelope.ts (`CURRENT_APP_SCHEMA_VERSION` jelenlegi értéke és a rá vonatkozó doc-comment),
    .planning/phases/02-felm-r-si-flow-s-coaching/02-PATTERNS.md ("domain/model/types.ts / schema.ts / factory.ts (playbookId field addition)" szakasz — pontos kódminta),
    .planning/phases/02-felm-r-si-flow-s-coaching/02-RESEARCH.md ("Pitfall 1: playbookId bevezetése séma-migrációt igényel" — miért KÖTELEZŐ paraméter, nem opcionális alapérték)
  </read_first>
  <action>
    `src/domain/model/types.ts`: add hozzá a `Project` interfészhez a `playbookId: string;` mezőt (az `archivedAt` mező mellé, bármelyik oldalra — a mezősorrend nem szerződés-kritikus).

    `src/domain/model/schema.ts`: add hozzá a `ProjectSchema`-hoz a `playbookId: z.string().min(1)` mezőt.

    `src/domain/model/envelope.ts`: emeld `CURRENT_APP_SCHEMA_VERSION`-t `1`-ről `2`-re, és egészítsd ki a doc-commentet: ez a domain-szintű séma-verzió MOST lép először nem-üres migrációt (a `playbookId` bevezetése miatt) — FÜGGETLEN az RxDB kollekció saját `version`-jétől (azt Task 2 emeli 0-ról 1-re).

    `src/domain/model/factory.ts`: változtasd a `createEmptyProject` szignatúráját `createEmptyProject(playbookId: string, overrides?: Partial<Project>): Project`-re — a `playbookId` KÖTELEZŐ, ELSŐ pozicionális paraméter, NEM az `overrides` része és NEM kap csendes alapértéket (D-03 "véglegesség" elve: a hívónak explicit döntenie kell, melyik playbookot választja, még akkor is, ha ma csak egy playbook létezik). A `base` objektum-literálban vedd fel `playbookId`-t a kapott paraméterből.

    Frissítsd a 3 MEGLÉVŐ hívási helyet, amik jelenleg `createEmptyProject({ ... })`-ot hívnak egyetlen `overrides`-objektummal — mindegyiket `createEmptyProject("general", { ... })`-re kell módosítani (a literál `"general"` string itt SZÁNDÉKOSAN egy átmeneti, ideiglenes érték — a `src/features/projects/ProjectListView.tsx`-beli hívást a 02-05-PLAN.md teljesen lecseréli egy valódi playbook-választó create-flow-ra; ADDIG ez a literál tartja typecheck-elhetőnek és futtathatónak a meglévő "Új teszt-projekt" gombot):
    - `src/features/projects/ProjectListView.tsx` (`handleAddTestProject` — `createEmptyProject({ id, name: ... })` -> `createEmptyProject("general", { id, name: ... })`)
    - `src/adapters/storage/indexeddb/StorageAdapter.test.ts` (`buildEnvelope()` helper — `createEmptyProject({ id, name: "Teszt projekt" })` -> `createEmptyProject("general", { id, name: "Teszt projekt" })`)
    - `src/adapters/storage/indexeddb/backup.test.ts` (ugyanez a minta — nézd meg a fájlt, azonos `createEmptyProject(...)` hívás javítandó)
  </action>
  <acceptance_criteria>
    - `grep -c "playbookId: string" src/domain/model/types.ts` legalább 1
    - `grep -c "playbookId: z.string" src/domain/model/schema.ts` legalább 1
    - `grep -c "CURRENT_APP_SCHEMA_VERSION = 2" src/domain/model/envelope.ts` egyenlő 1
    - `grep -c "createEmptyProject(playbookId: string" src/domain/model/factory.ts` egyenlő 1
    - `grep -c 'createEmptyProject("general"' src/features/projects/ProjectListView.tsx src/adapters/storage/indexeddb/StorageAdapter.test.ts src/adapters/storage/indexeddb/backup.test.ts` összesen legalább 3 találatot ad
  </acceptance_criteria>
  <verify>
    <automated>pnpm exec tsc --noEmit 2>&1 | grep -E "domain/model|ProjectListView|StorageAdapter.test|backup.test" ; echo "exit:$?"</automated>
  </verify>
  <done>Az 5 domain-fájl bővítve, a 3 meglévő hívási hely frissítve; `tsc --noEmit` az érintett fájlokra hiba nélkül fordul.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RxDB séma-verzió bump + migrationStrategies[1] (playbookId backfill) valós dokumentumon</name>
  <files>src/adapters/storage/indexeddb/db.ts, src/adapters/storage/indexeddb/StorageAdapter.test.ts</files>
  <behavior>
    - Egy v0-sémával (playbookId NÉLKÜL) beszúrt dokumentum, majd a v1-sémájú `createProjectDatabase()` újranyitása után, `get()`-tel visszaolvasva `playbookId: "general"`-t ad vissza
    - Az összes többi mező (name, checklistAnswers, stb.) változatlan marad a migráció után
    - `collection.migrationNeeded()` `false`, miután a migráció lefutott
  </behavior>
  <read_first>
    src/adapters/storage/indexeddb/db.ts (a teljes jelenlegi `projectEnvelopeSchema` + `createProjectDatabase` — `version: 0`, `migrationStrategies: {}`),
    src/adapters/storage/indexeddb/db.test.ts (a MEGLÉVŐ szintetikus migrációs bizonyíték — `addRxPlugin(RxDBMigrationSchemaPlugin)`, két `createRxDatabase()` hívás ugyanazzal a névvel, `getRxStorageMemory()` felett — ugyanezt a két-adatbázis-példány mintát kell alkalmazni ITT is, a PRODUKCIÓS `projectEnvelopeSchema`-ra, nem egy szintetikus sémára),
    .planning/phases/02-felm-r-si-flow-s-coaching/02-RESEARCH.md ("RxDB migrationStrategies — playbookId bevezetése (Pitfall 1 megoldása)" Code Example — a PONTOS migrationStrategies[1] függvénytest)
  </read_first>
  <action>
    `src/adapters/storage/indexeddb/db.ts`: emeld `projectEnvelopeSchema.version`-t `0`-ról `1`-re. Add hozzá `migrationStrategies: { 1: (oldDoc) => ({ ...oldDoc, data: { ...(oldDoc.data as Record<string, unknown>), playbookId: "general" } }) }`-t a `createProjectDatabase()`-ban az `addCollections()` hívásnak átadott konfigurációhoz (a jelenlegi `migrationStrategies: {}` helyett). Az `RxDBMigrationSchemaPlugin`-t (`rxdb/plugins/migration-schema`) ehhez a PRODUKCIÓS adatbázis-megnyitáshoz is regisztrálni kell (`addRxPlugin(RxDBMigrationSchemaPlugin)` a `db.ts` tetején, ha még nincs — a `db.test.ts` már regisztrálja a SAJÁT szintetikus tesztjéhez, de az egy külön modul-scope, a `db.ts`-nek saját maga is regisztrálnia kell, ha a produkciós migráció ténylegesen le akar futni böngészőben/valós használat közben). Frissítsd a doc-commentet: a `deletedAt`-ra vonatkozó `additionalProperties: false` figyelmeztetés MELLÉ írd oda, hogy ugyanez a kockázat minden ÚJ mezőnél fennáll — de a `data` mező maga itt VÁLTOZATLANUL `type: "object"` marad (nincs mezőnkénti JSON-séma-deklaráció a `data` alatt, mert a Zod a validáció-hordozó, nem az RxDB séma — lásd a fájl eredeti doc-commentjét), ezért a `playbookId` mezőt NEM kell felvenni a `projectEnvelopeSchema.properties.data`-ba (az `data` mező RxDB-szinten öntudatlan a saját belső alakjáról).

    `src/adapters/storage/indexeddb/StorageAdapter.test.ts`: adj hozzá egy ÚJ `describe`/`it` blokkot (a meglévő `describe("RxdbStorageAdapter...")` mellé, vagy azon belül, ahogy a fájl jelenlegi szerkezete engedi), ami a `db.test.ts`-ben már bizonyított két-adatbázis-példány mintát követi, DE a produkciós `createProjectDatabase()`-t hívja (nem szintetikus sémát): (1) nyiss egy adatbázist a MOSTANI (v1) `createProjectDatabase(getRxStorageMemory())`-vel, szúrj be egy v0-alakú dokumentumot KÖZVETLENÜL a kollekcióba (`collection.insert({...})`, playbookId NÉLKÜL, a többi kötelező mezővel) — VIGYÁZAT: mivel a `db.ts`-ben lévő éles séma MÁR v1 (`migrationStrategies[1]`-gyel), ehhez a teszthez vagy (a) egy ideiglenes, KÜLÖN v0-sémájú `createRxDatabase`+`addCollections` hívást kell építened UGYANAZZAL a `projectEnvelopeSchema`-alappal de `version: 0`/`migrationStrategies: {}`-vel (a `db.test.ts` mintájára, de a PRODUKCIÓS séma mezőire), majd (b) egy MÁSODIK, a valódi `createProjectDatabase()`-t hívó megnyitással (ami a v1 sémát + a migrationStrategies[1]-et hordozza) ugyanazon az adatbázis-néven, és onnan `get()`-teld vissza a dokumentumot az adapteren keresztül. Asszertáld: `result.data.playbookId === "general"`, és hogy a többi mező (pl. `result.data.name`) változatlan maradt. Kövesd a `db.test.ts` `afterEach`-mintáját (`db.remove()`) a teszt-izolációhoz, hogy ez a teszt ne ütközzön a fájl többi (v1-only) tesztjével, amik friss `db.remove()`-ot várnak minden `it()` előtt/után.
  </action>
  <acceptance_criteria>
    - `grep -c "version: 1" src/adapters/storage/indexeddb/db.ts` legalább 1 (a `projectEnvelopeSchema.version`)
    - `grep -c "migrationStrategies: { 1:" src/adapters/storage/indexeddb/db.ts` legalább 1 (nem üres migrationStrategies többé)
    - Az új migrációs teszt zöld: `pnpm vitest run src/adapters/storage/indexeddb/StorageAdapter.test.ts`
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run src/adapters/storage/indexeddb/StorageAdapter.test.ts src/adapters/storage/indexeddb/db.test.ts</automated>
  </verify>
  <done>Egy v0-alakú (playbookId nélküli) dokumentum a produkciós migráción átfuttatva `playbookId: "general"`-t kap; a meglévő szintetikus `db.test.ts` továbbra is zöld.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Legacy MIG-01 import playbookId-backfill (a szigorúbb ProjectSchema ne utasítson el minden legacy sort)</name>
  <files>src/adapters/migration/legacyImport.ts</files>
  <behavior>
    - Egy legacy JSON-blob sor, ami korábban érvényes volt (name kitöltve, playbookId fogalma nélkül), Task 1 UTÁN is sikeresen importálódik — playbookId: "general" backfill-lel, NEM válik hirtelen "invalid"-dá
    - A meglévő, name-hiányzó invalid fixture-sor TOVÁBBRA is skippedInvalid marad (a backfill nem menti meg a ténylegesen hibás sorokat)
    - A meglévő 5 legacyImport teszt (legacyImport.test.ts + legacyImport.integration.test.ts) VÁLTOZATLANUL zöld marad, ugyanazokkal a számokkal (imported=3, skippedInvalid=1)
  </behavior>
  <read_first>
    src/adapters/migration/legacyImport.ts (a teljes jelenlegi `importLegacyExport` — a `JSON.parse(row.data)` -> `ProjectSchema.safeParse(parsed)` sorrend, ami MOST törne, mert a `parsed` objektum sosem tartalmaz `playbookId`-t),
    src/adapters/migration/legacyImport.test.ts + src/test/fixtures/legacy-export-fixture.json (a meglévő fixture-elvárások: imported=3, skippedInvalid=1 — ezek a számok NEM változhatnak)
  </read_first>
  <action>
    `src/adapters/migration/legacyImport.ts`: a `JSON.parse(row.data)` UTÁN, de a `ProjectSchema.safeParse(parsed)` ELŐTT, told be a `playbookId` backfill-t, HA a parsolt objektum egy plain object és MÉG NEM hordoz `playbookId`-t: `const withPlaybookId = typeof parsed === "object" && parsed !== null && !("playbookId" in parsed) ? { ...parsed, playbookId: "general" } : parsed;` — majd a validációt `ProjectSchema.safeParse(withPlaybookId)`-ra futtasd (nem a nyers `parsed`-re). Ez UGYANAZT a "general" alapértéket alkalmazza, mint a Task 2 RxDB-migrációja, DE egy másik belépési ponton (egy kézzel importált, sosem RxDB-n átment JSON-blobra, nem egy már perzisztált RxDB-dokumentumra) — kommentben jelezd ezt a párhuzamosságot és hogy MIÉRT nem elég csak az egyik útvonalat javítani (a legacy import és az RxDB-migráció két KÜLÖNBÖZŐ adatbelépési pont, mindkettő ugyanazt a hiányzó-kötelező-mező problémát futtatja bele, ha csak az egyiket javítanánk).

    NE változtasd meg a `skippedInvalid`/`imported`/`skippedExisting` számítási logikát máshol — a backfill KIZÁRÓLAG a `playbookId` hiányát pótolja, egy ténylegesen hibás sor (pl. hiányzó `name`) TOVÁBBRA is `skippedInvalid`-ként landol, mert a többi Zod-validáció változatlanul fut a backfillelt objektumon.
  </action>
  <acceptance_criteria>
    - `grep -c 'playbookId: "general"' src/adapters/migration/legacyImport.ts` legalább 1
    - A meglévő `legacyImport.test.ts` mind az 5 tesztesete zöld, UGYANAZOKKAL a számokkal (imported=3, skippedInvalid=1, skippedExisting a második futáskor 3)
    - A meglévő `legacyImport.integration.test.ts` egyetlen tesztesete zöld
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run src/adapters/migration/legacyImport.test.ts src/adapters/migration/legacyImport.integration.test.ts</automated>
  </verify>
  <done>A legacy import útvonal a szigorúbb (playbookId-kötelező) ProjectSchema mellett is ugyanazt a 3 sort importálja sikeresen, mint a schema-szigorítás előtt.</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Type | Exports |
|---|---|---|
| `src/domain/model/types.ts` (bővítve) | type module | `Project.playbookId: string` |
| `src/domain/model/schema.ts` (bővítve) | Zod schema | `ProjectSchema.playbookId: z.string().min(1)` |
| `src/domain/model/factory.ts` (módosítva) | pure factory | `createEmptyProject(playbookId: string, overrides?: Partial<Project>): Project` |
| `src/domain/model/envelope.ts` (módosítva) | config/model | `CURRENT_APP_SCHEMA_VERSION = 2` |
| `src/adapters/storage/indexeddb/db.ts` (módosítva) | RxDB config | `projectEnvelopeSchema.version = 1`, `migrationStrategies[1]` (playbookId backfill) |
| `src/adapters/migration/legacyImport.ts` (módosítva) | migration logic | playbookId backfill a Zod-validáció előtt |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RxDB migrationStrategies -> perzisztált Phase-1 adat | Egy hibás migrációs lépés csendben korrumpálhatja/elveszítheti a meglévő projekteket |
| Legacy Tauri-export JSON-blob -> ProjectSchema.safeParse | Egy hibás backfill vagy a backfill hiánya hirtelen minden korábban érvényes legacy sort érvénytelenné tehet (regresszió a már működő MIG-01-en) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-02-01 | Tampering (adat-integritás) | `migrationStrategies[1]` (db.ts) | high | mitigate | Task 2 tdd="true": valós, produkciós sémán futtatott migrációs teszt bizonyítja a helyes backfillt és a többi mező érintetlenségét, mielőtt bármely Wave-2 plan a playbookId mezőre épülne |
| T-02-02-02 | Tampering (adat-integritás / regresszió) | `legacyImport.ts` playbookId backfill | high | mitigate | Task 3 tdd="true": a meglévő 5 legacyImport teszt UGYANAZOKKAL a számokkal kell maradjon zöld — egy regresszió itt a MÁR MŰKÖDŐ MIG-01-et törné néma silent-data-loss-szal |

</threat_model>

<verification>
- `pnpm exec tsc --noEmit` hiba nélkül lefut az érintett domain/adapters fájlokra
- `pnpm vitest run src/adapters/storage/indexeddb src/adapters/migration` mind zöld
- `pnpm run checkpoint` (a teljes suite, beleértve a Plan 01-ben már frissített React/Mantine stacket) zöld marad
</verification>

<success_criteria>
- `Project.playbookId` kötelező mezőként létezik, séma-migrációval a meglévő adatra
- A legacy MIG-01 import útvonal nem regresszált a szigorúbb séma mellett
</success_criteria>

<output>
Create `.planning/phases/02-felm-r-si-flow-s-coaching/02-02-SUMMARY.md` when done
</output>
