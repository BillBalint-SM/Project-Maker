---
phase: 02-felm-r-si-flow-s-coaching
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/content/playbook/types.ts
  - src/content/playbook/general.ts
  - src/content/playbook/index.ts
  - src/content/playbook/general.test.ts
  - src/domain/scoring/completion.ts
  - src/domain/scoring/readiness.ts
  - src/domain/scoring/decisionScore.ts
  - src/domain/scoring/gaps.ts
  - src/domain/scoring/followups.ts
  - src/domain/scoring/scoring.test.ts
autonomous: true
requirements: [SURVEY-04, SURVEY-06, SURVEY-07]
must_haves:
  truths:
    - "Az \"Általános\" playbook (id: \"general\") 30 tételt hordoz, 1:1 tartalmi egyezésben a legacy checklistTemplate-tel (hint mező NÉLKÜL)"
    - "calculateReadinessPercent/calculateDecisionScore/calculateCompletion/collectReadinessGaps playbook-paraméteresek — a képlet-struktúra változatlan, csak a súlyok jönnek playbook.weights-ből"
    - "Egy playbook-paraméteres calculateCompletion() ugyanazt az eredményt adja az \"general\" playbookkal, mint a legacy hardcoded verzió ugyanarra a bemenetre"
  artifacts:
    - path: "src/content/playbook/types.ts"
      provides: "Playbook, PlaybookItem, PlaybookWeights típusok"
    - path: "src/content/playbook/general.ts"
      provides: "playbook \"Általános\" konstans (30 tétel)"
    - path: "src/content/playbook/index.ts"
      provides: "playbooks: Record<string, Playbook> katalógus"
    - path: "src/domain/scoring/completion.ts"
      provides: "calculateCompletion(project, playbook): ProjectCompletion"
  key_links:
    - from: "src/domain/scoring/completion.ts"
      to: "src/content/playbook/types.ts"
      via: "playbook: Playbook paraméter, NEM globális checklistTemplate import"
      pattern: "playbook: Playbook"
---

<objective>
Ez a plan szállítja SURVEY-04 (playbook-választás) adatmodelljét és SURVEY-06/07 (haladás-jelző, readiness/döntési pontszám) motorját: a `content/playbook/` statikus katalógust (D-01/D-02 szerint egyetlen "Általános" playbook, saját tétel-listával ÉS saját súly-konfigurációval) és a `domain/scoring/` playbook-paraméteres pure függvénykészletet, ami a legacy `src/lib/project.ts` KÉPLETÉT viszi tovább változatlanul (D-04), csak a hardcoded súlyokat cseréli `playbook.weights.*`-ra és a globális `checklistTemplate` importot `playbook.items`-re.

Ez egy tisztán domain-réteg plan — nincs UI, nincs perzisztencia-változás. A `Playbook`/`PlaybookItem`/`PlaybookWeights` típusok itt születnek meg, ezekre épül minden Wave-2 UI-komponens (CreateProjectModal playbook-select, ChecklistCard tétel-render, CockpitPanel/DecisionPanel score-render).

Purpose: enélkül SURVEY-04 nem tud playbook-adatot felkínálni, és a readiness/decision score sosem tudna playbook-súlyokból számolni.
Output: `src/content/playbook/*`, `src/domain/scoring/*`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-felm-r-si-flow-s-coaching/02-CONTEXT.md
@.planning/phases/02-felm-r-si-flow-s-coaching/02-RESEARCH.md
@.planning/phases/02-felm-r-si-flow-s-coaching/02-PATTERNS.md
@src/data/checklist.ts
@src/lib/project.ts
</context>

<!-- planner-discipline-allow: checklistTemplate -->

<tasks>

<task type="auto">
  <name>Task 1: content/playbook — Playbook/PlaybookItem/PlaybookWeights típusok + "Általános" playbook + katalógus</name>
  <files>src/content/playbook/types.ts, src/content/playbook/general.ts, src/content/playbook/index.ts, src/content/playbook/general.test.ts</files>
  <read_first>
    src/data/checklist.ts (a TELJES `checklistTemplate` — mind a 30 tétel `id`/`category`/`controlPoint`/`exampleQuestion`/`requiredForMvp`/`requiredForEstimate`/`blockingIfMissing` mezőkkel — ez a forrás-tartalom az "Általános" playbookhoz, a `hint` mező KIVÉTELÉVEL, ami a coaching-content-be költözik 02-04-ben),
    src/data/types.ts (`ChecklistTemplateItem` interfész alakja — referencia a `PlaybookItem` mezőihez),
    src/lib/project.ts (a hardcoded súly-értékek: 0.2/0.2/0.15/0.3/0.15 a readiness-hez, 0.25/0.15/0.15/0.15/0.1/0.1/0.1 a decision score-hoz — ezek adják a `PlaybookWeights` alapértékeit az "Általános" playbookban),
    .planning/phases/02-felm-r-si-flow-s-coaching/02-RESEARCH.md ("Pattern 2: Playbook adatmodell" — a PONTOS `PlaybookItem`/`PlaybookWeights`/`Playbook` interfész-kód),
    .planning/phases/02-felm-r-si-flow-s-coaching/02-CONTEXT.md (D-01/D-02/D-04 — miért saját tétel-lista+súly-konfig playbookonként, miért marad a képlet-struktúra; D-14 — a `general.items` tömb sorrendje KÖVESSE a jelenlegi kategória-sorrendet (Üzleti cél elsőként, Dokumentáció utolsóként), mert a Wave-2 ChecklistCard-lista ebben a tömb-sorrendben renderel, szabad navigációval, nem kényszerített lépés-lánccal)
  </read_first>
  <action>
    Hozd létre `src/content/playbook/types.ts`-t PONTOSAN a `02-RESEARCH.md` "Pattern 2" kódja szerint: exportáld a `PlaybookItem` interfészt (`id: number`, `category: string`, `controlPoint: string`, `exampleQuestion: string`, `requiredForMvp: boolean`, `requiredForEstimate: boolean`, `blockingIfMissing: boolean` — a `hint` mező NEM kerül ide, D-11 szerint az a coaching-content-be költözik), a `PlaybookWeights` interfészt (a 12 súly-mező: `baseInfo`, `business`, `ownership`, `checklist`, `followUpResolution`, `businessValue`, `strategicAlignment`, `urgency`, `confidence`, `complexity`, `risk`, `readiness`), és a `Playbook` interfészt (`id: string`, `name: string`, `version: number`, `items: PlaybookItem[]`, `weights: PlaybookWeights`). Adj hozzá egy Zod-sémát is (`PlaybookSchema`) ugyanitt vagy egy melléksémafájlban — ez a Security Domain V5 (input validation) elvárása statikus tartalomra is (lásd `02-RESEARCH.md` "Known Threat Patterns" — hiányos/hibás statikus adat build-időben észrevétlen maradhat).

    Írd meg a `content/playbook/types.ts`-ben (vagy egy rövid kommentben a `PlaybookItem` mellett) a Pitfall 4 dokumentációs figyelmeztetést: egy jövőbeli 2. playbook (Deferred, D-01) `id`-jai NEM ütközhetnek ezzel a playbookkal, mert a `Project.checklistAnswers: Record<number, ChecklistAnswer>` GLOBÁLISAN kulcsol, nem playbook-scope-oltan — ez Phase 2-ben nem okoz hibát (csak 1 playbook létezik), de egy jövőbeli playbooknak vagy diszjunkt numerikus tartományt kell kapnia, vagy a `checklistAnswers` kulcsot kell playbook-scope-olt string-re váltani egy későbbi migrációval.

    Hozd létre `src/content/playbook/general.ts`-t: exportáld a `general: Playbook` konstanst `id: "general"`, `name: "Általános"`, `version: 1`, `items`: a `checklistTemplate` mind a 30 elemének 1:1 tartalmi másolata (`hint` mező NÉLKÜL), `weights`: a legacy hardcoded értékek (`baseInfo: 0.2, business: 0.2, ownership: 0.15, checklist: 0.3, followUpResolution: 0.15, businessValue: 0.25, strategicAlignment: 0.15, urgency: 0.15, confidence: 0.15, complexity: 0.1, risk: 0.1, readiness: 0.1`).

    Hozd létre `src/content/playbook/index.ts`-t: exportáld a `playbooks: Record<string, Playbook> = { general }` katalógust (jelenleg egyetlen bejegyzéssel — D-01 szerint ez a szándékosan bővíthető alak jövőbeli playbookokhoz).

    Hozd létre `src/content/playbook/general.test.ts`-t: (1) `PlaybookSchema.parse(general)` nem dob hibát; (2) `general.items.length === 30`; (3) `general.items.map(i => i.id)` pontosan `[1..30]` (nincs duplikáció, nincs hiányzó id); (4) `general.items` EGYIK eleme sem hordoz `hint` kulcsot (`"hint" in item` === false minden elemre) — ez a D-11 elkülönítés smoke-tesztje.
  </action>
  <acceptance_criteria>
    - `grep -c "interface PlaybookItem" src/content/playbook/types.ts` egyenlő 1
    - `grep -c "interface PlaybookWeights" src/content/playbook/types.ts` egyenlő 1
    - `grep -c "hint" src/content/playbook/general.ts` egyenlő 0 (a hint mező tudatosan KIMARADT)
    - `pnpm vitest run src/content/playbook/general.test.ts` zöld, mind a 4 esettel
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run src/content/playbook/general.test.ts</automated>
  </verify>
  <done>A "general" playbook 30 tétellel, hint nélkül, a legacy súlyokkal létezik, Zod-validáltan és teszttel bizonyítva.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: domain/scoring — playbook-paraméteres readiness/decision/completion/gaps/followups</name>
  <files>src/domain/scoring/completion.ts, src/domain/scoring/readiness.ts, src/domain/scoring/decisionScore.ts, src/domain/scoring/gaps.ts, src/domain/scoring/followups.ts, src/domain/scoring/scoring.test.ts</files>
  <behavior>
    - `calculateCompletion(project, playbook)` egy teljesen kitöltött ("Kész" válaszú mind a 30 tételre) projektre 100%-os `percent`-et és "Kész" `state`-et ad
    - `calculateReadinessPercent`/`calculateDecisionScore` a "general" playbook súlyaival PONTOSAN ugyanazt az eredményt adja, mint a legacy hardcoded `src/lib/project.ts` függvényei ugyanarra a bemenetre (regresszió-mentesség bizonyítéka)
    - `collectReadinessGaps` a `ReadinessGap.targetTab`/`targetField`/`checklistItemId`/`followUpId` szerződést változatlanul adja vissza (D-08 fixGap-kompatibilitás)
    - `createFollowUpFromChecklist(project, playbook, itemId)` a playbook megfelelő tételéből (nem globális `checklistTemplate`-ből) tölti a `category`/`question` mezőket
  </behavior>
  <read_first>
    src/lib/project.ts (a TELJES fájl — `checklistWeightedRatio`, `followUpResolutionRatio`, `addGap`, `collectReadinessGaps`, `calculateReadinessPercent`, `readinessState`, `scaleToPercent`/`inverseScaleToPercent`, `calculateDecisionScore`, `decisionLabel`, `decisionRecommendation`, `nextRecommendedAction`, `calculateCompletion`, `emptyCompletion`, `createFollowUpFromChecklist` — ez a PONTOS forrás-képlet, amit playbook-paraméteresre kell alakítani, MINDEN aritmetikai lépés változatlan marad),
    .planning/phases/02-felm-r-si-flow-s-coaching/02-PATTERNS.md ("src/domain/scoring/readiness.ts, decisionScore.ts, gaps.ts, completion.ts" szakasz — pontos import-csere minta és a legacy sor-tartományok),
    src/domain/model/types.ts (a MÁR LÉTEZŐ `Project`, `ProjectCompletion`, `ReadinessGap`, `DecisionScores`, `FollowUpQuestion` típusok — ezeket importáld, NE definiáld újra),
    src/content/playbook/types.ts (a Task 1-ben létrehozott `Playbook`/`PlaybookItem`/`PlaybookWeights` — ezekre paraméterezel)
  </read_first>
  <action>
    Hozd létre a `domain/scoring/` 5 fájlját, MINDEGYIKBEN a legacy `src/lib/project.ts` MEGFELELŐ függvényének 1:1 aritmetikai másolatával, DE minden helyen, ahol a legacy a globális `checklistTemplate`-et importálta vagy a hardcoded súlyt (0.2, 0.3, 0.25 stb.) használta, cseréld egy `playbook: Playbook` paraméterre és `playbook.items`/`playbook.weights.*`-ra:

    - `src/domain/scoring/readiness.ts`: `checklistWeightedRatio(project, playbook)`, `followUpResolutionRatio(project)` (ez NEM playbook-függő, változatlan marad), `calculateReadinessPercent(project, playbook)`, `readinessState(readinessPercent, criticalGaps)`.
    - `src/domain/scoring/decisionScore.ts`: `scaleToPercent`/`inverseScaleToPercent` (playbook-független segédek), `calculateDecisionScore(project, playbook, readinessPercent)`, `decisionLabel(score)`, `decisionRecommendation(...)`, `nextRecommendedAction(...)`.
    - `src/domain/scoring/gaps.ts`: `addGap(...)` (változatlan), `collectReadinessGaps(project, playbook)` — a `for (const item of checklistTemplate)` ciklus `playbook.items`-re vált; a `hasText`/`scoreTextFields` segédfüggvényeket is ide (vagy egy közös `helpers.ts`-be) told át, mert a legacy fájlban is itt élnek.
    - `src/domain/scoring/completion.ts`: `calculateCompletion(project, playbook): ProjectCompletion`, `emptyCompletion(playbook): ProjectCompletion` (FIGYELEM: a legacy `emptyCompletion()` a globális `checklistTemplate.length`-et használja `totalItems`/`missingItems`-hez — ez most `playbook.items.length`-re paraméterezendő, tehát `emptyCompletion` is playbook-paramétert kap, eltérően a legacy 0-paraméteres verziótól).
    - `src/domain/scoring/followups.ts`: `createFollowUpFromChecklist(project, playbook, checklistItemId): FollowUpQuestion` — a `template = playbook.items.find(item => item.id === checklistItemId)` mintával a legacy `checklistTemplate.find(...)` helyett.

    Domain-tisztasági szabály: EGYIK fájl se importáljon `rxdb`/`dexie`/`react`-ot vagy `adapters/`/`features/` alatti fájlt — csak `../model/types`, `../../content/playbook/types`, és egymást.

    Hozd létre `src/domain/scoring/scoring.test.ts`-t: építs egy minimál teszt-fixture-t (`makeProject(overrides)` helper, a `src/test/builders.ts` LEGACY mintájára — de az ÚJ `domain/model/types`+`domain/model/factory` alapján, NEM a régi `src/lib/project.ts`-ből importálva, mert az domain-tisztátalan lenne), és 4-5 tesztesetet a fenti `<behavior>` szerint. A "regresszió-mentesség" tesztesethez: vegyél egy konkrét, kézzel kiszámolt bemenetet (pl. néhány checklist-tétel "Kész", néhány "Nincs meg", pár overview-mező kitöltve), számold ki KÉZZEL (vagy a legacy `src/lib/project.ts` függvényeit ideiglenesen meghívva egy scratch-számításban, NEM a végleges tesztkódban) a várt `readinessPercent`/`decisionScore` értéket a "general" playbook súlyaival, és asszertáld, hogy az új `calculateReadinessPercent(project, playbooks.general)` ugyanazt adja.
  </action>
  <acceptance_criteria>
    - `grep -rc "checklistTemplate" src/domain/scoring/` összesen 0 (egyik scoring-fájl sem importálja a globális legacy sablont)
    - `grep -c "playbook: Playbook" src/domain/scoring/readiness.ts src/domain/scoring/decisionScore.ts src/domain/scoring/completion.ts src/domain/scoring/gaps.ts src/domain/scoring/followups.ts` összesen legalább 5
    - `pnpm vitest run src/domain/scoring/scoring.test.ts` zöld
  </acceptance_criteria>
  <verify>
    <automated>pnpm vitest run src/domain/scoring/scoring.test.ts && pnpm exec tsc --noEmit 2>&1 | grep -E "domain/scoring" ; echo "exit:$?"</automated>
  </verify>
  <done>Az 5 scoring-modul playbook-paraméteresen, a legacy képlettel megegyező eredményt ad; nincs globális `checklistTemplate` import.</done>
</task>

</tasks>

## Artifacts this phase produces

| Artifact | Type | Exports |
|---|---|---|
| `src/content/playbook/types.ts` | type module + Zod | `PlaybookItem`, `PlaybookWeights`, `Playbook`, `PlaybookSchema` |
| `src/content/playbook/general.ts` | static data | `general: Playbook` (30 tétel) |
| `src/content/playbook/index.ts` | catalog | `playbooks: Record<string, Playbook>` |
| `src/domain/scoring/completion.ts` | pure functions | `calculateCompletion(project, playbook)`, `emptyCompletion(playbook)` |
| `src/domain/scoring/readiness.ts` | pure functions | `calculateReadinessPercent(project, playbook)`, `readinessState(...)` |
| `src/domain/scoring/decisionScore.ts` | pure functions | `calculateDecisionScore(project, playbook, readinessPercent)`, `decisionLabel(...)`, `decisionRecommendation(...)`, `nextRecommendedAction(...)` |
| `src/domain/scoring/gaps.ts` | pure functions | `collectReadinessGaps(project, playbook)` |
| `src/domain/scoring/followups.ts` | pure function | `createFollowUpFromChecklist(project, playbook, checklistItemId)` |

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Statikus `content/playbook/general.ts` -> domain/scoring bemenet | Hibás/hiányos statikus tétel-lista vagy súly-konfiguráció csendben téves readiness/decision score-t termelhet |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-03-01 | Tampering (adat-integritás) | `content/playbook/general.ts` (30 tétel, súlyok) | medium | mitigate | Task 1 Zod-séma (`PlaybookSchema.parse`) + smoke-teszt (item-szám, id-tartomány, hint-hiány) build/teszt-időben elkapja a hiányos/hibás bejegyzést |
| T-02-03-02 | Tampering (regresszió) | `domain/scoring/*` playbook-paraméterezés | high | mitigate | Task 2 tdd="true": explicit regresszió-teszt a legacy képlettel megegyező eredményre, mielőtt bármely Wave-2 UI erre épülne |

</threat_model>

<verification>
- `pnpm vitest run src/content/playbook src/domain/scoring` mind zöld
- `pnpm exec tsc --noEmit` hiba nélkül lefut az érintett fájlokra
- `grep -rc "checklistTemplate" src/domain/scoring/` == 0
</verification>

<success_criteria>
- Playbook-katalógus és playbook-paraméteres scoring-motor létezik, a legacy képlettel bizonyítottan egyező eredménnyel
- SURVEY-04/06/07 adat- és számítási alapja kész a Wave-2 UI-hoz
</success_criteria>

<output>
Create `.planning/phases/02-felm-r-si-flow-s-coaching/02-03-SUMMARY.md` when done
</output>
